# Передача: состояние работ по ускорению CSV → ClickHouse

Дата: 2026-07-27. Ветка `perf/phase-0-instrumentation` влита в `main` (PR #2, #3).
Последний коммит работ: `2c7b48b` в ветке `fix/inference-leading-zeros`.
Тестов 427, все проходят за ~6,4 с.
**Итог по скорости: 5,5 ГБ / 500 млн строк за 369,6 с против 1949 с — 5,3×.**

Этот документ — единственное, что нужно прочитать целиком, чтобы продолжить.
Остальные чекпойнты — фундамент, к ним отсылки по месту.

---

## 1. С чего начинали и что оказалось правдой

**Исходная задача:** 1 ГБ грузился ~45 минут, при повышении batch загрузка
падала. План (`superpowers-writing-plans-linked-hippo.md`, фазы 0–9) исходил из
того, что **клиентский CPU занимает 7–9% времени, а остальное — провод и
сервер**, и главным рычагом называл сжатие.

**Фаза 0 это опровергла.** Замеры на рабочем ПК с доступом к кластеру:

| | Значение |
|---|---|
| RTT до кластера | **8 мс**, потерь 0% |
| Продюсер (read+convert+serialize) | **99% времени вставки** |
| Сериализация | **90,1%** времени вставки |
| Сервер, на соединение | 3,2% |

Прежние «45 минут» снимались на **личном** ПК через домашний VPN и на рабочий
контур не переносятся. Оператор подтвердил: это были субъективные часы, а не
строка приложения.

**Закрыто окончательно, переоткрывать не надо:**

- BDP-гипотеза и пин `SO_SNDBUF` — при RTT 8 мс окно 256 КиБ разрешает
  32,8 МБ/с, в 86 раз больше наблюдавшегося.
- Прокси НЕ срезает `X-ClickHouse-Summary` (`blocks_without_server_time: 0`).
- Молчаливых перезаливок драйвера нет (`driver_retries: 0`).
- TLS до кластера НЕ перехватывается: издатель `LiteSSL ECC CA 2025, TrustAsia`
  — публичный CA. (Ошибка winget `0x8a15005e` была про Microsoft Store.)
- Число HTTP-запросов не при чём: рост `batch_size` в 10 раз не изменил
  скорость (168 132 против 165 789 строк/с). Стоимость — **на строку**.
- Падение при росте batch **не воспроизводится**: 500k строк и 30 МБ payload
  проходят.
- `tp17.wb-bank.ru` — не один узел: `remote_network` менялся между прогонами с
  `10.31.0.0/16` на `10.24.0.0/16`. Это закрывает открытый вопрос №8 инвентаря.

---

## 2. Что сделано

| Коммит | Что |
|---|---|
| `a00ba77` | Документы анализа (118 находок + 16 «неизвестных неизвестных») |
| `cdaa12f` | **Фаза 0**: инструментовка, раздельные часы, запись о прогоне |
| `1a7e9c7` | Ретроспектива фазы 0 |
| `d772f70` | `loader.bat` находит `uv` вне `PATH` |
| `2339005` | **Фаза 3**: векторная сериализация через `DataFrame.to_json` |
| `52961a0` | **Фаза 3b**: путь загрузки читает файл как превью |
| `49fe1d4` | Векторизация `convert`, нативный разбор чисел |
| `fea63f9` | **Фаза 7, F7**: ведущие нули, плюс и не-ASCII цифры не типизируются числом |
| `6bb1e62` | **Фаза 8a**: запись о прогоне не врёт про исход, троттлинг интерфейса |
| `448f8e2` | **Фаза 3c**: JSONEachRow собирается через Arrow, байт-в-байт тот же |
| `aa2f846` | **Фаза 0b**: запись отвечает, кто кого ждал во время вставки |
| `7927491` | **Фаза 2**: сжатие тела вставки, по умолчанию выключено |
| `435a51b` | Выключатель `off` больше не уезжает в `Content-Encoding` |
| `2c7b48b` | Сжатие перенесено в воркеры: zlib отпускает GIL |

**Замеренный результат конец в конец на реальной выгрузке оператора: 3,7×.**
167 325 → 618 690 строк/с. Сериализация отдельно: 5,6×.

По стадиям, мкс на строку, 500k строк:

| Профиль | Было | Стало |
|---|---:|---:|
| 1 колонка String (профиль оператора) | ~2,6 | **0,650** |
| 1 колонка UInt64 | ~2,0 | **0,432** |
| 5 смешанных колонок | ~8,7 | **4,048** |

---

## 3. Ведущие нули: закрыто, с одной оговоркой

**Сделано, `fea63f9`.** Инференс больше не выбирает числовой тип, если разбор
съел бы текст: ведущий ноль перед цифрой, ведущий плюс, не-ASCII цифры (`\d`,
`float` и `Decimal` в Python юникодные и переписывают `١٢٣٤` в `1234`). Колонка
становится `String`, причина пишется в `notes`. Руками ставить String больше не
нужно. Это находка F7 инвентаря.

Валидация осталась разрешающей намеренно: если оператор САМ выбрал `UInt64`,
`00123` по-прежнему уедет как `123`. Инференс выбирает за оператора и обязан
быть осторожным, оператор выбирает сам и получает то, что попросил.

**Оговорка, которую оператор обязан знать.** Решение принимается по
ПРОСМОТРЕННЫМ строкам. В режиме по умолчанию (`Fast sample`, 100 000 строк)
первое значение с ведущим нулём может лежать за выборкой — тогда колонка
останется числом. И `Strict preflight validation` этого НЕ поймает: разбор
`00123` в число ошибки не даёт, в отличие от мусора вроде `N-123`, на котором
префлайт падает громко с номером строки. То есть для ЭТОГО класса совет «Full
scan ИЛИ префлайт» неверен — помогает только Full scan. Проверено на файле в
100 001 строку; до правки то же значение портилось в ОБОИХ режимах, так что
правка дыру сузила, а не открыла. Оговорка написана в справке шага Type review.

---

## 4. Что делать дальше, в порядке замеренной ценности

Порядок фаз из плана **больше не годится** — измерения его переставили.

### 4.0. Замер на настоящей выгрузке, 2026-07-27, 15:25

Прогон оператора: 5,5 ГБ, **190,5 млн строк**, 381 блок по 500 000, одна колонка
`nmid`, 5 воркеров, `Fast sample`. Оператор остановил его сам, то есть в
`sandbox.streamlit_tst_big1` лежит ПРЕФИКС файла.

| Стадия | Время | Доля `insert_wall` | На строку |
|---|---:|---:|---:|
| Сериализация | 215,77 с | **70,3%** | 1,133 мкс |
| Чтение | 51,52 с | 16,8% | 0,270 мкс |
| Конвертация | 23,17 с | 7,6% | 0,122 мкс |
| Продюсер всего | 290,46 с | **94,7%** | 1,525 мкс |
| Сервер (СУММА по 5 воркерам) | 82,34 с | ≈16,5 с стенных | 216 мс/блок |

621 100 строк/с, 11,8 МБ/с в провод, чтение файла 107 МБ/с. Подтверждено на 190
млн строк, а не на бенчмарке: **продюсер 94,7%, сериализация 70%**. Провод и
сервер по-прежнему ни при чём. Ещё раз подтверждено: прокси сводку не срезает (0
блоков без времени), перезаливок драйвера 0. `server_share: null` при 5 воркерах
— так задумано, не дефект.

### 4.0-бис. Полный прогон, 500 млн строк, и что он изменил

Второй прогон оператора того же файла — **до конца**: 499 992 014 строк, 1000
блоков, 818 с, `outcome: ok`, `source_fully_read: true`,
`blocks_unconfirmed: 0`. Поля из фазы 8a отработали: полнота таблицы читается
из записи, без запросов в ClickHouse.

| Стадия | Время | Доля | На строку |
|---|---:|---:|---:|
| Сериализация | 576,39 с | **70,5%** | 1,153 мкс |
| Чтение | 139,34 с | 17,0% | 0,279 мкс |
| Конвертация | 62,24 с | 7,6% | 0,124 мкс |
| Продюсер | 777,97 с | **95,1%** | 1,556 мкс |
| Сервер (сумма по 5 воркерам) | 201,87 с | ≈40 с стенных | 202 мс/блок |

611 100 строк/с. Стоимость на строку совпала с прерванным прогоном до третьего
знака — инструмент воспроизводим.

### 4.0-трижды. Прогон после Arrow: узкое место переехало в провод

Третий полный прогон того же файла, уже с Arrow: **1949 с против 818 с**, то
есть в 2,4 раза МЕДЛЕННЕЕ, хотя сериализация ускорилась в 5,5 раза.

| Стадия | Прогон 4 (до Arrow) | Прогон 5 (после) |
|---|---:|---:|
| Сериализация | 576,4 с | **105,3 с** |
| Продюсер всего | 778,0 с (95,1%) | **289,1 с (14,8%)** |
| Ожидание HTTP | 40,2 с | **1659,5 с (85,2%)** |
| Провод | 11,65 МБ/с | **4,89 МБ/с** |
| Сервер (сумма) | 201,9 с | 215,6 с |

`raw_bytes` совпал байт в байт — Arrow-путь отдал ровно те же данные.

**Код оправдан, и это проверено, а не предположено.** Шесть гипотез, каждая со
своим скептиком:

- *Arrow отбирает процессор у TLS* — опровергнуто. Ядра Arrow не параллельны
  (cpu/wall = 1,0), пул потоков не создаётся, TLS стоит 1,09 мс CPU на МБ, то
  есть 9,9 CPU-секунд на весь прогон. **Знак обратный**: `to_json` держит GIL, и
  конкурирующий поток сохраняет 36,5% скорости, а под Arrow — 89,1%.
- *GIL душит ввод-вывод* — опровергнуто тем же измерением.
- *Путь отправки изменился* — опровергнуто: тело в обоих случаях `bytes`, число
  запросов то же, сжатия не было.
- **Убийственная арифметика:** в прогоне 5 продюсер 1659 с ПРОВЕРЯЕМО стоял
  заблокированным, все ядра свободны, и провод в этом окне дал 5,75 МБ/с — вдвое
  меньше, чем 11,65 МБ/с прогона 4, где продюсер занимал процессор 95% времени.
  Больше свободного процессора у отправителей — вдвое медленнее провод.

**Чего мы не знаем и не можем узнать из одного прогона.** Код менялся вместе с
временем суток (16:46 против 22:08 UTC) и подсетью (10.108 против 10.31). Это
вырожденная диагональ: n = 1 в каждой ячейке.

**Что выяснилось попутно и важнее самой гипотезы.** Драйвер прибивает
`SO_SNDBUF` = 256 КиБ, что при RTT 8 мс разрешает 32,8 МБ/с на соединение.
Наблюдалось 2,33 и 0,98 МБ/с — 7,1% и 3,0% буфера. **Ни один прогон не упирался
в окно отправителя.** Оба упирались в то, как быстро приёмная сторона вычерпывает
сокет. И прогоны 2 и 4 мерились «в колене»: продюсер предлагал 12,3 и 12,4 МБ/с,
путь отдавал 11,65 и 11,78 — это нижняя граница ёмкости, не верхняя. Прогон 5 —
первый, где путь попросили о 33 МБ/с.

Отсюда живая версия, где код виноват КОСВЕННО: **полисер**. Давить 33 МБ/с в
путь, который режет на пяти, — это потери и коллапс окна, тогда как 12 МБ/с
проходили под лимитом. Разводится контрольным прогоном с `load_workers=1`: если
на соединение остаются те же ~1 МБ/с, потолок на СОЕДИНЕНИЕ.

### 4.0-четырежды. Контрольный прогон: потолок стоит на АГРЕГАТЕ

Прогон с `load_workers=1` и выключенным сжатием, 2026-07-28 00:16, подсеть
10.108. Прерван на 31 блоке, 15,5 млн строк — этого хватило.

| Величина | Значение |
|---|---:|
| Занятость воркера | **89,3%** |
| Продюсер (чтение+конвертация+сериализация) | 8,46 с = **10,5%** стенных |
| Одна вставка | 2,319 с на блок 9,38 МБ |
| Сервер внутри вставки | **8,5%** |
| Пропускная способность | 3,61 МБ/с |

Новые поля фазы 0b ответили ровно на то, ради чего заводились: клиент НЕ узкое
место. Продюсер занимает 10,5%, воркер занят 89,3%, сервер отчитывается о 8,5%
времени вставки. Остальное — путь между клиентом и ClickHouse.

**Сравнение по числу соединений на одном и том же контуре, десятичные МБ/с,
знаменатель `insert_wall_s`:**

| Прогон | Воркеров | Время | Агрегат |
|---|---:|---|---:|
| 4, до Arrow | 5 | 16:46 | **11,65 МБ/с** |
| 5, после Arrow | 5 | 22:08 | 4,89 МБ/с |
| 7, контроль | 1 | 00:16 | 3,61 МБ/с |

Два вывода, и оба меняют планы:

1. **Пять соединений дают 1,35× против одного, а не 5×.** Потолок стоит на
   АГРЕГАТЕ, а не на соединении. Значит `load_workers` — почти неработающая
   ручка на этом контуре, а не рычаг.
2. **Ёмкость пути меняется по времени суток**: 11,65 МБ/с в 16:46 против
   4,89 МБ/с в 22:08 на ОДНОЙ И ТОЙ ЖЕ подсети, 2,38×. Это и был конфаундер,
   из-за которого прогон 5 выглядел регрессией кода. Код к нему не причастен.

**zstd отвергнут окончательно** — два независимых прогона, `unsupported
compression method zstd` от того, что стоит перед ClickHouse. lz4 и gzip не
пробовали.

**Что из этого следует для дальнейшей работы.** Клиентская часть выжата: она
занимает 10% времени. Единственный рычаг, полностью в наших руках и не зависящий
ни от кодеков, ни от времени суток, — **меньше байт**. TabSeparated даёт 5,2 МБ
вместо 9,49 на том же блоке, то есть в 1,82 раза меньше: при 4,89 МБ/с это
~1070 с вместо 1949. Формат — наш выбор в теле запроса, а не заголовок, поэтому
прокси возразить нечем.

### 4.0-пятью. ИТОГ: 369,6 с на полном файле, и где теперь пол

Полный прогон 2026-07-28 01:06, gzip в воркерах: **499 992 014 строк за 369,6 с**,
`outcome: ok`, `source_fully_read: true`, `blocks_unconfirmed: 0`.
**1 358 911 строк/с**, сжатие 6,08× (9,54 ГБ → 1,57 ГБ).

| Прогон того же файла | Время | Против ночного 1949 с |
|---|---:|---:|
| без сжатия, ночь | 1949 с | 1,00× |
| без сжатия, лучший дневной | 818 с | 2,38× |
| gzip в продюсере (пересчёт) | 530 с | 3,68× |
| **gzip в воркерах** | **370 с** | **5,30×** |

**Где узкое место теперь — нигде в отдельности.** Продюсер занят 79% и стоял
21%; воркеры заняты 81%; сервер внутри вставки 17%. Провод: 1,57 ГБ при
4,26 МБ/с = 368 с, то есть ровно стенное время. Продюсер как пол — 291 с.

Провод и продюсер сошлись почти в одну цифру. Перекоса, который можно было бы
выправить, больше нет.

**Что осталось и чего оно стоит, честно:**

- *Параллельные продюсеры* дали бы пол 168 вместо 291 с, но провод всё равно
  требует ~365 с. Выигрыш ≈ 0. **Не делать.**
- *TabSeparated поверх gzip*: gzip уже съел повторяющиеся имена полей, так что
  экономия не 1,82×, а порядка 20–25%. Это ~290 с, и там упрётся продюсер.
  Цена — новый формат сериализации с риском тихой порчи и без эталона для
  сверки. **Плохой размен.**
- *Уровень gzip 9*: 3,40× против 3,32× на синтетике при 7 МБ/с. **Нет.**
- *Больше воркеров*: путь режет агрегат (5 против 1 дали 1,35×). **Почти ноль.**

**Вывод: работу по скорости считать законченной.** 5,3× при тех же условиях и
2,2× против лучшего, что эта выгрузка показывала. Дальше ценность выше в фазе 7,
где данные портятся или теряются.

### 4.1. Несколько продюсеров: ПОТОКИ МЕРТВЫ, приз возвращает Arrow

Замерено, не оценено: **`to_json` не отпускает GIL**. Четыре потока на двадцати
ядрах дают **1,01×**. `ProcessPoolExecutor` отвергнут ранее (Windows `spawn`
плюс pickle DataFrame). Значит «несколько продюсеров» как отдельная фаза
поверх `to_json` не работает вообще.

**Arrow GIL отпускает: 2,25× на четыре потока.** Но после контрольного прогона
(раздел 4.0-четырежды) приз обесценился: продюсер занимает 10,5% времени, а
потолок стоит на агрегате провода. Ускорять десятую часть, когда девять десятых
ждут сеть, смысла нет. **Параллельные продюсеры отложены до тех пор, пока
клиент снова не станет узким местом.**

### 4.1-бис. Что фаза 3c уже дала и чего стоила

Сделано в `448f8e2`. Быстрый путь берётся только при БАЙТ-В-БАЙТ совпадении с
`to_json`, иначе чанк идёт эталоном. Замерено на чанках по 200 тыс. строк:

| Профиль | Эталон | Arrow | |
|---|---:|---:|---|
| 1 колонка UInt64 | 0,288 | **0,081** | 3,5× |
| 1 колонка String | 0,395 | 0,240 | 1,6× |
| 4 смешанные колонки | 0,833 | 0,561 | 1,5× |
| 5 колонок, одна Decimal | 1,115 | 1,109 | без изменений, отказ бесплатный |
| 1 колонка Float64 | 0,312 | 0,316 | без изменений, отказ |
| String, где экранировать надо каждое значение | 0,296 | 0,351 | **1,2× МЕДЛЕННЕЕ** |

Последняя строка — честная цена. Три прохода `replace_substring` перевешивают
выигрыш, когда почти в каждом значении есть кавычка, слэш или обратный слэш.
Один проход регуляркой с обратной ссылкой замерен и **вдвое медленнее** трёх
литеральных замен, поэтому оставлены замены.

Быстрый путь покрывает **12 типов из 18** в редакторе. `Float64` и `Decimal`
отказаны: формат чисел у `to_json` свой, совпадение не доказано.

Чего НЕ надо переоткрывать: отказ обязан быть дешёвым. `from_pandas` на
Decimal-объектах стоит 0,855 мкс/строку, и конвертация ради отказа делала кадр
с одной такой колонкой в 2,15 раза медленнее, чем до правки. Поэтому решение о
пригодности принимается по dtype и первым значениям ДО любой конвертации.

### 4.1-трижды. Что сделано против нового узкого места

**Фаза 0b, `aa2f846` — сначала измерить.** Прежняя запись не отличала «ждём
провод» от «ждём сервер» от «воркеры простаивают»: 1659 с были получены
ВЫЧИТАНИЕМ, а не замером. Добавлены три поля: `producer_stall_s` (оборачивает
ровно ожидание готового блока), `insert_busy_s` (стенные часы внутри воркера
вокруг `raw_insert`), `insert_queue_s` (от `submit` до начала отправки). И три
производных: `server_share_of_insert` (знаменатель — сумма вставок, поэтому
работает при любом числе воркеров, в отличие от `server_share`),
`worker_occupancy`, `producer_unattributed_s`.

Как читать следующий прогон: занятость около 1,0 вместе с большой стоянкой —
узкое место в проводе или сервере; занятость заметно ниже — в продюсере.

**Фаза 2, `7927491` — сжатие, по умолчанию ВЫКЛЮЧЕНО.** Замерено на форме
payload оператора (блок 9,49 МБ):

| Кодек | Сжатие | Скорость | Провод 9,5 ГБ при 4,89 МБ/с |
|---|---:|---:|---:|
| **zstd** | **3,76×** | 493 МБ/с | 516 с (+19 с на сжатие) |
| gzip | 3,32× | 34 МБ/с | 585 с (+283 с) |
| lz4 | 1,93× | 878 МБ/с | 1004 с (+11 с) |

Жмёт ПРОДЮСЕР, а не воркер: продюсер простаивает 85% времени, а часы воркера —
единственный измеритель провода, и мешать в них процессор нельзя.

Почему выключено по умолчанию: `Content-Encoding` — одна из трёх переменных,
которыми отличался падающий на этом контуре `insert()`. Включённое сжатие
проверяется первым же блоком за секунды, а не через полчаса.

**GZIP ПРОШЁЛ. Это ответ, ради которого фаза делалась.**

Прогон 2026-07-28 00:38, 5 воркеров, 974 блока из 1000:

| Величина | Значение |
|---|---:|
| Сжатие на РЕАЛЬНЫХ данных | **6,08×** (на синтетике было 3,32×) |
| Строк в секунду | **943 436** |
| Пересчёт на полный файл | **~530 с** против 1949 с ночью, 3,7× |
| | и против 818 с лучшего дневного, 1,54× |

И сразу же узкое место перевернулось обратно: продюсер занят **99,5%**, из них
**53% — gzip в одном потоке**, а воркеры простаивают 74%.

Поэтому `2c7b48b`: **сжатие перенесено в воркеры.** zlib отпускает GIL —
замерено 4,5× на пяти потоках. Продюсер падает с 513,7 до 242 с, оставшиеся
272 с процессора расходятся по пяти потокам. На фейковом клиенте сквозная стена
0,52 → 0,24 с при неизменной сумме сжатия.

Часы вставки по-прежнему начинаются ПОСЛЕ сжатия: `insert_busy_s` обязан
остаться измерителем провода. Время сжатия возвращается вместе с блоком и
складывается в главном потоке — `float +=` из пяти воркеров теряет слагаемые.

Уровень gzip не трогали. Замерено: уровень 1 даёт 148 МБ/с против 35 у
шестого, а сжатие падает лишь с 3,32× до 2,94×. Но после переноса в воркеры
процессор перестал быть дефицитом, и меньше байт важнее.

**zstd ОТВЕРГНУТ, `435a51b`.**

```
HTTP 500: clickHouse engine unsupported compression method zstd
```

Отвергает не ClickHouse, а то, что стоит перед ним. Список поддерживаемых
кодеков угадать нельзя — только перебрать, и перебор дал ответ: **gzip проходит,
zstd нет.** lz4 не пробовали и уже не нужно: он жмёт вдвое хуже gzip.

И там же вскрылся мой дефект, за который заплатили два прогона оператора:
приложение отдавало выключатель строкой `off`, драйвер делал из неё
`Content-Encoding`, контур отвечал `unsupported compression method off` — то
есть **ломался путь ПО УМОЛЧАНИЮ**. Тесты это пропустили, потому что передавали
`None` там, где приложение передаёт `"off"`, а фейк принимал любое значение, не
ведя себя как драйвер. Теперь выключатель нормализуется в `None` в одном месте, а
фейк отвергает незнакомый `Content-Encoding`, как это делает контур.

### 4.2. Фаза 7 — тихие порчи данных

Ни один пункт не про скорость, все про то, что данные портятся правдоподобно.

- ~~Ведущие нули в инференсе~~ — сделано, `fea63f9`, раздел 3.
- **Префлайт не видит потерь, которые не ломают разбор** (раздел 3). Чинить —
  значит добавить в `validate_csv_against_schema` проверку обратной сборки
  текста, а не расширять инференс: инференс видит только выборку, префлайт
  читает весь файл. Найдено тремя линзами ревью, замерено.
- Нет проверки величины: 20+ значащих цифр выводятся как `UInt64` и роняют
  загрузку на переполнении.
- Точность `Decimal` не отслеживается: `Decimal(18,2)` выбирается для значений,
  которым нужно 22+ цифр.
- Даты: 1950 год или сентинел 1900 переполняют `Date`; офсет часового пояса
  инферится в naive `DateTime` (на пути загрузки я это сделал громкой ошибкой,
  но инференс всё ещё типизирует такую колонку как `DateTime`).
- **Автоудаление обеих таблиц при любом сбое загрузки.** Один транзиентный
  HTTP 5xx на 487-й вставке из 500 уничтожает всё уже залитое.
- `ORDER BY` и ключ шардирования молча сбрасываются на первую колонку при
  rerun (`st.selectbox` без `key=`).

### 4.3. Фаза 8 — эргономика

Сделано в `6bb1e62` (фаза 8a):

- Отметка успеха ставится сразу после возврата загрузчика, до любого `st.*`.
  Прошедшая загрузка больше не записывается прерванной, а сбой отчёта больше не
  уводит в `except Exception`, где **удалялись обе залитые таблицы**.
- Сбой отчёта и описан как сбой отчёта: раньше текст шёл через
  `_format_load_error`, и оборванный сокет объявлялся невидимой таблицей.
- `source_fully_read` и `blocks_unconfirmed` в записи. «Таблица полная» =
  оба плюс `outcome == "ok"`. Докстринги говорят, чего каждое поле НЕ обещает.
- Интерфейс трогается не чаще раза в секунду, лог шлёт ограниченный хвост с
  числом скрытых строк, плитка обновляется в конце. На 381 блоке это было 1143
  `st.*`-вызова и O(n²) в сокете.

Осталось:

- **Загрузку по-прежнему нельзя отменить, и rerun её убивает.** Правка сделала
  последствия честными и неразрушительными, но не спасла саму загрузку. Чинить —
  значит уносить продюсера с потока скрипта, и там ждут ловушки
  `st.session_state` из инвентаря.
- **После rerun экран стирается, и оператор не видит, что осталось.** Оба новых
  поля есть только в JSON: `format_load_stats_lines` зовётся лишь на успешном
  пути, где они всегда «всё хорошо». Естественная следующая фаза — показывать
  состояние последнего прогона из `st.session_state` после перерисовки. Строку
  предупреждения в форматтер я добавил и удалил: она недостижима, а мёртвого
  кода тут не держим.
- **Запись не говорит, отработала ли чистка.** При `outcome: failed` оператор не
  отличит «обе таблицы удалены» от «обе на месте»: ошибка чистки уходит только в
  лог, а `error_message` затем перезаписывается исходной ошибкой загрузки.
- **Последовательный путь не отличает прерывание на последнем блоке от
  прерывания на середине** — для этого нужен ещё один `next()` у итератора
  чанков. Оператор грузит с воркерами, где такой неоднозначности нет.
- Прогресс-бар мёртв для файлов больше 50 МБ (`total_rows` остаётся 0). Долю
  можно считать по прочитанным БАЙТАМ, а не строкам.
- README описывает интерфейс, которого нет: нет кнопки «Analyze CSV», нет
  таблицы «Schema», не упомянуты обязательные «Apply column mapping» и «Apply
  types», не описаны `Load workers` и режим инференса. Показан неверный путь
  ZooKeeper для `ReplicatedMergeTree`.

### 4.4. Два блокера фазы 6

Без них фаза 6 не отгрузится работающей:

- `_convert_series` не умеет узкие int-типы: `Int32`/`UInt16` сериализуются
  строкой `'1.0'`, ClickHouse отвергнет.
- `Nullable()` оборачивается снаружи `LowCardinality()`, давая невалидный
  `Nullable(LowCardinality(String))`. Фикс нужен общий, не точечный: так же
  ломаются `Array(...)` и `Map(...)`.

### 4.5. Один дешёвый замер

`pd.to_datetime(series, errors="raise")` в `_format_temporal` вызывается без
явного формата, и pandas прямо предупреждает в логе, что сваливается в разбор
каждого значения через `dateutil`. На файле с датами это может быть заметной
долей `convert` (в смешанном профиле `convert` = 1,73 из 4,05 мкс/строку).
Проверить `format=` или `format="ISO8601"`.

### 4.6. Обесценено замерами — НЕ делать без новых оснований

- **Потоки поверх `to_json`.** GIL не отпускается: 1,01× на четырёх потоках,
  двадцать ядер. Приз возвращается только вместе с Arrow — см. 4.1.
- **Один проход регуляркой вместо трёх замен при экранировании.** Замерено:
  `replace_substring_regex` с обратной ссылкой вдвое медленнее трёх
  `replace_substring`.
- **Фаза 2, сжатие.** Провод не стена: RTT 8 мс, сервер 5%. Отдельно: сжатие
  сократило бы трафик вчетверо, что имеет смысл, только если канал общий.
- **Фаза 1, транспортные однострочники.** `max_connection_age`, общий
  `PoolManager` — экономят секунды там, где их нет.
- **Фаза 4.** Падение при росте batch не воспроизводится.
- **Фаза 9, мёртвый код.** Чистая churn; делать последней.

---

## 5. Как здесь работать, чтобы не наступить на грабли снова

Подробности — в `2026-07-27-phase0-retrospective.md`. Кратко самое дорогое:

**Зелёные тесты тут systematically не означают работающий код.** 107 тестов
проходили при двух критических дефектах, делавших фазу 0 вредной. Тесты пишутся
вместе с кодом и наследуют то же неверное представление о поведении библиотеки.

**Что реально ловит:**

1. **Мутационная проверка.** Скриптом вернуть каждую исправленную поломку и
   убедиться, что тесты падают. Из 18 поломок 3 не ловились — три теста были
   декоративными. Тот же прогон дважды поймал дефект в свежей правке.
   Обязательно с таймаутом на прогон: одна из поломок зацикливает нарезку.

   Два урока с правки `fea63f9`. Первый: свой список мутаций слеп там же, где
   слеп автор — границу `> 1` против `> 2` и ASCII/юникод-щель нашло ревью, а
   мутации нет, потому что я не подумал их туда положить. Второй: оптимизация
   умеет сделать условие корректности нетестируемым. Кэширующая охрана в
   `add_value` заставила выжить мутацию гейта, хотя до неё та ловилась. Держите
   в списке ЭКВИВАЛЕНТНУЮ мутацию — снять оптимизацию и убедиться, что тесты
   остались зелёными: она отвечает на вопрос «это ускорение или это теперь
   носитель корректности».

   Третий урок, с фазы 8a: **мутация, которая не компилируется, не проверяет
   ничего**, а в отчёте выглядит пойманной. Одна из моих превратилась в
   `IndentationError`, и прогон честно упал — но не из-за теста. Различайте в
   выводе «1 failed» и «1 error». В фазе 3c это уже автоматизировано: скрипт
   сам считает мутацию непроверенной, если в сводке `error` без `failed`.

   Пятый урок, с фазы 2: **фейк обязан повторять не форму возврата, а
   ПОВЕДЕНИЕ библиотеки на аргументе.** Мой фейк принимал `compression` любым
   значением, а настоящий драйвер делает из любой непустой строки заголовок.
   Из-за этого `off` уехал в `Content-Encoding`, сломал путь по умолчанию и
   стоил двух прогонов оператора. Тесты при этом были зелёные, потому что в них
   передавался `None`, а приложение передаёт `"off"`: **проверяйте то значение,
   которое реально ходит по пути, а не удобное.**

   Четвёртый урок, с фазы 3c: **гоночный тест проходит по случайности ровно до
   того дня, когда что-то ускорится.** Утверждение про отменённые блоки жило две
   фазы: с мгновенным фейком все отданные блоки успевали завершиться, и отменять
   было нечего. Ускорение сериализации сдвинуло тайминг, и тест упал. Если тест
   зависит от того, кто первый успел, — ставьте паузу или барьер, а не надейтесь.
2. **Состязательное ревью несколькими независимыми линзами** (инструмент
   `Workflow`). Семь линз по одному дифу, четыре независимо нашли один и тот же
   критический дефект. `caveman:cavecrew-reviewer` на том же дифе сказал «без
   замечаний» — одного ревьюера мало.

   **Дифференциальный фаззер и ревью ловят РАЗНОЕ, и нужны оба.** Фаза 3c: мой
   фаззер за 14 итераций нашёл неэкранированные ИМЕНА колонок (`q"q` давало
   сломанный JSON) — ни один ручной случай этого не подозревал. А ревью нашло
   три пути падения, которых фаззер найти не мог, потому что не генерировал
   колонок на 2 ГиБ: строковая колонка сверх 2 ГиБ приезжает `ChunkedArray`, и
   `ListArray.from_arrays` бросает обычный `TypeError` мимо `except
   pa.ArrowException`. Из 18 находок ревью 16 пережили скептиков — это худший
   диф сессии по плотности дефектов, и все они мои.
3. **Новое поле умеет врать в две стороны сразу.** `source_fully_read` из фазы
   8a обещал «все блоки отправлены» — на параллельном пути это было неправдой
   (часть блоков отменялась), а на последовательном неправдой было обратное
   («False значит префикс», хотя строки все). Четыре линзы из семи пришли к
   этому независимо. Лечится не переносом строки, а вторым полем
   (`blocks_unconfirmed`) и докстрингом, который говорит, чего поле НЕ обещает.
   Заодно: **перед тем как добавить сообщение оператору, проверьте, достижимо ли
   оно** — предупреждение в `format_load_stats_lines` оказалось мёртвым кодом.

4. **Отдельная линза на диф самих правок.** Раунды 2 и 3 нашли дефекты,
   которых до правок не было: двойной счёт блоков, `_stop` поверх метода
   `Thread`, потолок `SO_SNDBUF`, выпавший из сравнения. **Правки вносят
   регрессии не реже, чем исправляют.**
5. **Фейки обязаны воспроизводить РЕАЛЬНУЮ форму возврата библиотеки.**
   Проверять её выполнением кода библиотеки, а не догадкой.
6. **`uvx pyflakes`** — бесплатный статический контроль там, где нет ни линтера,
   ни типизации.

**Три ловушки инструментов, на которых я терял время каждый раз:**

- `Set-Content -Encoding utf8` в PowerShell пишет **BOM**. Трижды за сессию
  испортил файлы, включая закоммиченный. Правьте файлы инструментами
  Write/Edit или питоном, не PowerShell.
- Here-string в PowerShell ломается на кавычках внутри. Сообщения коммитов
  передавать только через `git commit -F файл`.
- `$` в регулярке PowerShell не совпадает при CRLF, и замена молча не
  применяется — проверяйте, что длина файла изменилась.

**Ловушки библиотек, проверенные выполнением:**

- `clickhouse-connect` НИКОГДА не возвращает пустую сводку: `_summary()`
  безусловно дописывает `query_id`. Признак «сервер не сообщил» — отсутствие
  `elapsed_ns`.
- `urllib3.RecentlyUsedContainer` бросает `NotImplementedError` на `.values()`
  и итерации; только `.keys()`.
- Пул соединений urllib3 — `LifoQueue`: следующий запрос получит ПОСЛЕДНИЙ
  элемент, перебирать `reversed()`.
- `threading.Thread` владеет именем `_stop` (а также `_target`, `_args`,
  `_started`, `_tstate_lock`). Не наследовать — композиция.
- `to_json` не имеет аналога `allow_nan=False`: `inf` молча становится `null`.
- `.dt.strftime` не дополняет год нулями: 1-й год уезжает как `1-01-01`.
  Использовать `np.datetime_as_string`.

---

## 6. Окружение

- **Рабочий ПК** (`C:\Users\Holdzhgonov.A\...`) — единственный с доступом к
  кластеру. RTT 8 мс, приватная сеть. `uv` поставлен через
  `py -3.12 -m pip install --user uv`; `winget` там требует прав администратора,
  которых нет. `loader.bat` теперь находит `uv` в каталоге пользовательских
  скриптов сам.
- **Личный ПК** — разработка. Кластер недоступен, `tp17.wb-bank.ru` не
  резолвится.
- Проверка: `.venv/Scripts/python.exe -m pytest -q` **из корня репозитория**
  (часть тестов читает файлы по относительным путям).
- Бенчмарки: `benchmarks/serialize_bench.py` (приёмка фазы 3),
  `benchmarks/tcp_info_probe.py` (диагностика транспорта, нужен кластер).
- Записи о прогонах: `~/.csv_click/runs/<utc>-<таблица>.json`. Пишутся из
  `finally`, то есть и при падении, и при прерывании.
- Репозиторий **публичный**. Оператор оставил так осознанно: нужен коллегам.
  В нём уже лежат корпоративный логин, личная почта в авторстве коммитов и
  реальные имена таблиц банка. Новых персональных данных не добавлять.
