# RAW EXTRACT from workflow journal wf_c666c8fb-659

118 unique findings (of 118), 134 verdicts

## FINDINGS

### F1. Validate PARTITION BY cardinality before CREATE TABLE (max_partitions_per_insert_block)
- WHERE: src/csv_click/clickhouse.py:204-207, src/csv_click/clickhouse.py:219, src/csv_click/clickhouse.py:227, src/csv_click/app.py:272, src/csv_click/app.py:372
- MECHANISM: ClickHouse hard-rejects any INSERT block touching more than `max_partitions_per_insert_block` partitions (default 100) with "Too many partitions for a single INSERT block". The number of distinct partitions per block scales with batch_size, so `PARTITION BY <date_col>` on a multi-year CSV works at batch=20000 and dies at batch=100000 — a direct, testable mechanism for the reported "raising batch/bucket beyond 20000 causes a crash". Independently: each block spanning P partitions writes P parts. At ~500 HTTP blocks per GB and P=30 that is ~15000 parts to merge for one load, which saturates the background merge pool and walks toward `parts_to_throw_insert` (default 3000 per partition), degradi
- FIX: (1) Replace the free-text box with a builder: `none` / `toYYYYMM(col)` / `toYear(col)` / `toStartOfMonth(col)` over Date/DateTime columns, plus an explicit "advanced expression" escape hatch. (2) Before create_tables(), evaluate the chosen expression over the pandas sample already read by analyze_csv_with_pandas_sample (app.py:642) and compute nunique; warn above 100 projected partitions, hard-block above 1000 unless overridden. (3) Additionally compute distinct partitions inside a rolling batch_size window and refuse the load if it exceeds the server limit, naming `max_partitions_per_insert_block` in the error. (4) Read the real limit at connection-test time: `SELECT value FROM system.setti
- GAIN: Converts a mid-load abort (unbounded wall time) into a pre-flight error. Where partitioning is currently over-granular, estimated 2-10x on sustained server-side ingest and merge load — estimate only, no live server reachable from this session. | EFFORT: M
- RISK: tests/test_clickhouse.py:84 asserts `"PARTITION BY toYYYYMM(id)" in sql` — still passes if the builder emits the same string. A new UI control changes _render_connection_and_load_form, so the source-scraping assertions in tests/test_app_state.py:137-140 style may need a sibling assertion added. Risk of over-blocking a legitimately partitioned load;

### F2. Prevent Nullable columns from being offered as ORDER BY / sharding key
- WHERE: src/csv_click/app.py:271, src/csv_click/app.py:273, src/csv_click/app.py:538-539, src/csv_click/clickhouse.py:223, src/csv_click/clickhouse.py:253, src/csv_click/schema.py:191, src/csv_click/pandas_loader.py:263
- MECHANISM: ReplicatedMergeTree refuses `ORDER BY <Nullable col>` unless `allow_nullable_key=1` ("Sorting key contains nullable columns, but merge tree setting allow_nullable_key is disabled"). The DDL is only rejected at CREATE TABLE, after the user has already walked the whole wizard and after schema inference has scanned the file. `sipHash64(Nullable(x))` returns a Nullable expression, which Distributed also rejects as a sharding key. Cost is not throughput per se — it is a guaranteed restart of a multi-minute workflow, and the user's natural workaround (pick a different column) is the wrong ORDER BY.
- FIX: Filter both selectboxes to columns whose final_type does not start with `Nullable(`. If none qualify, surface an explicit choice: either flip the intended key column to non-Nullable with a DEFAULT (see the Nullable-vs-DEFAULT finding), or emit `ORDER BY assumeNotNull(col)`, or add `allow_nullable_key = 1` to the SETTINGS clause with a visible warning that a nullable sorting key degrades index pruning. Validate the same invariant inside build_create_local_table_sql / build_create_distributed_table_sql so it cannot be bypassed programmatically.
- GAIN: Eliminates a deterministic CREATE TABLE failure. No direct throughput change. | EFFORT: S
- RISK: Low. tests/test_clickhouse.py:85 asserts `"ORDER BY \`id\`" in sql` with a non-nullable id column and is unaffected. A new ValueError path in build_create_local_table_sql needs a new test; existing DDL tests use non-nullable keys.

### F3. Cut per-value cost of _ColumnStats.add_value (full-scan inference is ~17 min on 1 GB)
- WHERE: src/csv_click/schema.py:126-154, src/csv_click/schema.py:148, src/csv_click/schema.py:388-402, src/csv_click/pandas_loader.py:232-234, src/csv_click/app.py:635-640
- MECHANISM: app.py:635-640 exposes a user-selectable SCHEMA_INFERENCE_FULL_SCAN mode that routes to analyze_csv_with_pandas_chunks, which calls add_value on EVERY value. A 1 GB numeric-heavy CSV of ~10M rows x 40 columns is 4e8 values x 2.5 us = ~1000 s = ~17 minutes of single-threaded GIL-held Python before one byte is inserted. If the user selected full scan, this is a large, previously-unattributed slice of the measured 45 minutes. In sample mode (100k rows) it is ~10 s and irrelevant.
- FIX: (1) Guard schema.py:148 with `if self.all_decimal:` and set `decimal_scale = None` otherwise. (2) Replace `_is_int`/`_is_uint` with one module-level `_INT_RE = re.compile(r"([+-]?)(\d+)")` matched once per value; derive int-ness and uint-ness from the sign group. (3) Add an early return at the top of add_value once every `all_*` flag is False AND `has_empty` is True AND `len(sample_values) == 5` — the column is already pinned to Nullable(String) and no further probe can change the answer. (4) In analyze_csv_with_pandas_chunks, stop probing a column entirely once it is pinned, instead of iterating `chunk[source_name].tolist()` for it (pandas_loader.py:233-234).
- GAIN: Measured components give 1.5-3x on the inference pass: -0.36 us/value on text columns (-40%), -0.15 to -0.25 us/value from the merged precompiled regex on integer columns, and the early exit skips ~100% of the cost on already-pinned String columns. A | EFFORT: S
- RISK: _infer_type output must stay byte-identical. tests/test_schema.py:48-54 pins the full inferred type list, tests/test_schema.py:113 pins UInt64, tests/test_pandas_loader.py:154-156 pins inferred_type/final_type/sample_values. The early exit must not short-circuit sample_values collection (schema.py:129-131) or tests/test_pandas_loader.py:156 breaks.

### F4. Track min/max in _ColumnStats and emit minimal-width integer types
- WHERE: src/csv_click/schema.py:111-124, src/csv_click/schema.py:146-147, src/csv_click/schema.py:331-334
- MECHANISM: Violates .codex/.agents/skills/clickhouse-architect/references/schema-design.md:21 "Use the smallest semantically correct numeric and temporal types." A boolean-ish 0/1 flag, a status code, or a small counter is stored as 8 bytes instead of 1, so the server materialises 8x the in-memory block bytes per column during insert, writes 8x the pre-compression column bytes, and merges 8x the data. LZ4 recovers much of this on disk but not the in-memory block or the merge I/O. Note honestly: the WIRE payload is JSONEachRow text, so narrowing types saves ZERO bytes on the network and cannot move the 380 KB/s client-side number. The gain is entirely server-side and on the resulting table.
- FIX: Add `min_int: int | None = None` and `max_int: int | None = None` to _ColumnStats, updated inside the existing `if self.all_int:` short-circuit path (parse the digits already captured by the merged regex — no extra int() when the value is not integral). In _infer_type, when all_uint: <=255 -> UInt8, <=65535 -> UInt16, <=4294967295 -> UInt32, <=2^64-1 -> UInt64, above that -> UInt128/UInt256 or String. When all_int: [-128,127] -> Int8, [-32768,32767] -> Int16, [-2^31,2^31-1] -> Int32, else Int64. CRITICAL SAFETY RULE: the schema comes from a SAMPLE (DEFAULT_SCHEMA_SAMPLE_ROWS = 100000, pandas_loader.py:38), so narrowing to UInt8 on sampled evidence risks an out-of-range value at row 8,000,000
- GAIN: Estimated 20-50% smaller uncompressed column footprint and correspondingly less merge I/O on integer-heavy tables; 5-15% of server-side insert CPU. No client-side wire gain. Sampling cost +10.3% (measured). | EFFORT: M
- RISK: BLOCKED until _convert_series learns narrow integer types (see the separate finding) — today Nullable(Int32) serialises as the string '1.0'. tests/test_schema.py:48-49 asserts "UInt64" for a small-id column and tests/test_schema.py:113 and tests/test_pandas_loader.py:154-155 also pin UInt64; all three must be updated to the new narrow type or the t

### F5. _convert_series has no branch for narrow integer types — it silently emits strings (and '1.0')
- WHERE: src/csv_click/pandas_loader.py:683-722, src/csv_click/pandas_loader.py:687, src/csv_click/pandas_loader.py:719-722
- MECHANISM: This is the blocker for every bit-width optimisation and it fails at load time, not at validation time. `"1.0"` cannot be parsed into Int32 by JSONEachRow — ClickHouse raises "Cannot parse Int32 from String". Even the well-formed case ('1' instead of 1) depends on `input_format_json_read_numbers_as_strings` being enabled server-side, and the quotes add 2 bytes per value to an already-uncompressed payload. Users who type a narrow type into the `custom_type` column of the type editor (app.py:728-731) get a load that dies partway through.
- FIX: Generalise line 687 from a two-element set to a regex over `^U?Int(8|16|32|64|128|256)$` and route all of them through the existing integer branch (`pd.to_numeric` -> list comp of Python ints). Add `Float32` to the Float64 branch at line 696. Add a range assertion inside that branch so an out-of-range value is reported as a CsvSchemaError naming the row and target type rather than being silently truncated by ClickHouse. Do the same for Date32 / DateTime64(p) (route to the existing Date/DateTime branches) so those dropdown entries also work.
- GAIN: No direct speedup on its own; it is the precondition that makes minimal-width types, Date32 and DateTime64 usable at all. Removes a class of mid-load aborts. | EFFORT: S
- RISK: Low. tests/test_pandas_loader.py:203,258,274,290,305,333,366,401,419,451,478 all use Int64/UInt64/String and are unaffected. New tests needed for each added width.

### F6. Nullable() wrapper is generated OUTSIDE LowCardinality(), producing an invalid ClickHouse type
- WHERE: src/csv_click/pandas_loader.py:755-760 (_normalize_nullable_type), src/csv_click/schema.py:279-282 (unwrap_nullable), src/csv_click/schema.py:290-294, src/csv_click/pandas_loader.py:719-722
- MECHANISM: Both directions are broken, so LowCardinality is unusable for any column that has empty values — which is precisely the categorical-string case where LowCardinality pays off most. tests/test_pandas_loader.py:505 and :515-516 and :526 already exercise LowCardinality(String) via custom_type, so the feature is nominally supported and quietly broken for nullable data. The user hits it at CREATE TABLE (invalid type) or at convert time (spurious error).
- FIX: Teach `_normalize_nullable_type` and `schema_from_editor_rows` that the Nullable wrapper goes inside LowCardinality: if the base type matches `^LowCardinality\((.+)\)$`, wrap the inner type instead, yielding `LowCardinality(Nullable(String))`. Teach `unwrap_nullable` to peel LowCardinality first and report nullability of the inner type — return a 3-tuple (low_cardinality, nullable, base) or add a sibling `unwrap_type()` helper. Then _convert_series line 720 sees nullable=True and the String branch at line 685-686 handles it correctly.
- GAIN: No throughput change; unblocks LowCardinality on the columns that need it. Prevents a CREATE TABLE failure and a spurious conversion error. | EFFORT: S
- RISK: Low. tests/test_pandas_loader.py:515-516 (non-nullable LowCardinality) keeps passing. schema.py:290-294 and pandas_loader.py:755-760 duplicate the same logic — fix both or the behaviour diverges between the two editor paths.

### F7. Leading-zero numeric strings are silently corrupted into integers
- WHERE: src/csv_click/schema.py:392-393 (_is_uint), src/csv_click/schema.py:388-389 (_is_int), src/csv_click/schema.py:331-334
- MECHANISM: Not a speed issue — a silent correctness issue that is severe in this deployment's domain (clickhouse.py:19 DEFAULT_HOST = "tp17.wb-bank.ru"; banking data is full of zero-padded account numbers, BIK/INN/KPP codes, phone numbers, branch codes, postal codes). The data lands in ClickHouse looking plausible and is wrong, which is worse than an abort. It also throws away the best LowCardinality/String candidates by mistyping them as UInt64.
- FIX: In `_ColumnStats.add_value`, set a new `has_leading_zero: bool` flag when `len(raw) > 1 and raw.lstrip('+-').startswith('0') and raw.lstrip('+-') != '0'`. In `_infer_type`, when `has_leading_zero` is set, return `("String", "Leading zeros detected; kept as String to preserve the literal value")` before the all_uint/all_int checks. Surface the note in the type-editor `notes` column so the user can override to UInt64 deliberately if they really want it.
- GAIN: No speed change. Prevents silent data corruption on a class of columns that is very likely present in this workload. | EFFORT: S
- RISK: Very low, but it changes inference for any fixture with zero-padded values. Check tests/test_csv.csv and the inline CSV literals in tests/test_schema.py:48-54 / tests/test_pandas_loader.py:154 for zero-padded values before landing.

### F8. No integer magnitude check — 20+ digit values infer UInt64 and abort the load on overflow
- WHERE: src/csv_click/schema.py:388-393, src/csv_click/schema.py:331-334, src/csv_click/pandas_loader.py:687-695
- MECHANISM: ClickHouse rejects the block with a range error, and because the schema was inferred from a sample the offending row may be anywhere in the file — the load can die 40 minutes in, making effective throughput zero. `_is_uint`'s `\+?\d+` accepts unbounded digit strings and nothing downstream bounds them.
- FIX: Fold into the min/max tracking finding: once _ColumnStats has min_int/max_int, _infer_type can pick UInt128/Int128/UInt256/Int256 when the magnitude exceeds 64 bits, or fall back to `("String", "Integer exceeds 64-bit range; kept as String")`. Add the corresponding widths to CLICKHOUSE_TYPE_OPTIONS and to the _convert_series integer branch. As a defence-in-depth measure, have the integer branch of _convert_series range-check against the declared width and raise a CsvSchemaError naming the row and value, so the failure happens in preflight instead of mid-insert.
- GAIN: Turns an unbounded mid-load abort into a preflight error or a correct type. No throughput gain on well-formed data. | EFFORT: S
- RISK: Depends on the min/max and _convert_series findings landing first. Low test impact.

### F9. No Decimal precision tracking — Decimal(18,2) is chosen for values needing 22+ significant digits
- WHERE: src/csv_click/schema.py:123 (max_decimal_scale), src/csv_click/schema.py:148-154, src/csv_click/schema.py:335-340, src/csv_click/schema.py:396-402 (_decimal_scale)
- MECHANISM: ClickHouse rejects the value as out of range for Decimal(18,2) — Decimal(18,2) holds at most 16 integer digits. Same failure profile as the integer-overflow finding: aborts a long-running load, potentially near the end.
- FIX: Have `_decimal_scale` return `(precision, scale)` from `parsed.as_tuple()` (precision = len(digits)) and store `max_decimal_precision` alongside `max_decimal_scale`. In _infer_type choose the smallest Decimal that fits both: Decimal32(s) / Decimal(9,s) for precision<=9, Decimal(18,s) for <=18, Decimal(38,s) for <=38, Decimal(76,s) for <=76, else Float64 with a note. Keep the existing scale-based selection as the tiebreak. Add Decimal(9,2) and Decimal(38,18) to CLICKHOUSE_TYPE_OPTIONS.
- GAIN: Removes a mid-load abort. Secondary table-quality win: Decimal(9,s) is 4 bytes vs 8 for Decimal(18,s) and 16 for Decimal(38,s), so correct sizing can halve or quarter that column's footprint. | EFFORT: S
- RISK: tests/test_schema.py:50 asserts `"Decimal(18, 2)"` for a small-value column — with precision tracking that column would become Decimal(9, 2) and the assertion must change. tests/test_app_state.py:415 asserts the string "Decimal(18, 2)" appears in help text (unaffected).

### F10. Add distinct-value tracking to enable LowCardinality(String) proposals
- WHERE: src/csv_click/schema.py:124 (sample_values), src/csv_click/schema.py:128-131, src/csv_click/schema.py:326-347 (_infer_type), src/csv_click/schema.py:14-33
- MECHANISM: .codex/.agents/skills/clickhouse-architect/references/schema-design.md:22 says "Consider LowCardinality(String) for repeatedly occurring categorical strings after measuring suitability" — the app currently has no measurement to base that on. Missing LowCardinality on a categorical string column costs 3-10x on disk and on every scan of that column. At INSERT time LowCardinality is roughly neutral-to-slightly-negative on the server: it hashes each value into a per-part dictionary (one hash lookup per value) but then writes a UInt8/UInt16 index instead of the full string, so less data is written and merged; on a genuinely low-cardinality column the write savings usually dominate the dictionary 
- FIX: Add `distinct_values: set[str] | None` and `distinct_overflow: bool` to _ColumnStats. In add_value, `if not self.distinct_overflow: self.distinct_values.add(raw); if len(self.distinct_values) > DISTINCT_CAP: self.distinct_overflow = True; self.distinct_values.clear()`. Use DISTINCT_CAP = 10000 (ClickHouse's own practical upper bound for LowCardinality). Memory: 10000 strings x ~60 bytes = ~600 KB per column, ~30 MB for a 50-column file — acceptable; the overflow path frees it. Prefer the exact capped set over HyperLogLog: it is simpler, exact below the cap, and the cap bounds memory. Then in _infer_type, when the result is String and `not distinct_overflow` and `len(distinct_values) <= 1000`
- GAIN: Estimated 3-10x compression on genuinely categorical string columns and correspondingly faster scans; roughly neutral on insert throughput (small write savings offset by dictionary hashing). Sampling cost +10.3% (measured). | EFFORT: M
- RISK: Requires the LowCardinality/Nullable nesting fix to land first, otherwise any proposed LowCardinality column with empty values produces an invalid `Nullable(LowCardinality(String))`. tests/test_schema.py:53 and tests/test_pandas_loader.py:166 pin `Nullable(String)` for columns that could become LowCardinality — check their distinct counts and sampl

### F11. Nullable-by-default should become DEFAULT-by-default for low-empty-ratio columns
- WHERE: src/csv_click/schema.py:191, src/csv_click/schema.py:198, src/csv_click/schema.py:350-351, src/csv_click/pandas_loader.py:263, src/csv_click/schema.py:113 (has_empty)
- MECHANISM: A Nullable(T) column in MergeTree is stored as two streams: the T data plus a `.null` UInt8 bitmap — one extra byte per row per column before compression, one extra file per column per part, and extra merge I/O. On a 40-column table where 30 columns are Nullable only because of a handful of blanks, that is +30 bytes per row of raw column data and +30 streams per part. Nullable also disables null-free fast paths in comparisons and aggregations, and (per the ORDER BY finding) makes the column ineligible as a sorting key. Nullable is right when "missing" is semantically distinct from zero/empty; it is wrong when the CSV just has trailing blanks. DEFAULT ('' for String, 0 for numerics) keeps the
- FIX: Add `empty_count: int` to _ColumnStats (increment where has_empty is currently set, schema.py:132-134). Compute the empty ratio in _infer_type/_schema_from_stats and put it in `notes` ("3 of 100000 sampled rows empty"). Change the type editor's binary `nullable` checkbox (app.py:732) into a tri-state per column: `Nullable` / `DEFAULT` / `reject empties`. Default it to DEFAULT when the empty ratio is below 1% and to Nullable at or above 1%. Emit `\`col\` String DEFAULT ''` / `\`col\` Int32 DEFAULT 0` in build_create_local_table_sql for the DEFAULT case, and make _convert_series map empty to '' / 0 rather than None for those columns.
- GAIN: Estimated 5-15% smaller parts and correspondingly less merge CPU on a wide table where most Nullables were spurious; makes more columns eligible as ORDER BY keys. No client-side wire throughput change. | EFFORT: L
- RISK: Behaviour-visible. tests/test_schema.py:53 asserts "Nullable(String)" and tests/test_pandas_loader.py:166 asserts `name_column.final_type == "Nullable(String)"`; both would flip to `String DEFAULT ''` under a 1% threshold with tiny fixtures (1 empty in 2 rows = 50%, so they may actually stay Nullable — verify per fixture). build_create_local_table_

### F12. Support multi-column ORDER BY (and an explicit PRIMARY KEY) — today it is structurally impossible
- WHERE: src/csv_click/clickhouse.py:223, src/csv_click/clickhouse.py:67-71 (quote_column_identifier), src/csv_click/app.py:271
- MECHANISM: Per .codex/.agents/skills/clickhouse-architect/references/schema-design.md:13-15, ORDER BY must serve the frequent selective access paths and "Put columns used in common filtering/range scans early when their ordering helps prune data". Low-to-high cardinality ordering matters for two independent reasons: (a) the sparse primary index only prunes granules on a prefix of the sorting key, so a near-unique leading column makes the index useless for every other predicate; (b) sorting by the low-cardinality column first creates long runs of equal values in the following columns, which is what makes LZ4/ZSTD and the delta-family codecs actually compress. Choosing a unique id as the single ORDER BY 
- FIX: Change clickhouse.py:223 to accept a list and emit `ORDER BY (\`a\`, \`b\`, \`c\`)`, quoting each element with quote_column_identifier and keeping the single-column form `ORDER BY \`a\`` for a one-element list so tests/test_clickhouse.py:85 still matches. Change app.py:271 to `st.multiselect` preserving order. Add an optional PRIMARY KEY input validated to be a prefix of ORDER BY (schema-design.md:15). Once distinct tracking exists, warn in the UI when the FIRST ORDER BY column has a distinct ratio above ~50% in the sample ("the primary index will not prune on this key") and suggest reordering low-to-high cardinality.
- GAIN: Large on the resulting table's query performance (order-of-magnitude granule pruning improvements are typical when the key matches the predicate). Estimated 5-20% SLOWER on server-side insert due to the wider sort — irrelevant while the client is the | EFFORT: M
- RISK: tests/test_clickhouse.py:85 asserts `"ORDER BY \`id\`"` — preserved by the one-element special case. tests/test_app_state.py:138 asserts `'st.selectbox("ORDER BY"' in form_source` and WILL FAIL when the widget becomes st.multiselect; that assertion must be updated to `st.multiselect("ORDER BY"`. tests/test_app_state.py:418 asserts the help text con

### F13. Emit per-type CODEC clauses (Delta/DoubleDelta/T64/Gorilla + ZSTD) — free here because the server is idle
- WHERE: src/csv_click/clickhouse.py:224-226 (columns_sql), src/csv_click/clickhouse.py:236 (SETTINGS)
- MECHANISM: HONEST ANSWER ON INSERT SPEED: codecs COST server CPU at insert. ZSTD(1) is roughly 2-4x slower to compress than LZ4; ZSTD(3) is 4-8x slower. T64 adds a bit-transpose pass. Delta/DoubleDelta/Gorilla are nearly free (a subtraction per value) and usually make the following general codec faster because there is less entropy left. So codecs would normally trade insert throughput for storage. In THIS deployment that trade is free: the measured ingest rate is ~380 KB/s, roughly three orders of magnitude below what a single ClickHouse node compresses per core, so the server's compression path is idle and the extra CPU is unobservable. Recommend codecs for table quality, and say plainly that they wi
- FIX: Add an optional per-column `codec` field to CsvColumn and render `\`col\` <Type> CODEC(<codec>)`. Defaults keyed off the inferred type: monotonic/near-sorted integers and the ORDER BY key -> `CODEC(Delta, ZSTD(1))`; Date/Date32 -> `CODEC(Delta, ZSTD(1))`; DateTime/DateTime64 that increase along the sort order -> `CODEC(DoubleDelta, ZSTD(1))`; non-monotonic bounded integers (ids, codes, enums-as-int) -> `CODEC(T64, ZSTD(1))`; Float32/Float64 sensor-like measurements -> `CODEC(Gorilla, ZSTD(1))` (note: Gorilla helps slowly-varying floats and HURTS on random floats — gate it behind an opt-in); Decimal -> `CODEC(T64, ZSTD(1))`; free-text String -> `CODEC(ZSTD(3))`; LowCardinality(String) -> leav
- GAIN: Estimated 1.5-4x smaller on-disk table versus plain LZ4, concentrated in timestamps and monotonic integers where Delta/DoubleDelta+ZSTD routinely reach 5-10x. Insert throughput: 0 to -10% server-side, invisible at the current client-bound 380 KB/s. V | EFFORT: M
- RISK: A wrong codec silently costs compression (Gorilla on random floats can EXPAND data). Delta on a String column is a DDL error. Requires a CsvColumn field addition, which touches schema_to_editor_rows / schema_from_editor_rows / mappings_to_editor_rows / mappings_from_editor_rows and their tests. tests/test_clickhouse.py DDL assertions are substring-

### F14. Extend CLICKHOUSE_TYPE_OPTIONS with the types the inference and the UI both need
- WHERE: src/csv_click/schema.py:14-33, src/csv_click/app.py:723-727
- MECHANISM: No throughput impact by itself — it is the UI surface that makes every other type recommendation reachable. Today a user who knows the right type must type it as free text, which bypasses the dropdown's implicit validation and, for narrow ints, produces the '1.0' serialisation bug.
- FIX: Add exactly these, keeping the existing 18: integers Int8, Int16, Int32, UInt8, UInt16, UInt32, Int128, UInt128, Int256, UInt256; float Float32; decimals Decimal(9, 2), Decimal(38, 18); temporal Date32, DateTime('UTC'), DateTime64(3), DateTime64(3, 'Europe/Moscow'), DateTime64(6); strings LowCardinality(String), UUID, IPv4, IPv6. Add the Nullable() form of each EXCEPT the LowCardinality one, which must be spelled LowCardinality(Nullable(String)) — add that literal entry rather than relying on the checkbox, and see the nesting-fix finding. Group the list (or prefix with a category) because 60+ flat entries in a Streamlit selectbox column is unusable; alternatively keep the dropdown to the ~20
- GAIN: None directly. Precondition for the bit-width, Date32/DateTime64, Decimal-precision and LowCardinality findings. | EFFORT: S
- RISK: tests/test_schema.py:116-118 only asserts that "String" and "Nullable(Decimal(38, 10))" are present, so additions are safe. Every added entry MUST have a working _convert_series branch first, or the dropdown offers types that break the load.

### F15. Date/DateTime inference is too permissive and never reaches Date32 / DateTime64
- WHERE: src/csv_click/schema.py:413-426 (_is_date, _is_datetime), src/csv_click/schema.py:341-344, src/csv_click/pandas_loader.py:707-716
- MECHANISM: Three distinct failures. (1) Range: a 1950 birth date or a 1900 sentinel overflows Date/DateTime — ClickHouse either errors or clamps to the epoch, silently. (2) Precision: fractional seconds are inferred as DateTime and truncated; the pandas_loader DateTime branch (:712-716) calls `value.to_pydatetime()` and _clean_json_value emits `.isoformat()` WITH microseconds, so the wire value carries precision the column cannot hold. (3) Timezone: a per-row offset like +03:00 is accepted by the probe and then flattened into a timezone-naive DateTime, silently shifting values. (4) A compact numeric date like '20240115' is ALSO matched by `_is_uint`, and since all_uint is checked first (schema.py:331) 
- FIX: Track `min_date`/`max_date`, `has_subsecond`, and `has_tz_offset` in _ColumnStats. In _infer_type: below 1970 or above 2149 -> Date32; below 1970 or above 2106 for datetimes -> DateTime64(3, 'UTC') (which covers 1900..2299); has_subsecond -> DateTime64(3) or DateTime64(6) depending on observed digits; has_tz_offset -> DateTime64(3, 'UTC') with a note that the offset is normalised to UTC. Tighten the probes: reject the compact `YYYYMMDD` form unless the whole column is 8-digit and in a plausible date range (and then prefer Date over UInt64 with a note). Route Date32/DateTime64 through the existing Date/DateTime branches of _convert_series (they currently fall through to the string tail).
- GAIN: No throughput change. Prevents silent value corruption (epoch clamping, timezone shifts, truncated subseconds) and one class of mid-load range errors. Date32 is 4 bytes vs Date's 2, DateTime64 is 8 vs DateTime's 4 — so this makes those columns bigger | EFFORT: M
- RISK: Changes inference for any date-bearing fixture. tests/test_schema.py:51 asserts "Date" for an in-range column and stays correct. Needs new _convert_series branches (see that finding) before the new types are selectable.

### F16. Inserting into the Distributed table doubles the server-side write path
- WHERE: src/csv_click/app.py:977 (table=distributed_table), src/csv_click/clickhouse.py:239-262 (build_create_distributed_table_sql), src/csv_click/clickhouse.py:253
- MECHANISM: With the default `distributed_foreground_insert = 0` (formerly insert_distributed_sync), the initiator node accepts the whole block, splits it by `sipHash64(key) % shards`, SERIALISES each shard's slice to temporary files on the initiator's local disk, and only then forwards them over the internal protocol. Every row is therefore written to disk twice and traverses the network twice, and the initiator node becomes a single-node funnel for the entire load regardless of how many workers the client uses. It also breaks the client's error reporting: a forwarded batch that fails on a remote shard surfaces asynchronously, long after raw_insert_batch returned success, so pandas_loader._raw_insert_e
- FIX: Two options, in increasing order of effort. (a) Keep the Distributed target but set `distributed_foreground_insert = 1` in the insert settings so failures are reported synchronously and the temp-file spool is skipped — this makes each insert slower but the pipeline honest and removes the disk double-write. (b) Resolve the shard host list once (`SELECT host_name, shard_num FROM system.clusters WHERE cluster = '<cluster>'`), have each worker open a client against a distinct shard host and insert into `<db>.<table>_local` directly, computing `sipHash64(key) % shard_count` client-side to route rows. Option (b) removes the funnel entirely and is the only way to make the per-worker parallelism in 
- GAIN: Estimated 1.5-3x on server-side ingest for (b) on a multi-shard cluster; (a) is roughly neutral on throughput but fixes silent partial-failure. NOTE: neither helps while the client is the bottleneck at 380 KB/s — this only matters after the client-si | EFFORT: L
- RISK: Option (b) is a real behavioural change to sharding and can silently misroute rows if the sipHash64 reimplementation diverges from the server's. Verify with `SELECT shard_num, count() FROM clusterAllReplicas(...)` against a Distributed-inserted control load. No existing test covers the target-table choice; tests/test_clickhouse.py:239-262 area test

### F17. index_granularity is hardcoded and no other MergeTree settings are reachable
- WHERE: src/csv_click/clickhouse.py:236, src/csv_click/clickhouse.py:209-216 (build_create_local_table_sql signature)
- MECHANISM: HONEST ANSWER: 8192 is the correct ClickHouse default and changing it will NOT make inserts faster — do not present it as a throughput lever. It matters only for the resulting table: lowering it (2048-4096) reduces the bytes read per point lookup on very wide rows at the cost of a larger primary index in memory; raising it saves index memory on very narrow tables. `index_granularity_bytes` (default 10 MB, adaptive granularity) is the setting that actually protects wide-row tables and is already on by default, so leaving both alone is defensible. The real gap is the missing TTL and table comment, which schema-design.md:36-38 lists in the DDL review checklist ("TTL, reload, correction, and ded
- FIX: Widen build_create_local_table_sql to take an optional `settings: dict[str, str] | None` and an optional `ttl: str | None` and `comment: str | None`, defaulting to `{'index_granularity': '8192'}` so the current output is unchanged. Expose in the UI: an optional TTL builder (`<date_col> + INTERVAL N MONTH DELETE`) and a free-text table comment recording the source CSV filename, row count and load timestamp. Leave index_granularity at 8192 and label it "advanced" rather than promoting it. Also surface the replay hazard from operations-and-performance.md:15 ("Replayed loads may duplicate rows unless the table strategy or load logic is explicitly idempotent") — the app currently refuses to load 
- GAIN: No throughput change. Closes three items on the vendored DDL review checklist and makes the load's failure/replay semantics explicit. | EFFORT: S
- RISK: tests/test_clickhouse.py:86 asserts `"SETTINGS index_granularity = 8192" in sql` — preserved by the default dict. New optional clauses need new tests; appending TTL/COMMENT before SETTINGS changes clause order, so verify the substring assertions at tests/test_clickhouse.py:83-87 still match.

### F18. get_client() passes no settings dict, so no insert-side server setting can ever be applied
- WHERE: src/csv_click/clickhouse.py:74-99, src/csv_click/clickhouse.py:79-88 (kwargs)
- MECHANISM: This closes off the cheapest server-side ingest lever available for a many-small-HTTP-inserts workload: `async_insert = 1` with `wait_for_async_insert = 0`, which makes the server buffer incoming blocks and flush them as larger parts (controlled by async_insert_max_data_size / async_insert_busy_timeout_ms), collapsing the parts pressure created by ~500 separate 16 MB HTTP inserts per GB. It also blocks `max_insert_threads`, `input_format_parallel_parsing` (on by default but worth pinning for JSONEachRow), `min_insert_block_size_rows/bytes`, and `input_format_json_read_numbers_as_strings` — the last of which is exactly what makes the current string-serialisation of narrow ints either work or 
- FIX: Add a `settings: dict[str, object] | None` field to ClickHouseConfig and pass it through to clickhouse_connect.get_client(settings=...). Default it to `{'async_insert': 1, 'wait_for_async_insert': 0, 'input_format_parallel_parsing': 1}` behind a UI checkbox labelled with the durability caveat: with wait_for_async_insert=0 the HTTP 200 means "buffered", not "committed", so the client's rows_count is optimistic and a server crash loses the buffer. Offer wait_for_async_insert=1 as the safe default and 0 as the fast opt-in. Also expose `max_insert_threads`. Do NOT enable deduplicate_blocks_in_dependent_materialized_views or non-default insert_deduplicate without a deliberate decision — see the r
- GAIN: Estimated 1.3-2.5x on server-side ingest for a small-block JSONEachRow workload with async_insert enabled (this is the documented use case for the feature). Invisible until the client-side bottleneck is fixed. Adjacent to the transport lens; expect o | EFFORT: S
- RISK: wait_for_async_insert=0 weakens the delivery guarantee and makes the progress counter report rows the server has not yet committed — must be surfaced in the UI, not enabled silently. No existing test covers get_client kwargs; add one asserting the settings dict is forwarded.

### F19. Measure first: the whole write-topology question is undecidable until you split per-insert time into uplink-bytes vs coordinator-latency
- WHERE: <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\clickhouse.py:351-363, <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\pandas_loader.py:512-537
- MECHANISM: 0.97 MB/s is consistent with two mutually exclusive causes with opposite fixes: (a) the workstation uplink/TLS proxy is ~8 Mbit/s, in which case the bytes are the bottleneck and NO change to cluster write topology helps, because every shard stream leaves through the same NIC; or (b) there is a ~3.5 s fixed per-INSERT cost at the coordinator (JSONEachRow parse + sipHash64 split + Distributed spool fsync + proxy round trip), in which case fewer/bigger/parallel inserts and shard fanout help ~linearly. Today `worker_count` defaults to 1 (settings.py:32), so the 672 POSTs are strictly serial and the two causes are indistinguishable from the outside.
- FIX: Before any topology work, capture the return value of `client.raw_insert(...)` in `raw_insert_batch` and log `summary.written_rows`, `summary.query_id` and server `elapsed_ns` next to the client-measured wall time for each block. Then run three one-off comparison loads of the same 100 MB slice: (1) into `<db>.<table>` (Distributed, today), (2) into `<db>.<table>_local` on the coordinator, (3) into a throwaway `ENGINE = Null` table with the same columns. Also repeat (1) with a 4x larger block. If wall time per insert stays flat as the block grows => latency-bound => pursue bigger blocks + concurrency + shard fanout. If it scales with bytes => bandwidth-bound => pursue compression and stop; di
- GAIN: No direct gain; this measurement decides whether the direct-to-shard work (L effort) is worth 0% or up to shard-count-x. Estimated 30-60 min of work to obtain. | EFFORT: S
- RISK: Adding a return value/logging to `raw_insert_batch` is additive. Note that `tests/test_clickhouse.py:312 test_raw_insert_batch_uses_json_each_row` asserts exact dict equality on the kwargs passed to `client.raw_insert`, so any signature change to `raw_insert_batch` (settings=, compression=) breaks that one test and it must be updated.

### F20. Do not touch write topology before turning on insert compression: 2.30 GB of uncompressed JSON currently crosses the wire and lz4/zstd are free
- WHERE: <HOME>\Downloads\codex\projects\csv_to_click\.venv\Lib\site-packages\clickhouse_connect\driver\httpclient.py:402-433, <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\clickhouse.py:351-363
- MECHANISM: 1 GB of source CSV becomes 2.30 GB of JSONEachRow bytes on the wire. At the measured 0.97 MB/s effective uplink that is 2436 s = 40.6 min of the observed 45 min. Every proposed topology change (direct-to-shard, more workers) multiplies the number of concurrent streams but not the number of bytes, and all streams share one client uplink. If the link is the constraint, shard fanout gives exactly 0.
- FIX: Add `compression` (and `settings`) parameters to `raw_insert_batch` and pass `compression="zstd"` (fallback "lz4") after compressing the payload with the already-installed `zstandard`/`lz4` packages. ClickHouse accepts `Content-Encoding: zstd|lz4|gzip` on the HTTP insert path. Cost measured at 7 ms per 3.5 MB block against a 0.39 s/block producer, i.e. ~2% CPU overhead.
- GAIN: If bandwidth-bound (the arithmetic above suggests it is): 3.7x with lz4, 6.5x with zstd on the network term -> 2.30 GB becomes 361 MB, projected load time drops from ~45 min to ~10-12 min. If latency-bound: ~0%. Honest range 1.0x-6.5x pending the mea | EFFORT: S
- RISK: An intermediate proxy (the environment demonstrably has one -- example_pandas_to_click.md section 12 documents Native inserts failing with HTTP 500 through it, and clickhouse_connect's own source at client.py:225-233 comments about CHProxy stripping query params) may not forward `Content-Encoding` correctly. Must be tried against the real endpoint;

### F21. What a Distributed INSERT actually costs: per-row sharding-key evaluation, block split, and a spooled double write on one coordinator's disk
- WHERE: <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\app.py:977, <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\clickhouse.py:239-262
- MECHANISM: ClickHouse behavior (documented engine behavior, not derived from this repo): the node that terminates the HTTP request is the initiator. It parses the JSONEachRow stream into Blocks, evaluates `sipHash64(col) % sum(shard weights)` for EVERY row, builds a block selector and permutes every column into per-shard sub-blocks. With `distributed_foreground_insert=0` (the default; named `insert_distributed_sync` before 24.2) each sub-block is then serialized to Native, compressed with `network_compression_method` (LZ4) and written to a .bin file under the Distributed table's directory on the INITIATOR's local disk; the HTTP 200 is returned at that point. A per-shard background queue later reads tho
- FIX: Two cheap, non-topological levers, both reachable because `HttpClient.raw_insert` accepts `settings: dict` (httpclient.py:407) and `_validate_settings` (client.py:244) drops or errors on unknown names so it is version-safe if you probe `client.server_settings` first: (a) pass `{"distributed_foreground_insert": 1}` (probe for the legacy name `insert_distributed_sync` via `client.server_settings`) to eliminate the spool entirely, make errors synchronous and make rows immediately visible; (b) leave it async but add a post-load `SYSTEM FLUSH DISTRIBUTED <db>.<table>` and a `SELECT count()` verification. Expose neither as UI; pick one and log which mode was used.
- GAIN: Foreground mode removes the initiator's double disk write (est. ~1.2 GB of I/O per 1 GB CSV) but adds the shard-side ReplicatedMergeTree commit (including the ZooKeeper transaction) to the client's critical path. Net client-visible effect is genuinel | EFFORT: S
- RISK: Foreground mode makes the insert latency equal to the slowest shard and will surface shard errors that are currently swallowed into the background queue -- which is a feature under AGENTS.codex.md 'fail loudly', but it will make previously 'successful' loads start failing. `distributed_foreground_insert_timeout` defaults to 0 (wait forever), bounde

### F22. With async Distributed insert the app reports 'Load finished' before the rows exist anywhere, and the failure-cleanup path can silently discard queued data
- WHERE: <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\app.py:985-988, <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\app.py:828-842, <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\clickhouse.py:146-161
- MECHANISM: Not a speed issue, a truthfulness issue that the topology discussion depends on. In default async mode the client's 'success' only means '.bin files were written on the initiator'. Rows appear in `SELECT` seconds to minutes later, and forwarding can fail permanently (schema mismatch, shard down) with the error visible only in `system.distribution_queue.last_exception` and the initiator's log -- never in this UI. Worse: if any later block fails, `_cleanup_after_failed_load` DROPs the Distributed table, which deletes its spool directory and therefore silently throws away rows the user was told were loaded.
- FIX: After the load: (1) `SYSTEM FLUSH DISTRIBUTED <db>.<table>` (or wait on `SELECT sum(data_files) FROM clusterAllReplicas(<cluster>, system.distribution_queue) WHERE database=... AND table=...` reaching 0); (2) `SELECT count() FROM <db>.<table>` and compare to the client-side row count, logging both; (3) fail loudly on mismatch instead of printing a green success. This check is what makes any later topology change verifiable at all.
- GAIN: No throughput gain. Turns an unverifiable load into a verified one, which is a precondition for benchmarking topology changes. | EFFORT: S
- RISK: `SYSTEM FLUSH DISTRIBUTED` needs the privilege; if denied, fall back to polling `system.distribution_queue`. Adds one to three round trips at the end of a 45-minute load. No existing test asserts the absence of a post-load count, so this is additive; `tests/test_app_state.py` assertions on `_create_and_load` are all substring `in` checks and stay g

### F23. Direct-to-shard-local writes: the actual proposal, its speedup model, and the reachability probe that must gate it
- WHERE: <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\app.py:972-983, <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\clickhouse.py:19-22, <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\pandas_loader.py:541-645
- MECHANISM: Today the effective server-side write concurrency is 1 (settings.py:32 `load_workers: int = 1`), and even at the UI maximum of 6 (app.py:290) all six connections go to the same hostname. If that hostname is a single ingress node, all 672 inserts are parsed, hashed, split and spooled by one node's CPU and disk. Writing straight to `<db>.<table>_local` on each shard host would (a) delete the sharding-key evaluation and block split entirely, (b) delete the spool and its double write, (c) turn one coordinator into S independent write paths.
- FIX: Implement as a distinct, opt-in code path, in this order. Step 0 (read-only probe, do this before writing any loader code): `SELECT cluster, shard_num, replica_num, host_name, host_address, port, is_local, errors_count FROM system.clusters WHERE cluster = '<cluster>' ORDER BY shard_num, replica_num`, then for each distinct `host_name` attempt `get_client(replace(config, host=host_name))` and `SELECT hostName()`. Also run `SELECT hostName()` on six clients to the existing VIP to learn whether tp17 is already a load balancer over K nodes (if it is, most of the funnel argument evaporates and you can get fanout for free just by raising `load_workers`). Step 1: pick exactly ONE replica per shard 
- GAIN: Model: T = max(producer_cpu, payload_bytes / client_uplink, num_inserts * per_insert_server_latency / S). The third term scales ~1/S; realistic efficiency 0.7-0.9 of ideal because the client uplink, the TLS stack and the GIL-bound producer are shared | EFFORT: L
- RISK: HIGH and mostly non-performance: (1) `system.clusters.port` is the NATIVE TCP port (9000/9440), not the HTTPS port this app uses, and `host_name` is an intra-cluster FQDN -- there is no evidence any of them resolves or accepts mTLS from a Windows workstation; the whole idea dies at step 0 if the probe fails. (2) The client certificate may be author

### F24. Do not reproduce sipHash64 client-side: no SipHash library is installed, pure-Python is too slow, and ClickHouse's per-type binary hashing is a silent-corruption minefield
- WHERE: <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\clickhouse.py:253, <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\schema.py
- MECHANISM: To place a row on the same shard the Distributed engine would choose, you must compute `sipHash64(value) % sum(shard weights)` over ClickHouse's own binary representation of the value -- raw bytes for String, fixed-width little-endian for integers, UInt16 days for Date, UInt32 for DateTime, the underlying integer for Decimal, plus separate handling for the Nullable wrapper -- and then map the slot to a shard using the per-shard `weight` from `system.clusters` in the same slot order. A pure-Python SipHash-2-4 costs an estimated 3-10 us/row; at 13.44M rows that is 40-135 s added to a producer that already only manages 3.88 MB/s. And a subtle mistake is SILENT: rows land on a shard the engine w
- FIX: Do not attempt exact hashing. Use deterministic round-robin at block granularity and make the loss of sharding-key fidelity an explicit, surfaced property of the mode (see the next two findings). If exact placement is ever a hard requirement, the correct answer is not client-side hashing but keeping the Distributed INSERT with `distributed_foreground_insert=1`.
- GAIN: Avoids an estimated 40-135 s of added producer CPU and an unbounded correctness liability. Round-robin at block granularity gives near-uniform row counts per shard for a 672-block load (imbalance under 1%). | EFFORT: S
- RISK: Round-robin destroys value locality on the sharding column, which can slightly worsen compression and granule pruning on each shard. With `ORDER BY` restricted to a single column (clickhouse.py:223) and no codecs emitted anywhere (build_create_local_table_sql:228-236), the penalty is estimated at 0-15% on compressed size and is probably not measura

### F25. If the sharding key is not honoured, `optimize_skip_unused_shards` silently returns incomplete results -- create the Distributed table WITHOUT a sharding key in that mode
- WHERE: <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\clickhouse.py:239-262, <HOME>\Downloads\codex\projects\csv_to_click\tests\test_clickhouse.py:122-130
- MECHANISM: Correctness, not speed. A Distributed table that declares `sipHash64(col)` is a promise to ClickHouse that rows are placed by that function. `optimize_skip_unused_shards=1` (off by default, but settable per query, per user profile, or in the Distributed table's own SETTINGS) uses that promise to skip shards whose slot cannot match a query's predicate on the sharding column. If the loader placed rows round-robin, such a query returns fewer rows than exist, with no error. Colocated-JOIN behavior (`distributed_product_mode='local'`) and any `INSERT INTO other_dist SELECT ... FROM this_dist` that relies on co-residency break the same way.
- FIX: When the direct-to-shard mode is enabled, emit `ENGINE = Distributed('<cluster>','<db>','<local>')` with NO sharding key. This is legal DDL and it makes shard pruning impossible by construction -- ClickHouse cannot prune what it cannot compute -- so reads through the Distributed table are correct forever regardless of placement. The only thing you give up is `INSERT INTO <distributed>` (it then requires `insert_distributed_one_random_shard=1`), which this mode does not use. Implement by adding an explicit `sharding_key: str = ""` allowance behind a new parameter rather than removing the existing validation.
- GAIN: No throughput gain; converts a silent-wrong-answer class of bug into a structural impossibility. | EFFORT: M
- RISK: Anyone who later wants to `INSERT INTO <db>.<table>` (the Distributed table) by hand, as example_pandas_to_click.md section 10 and section 14 instruct, gets `Method write is not supported by storage Distributed with more than one shard and no sharding key` unless they set `insert_distributed_one_random_shard=1`. That must be documented in the UI ca

### F26. Round-robin plus urllib3's POST retry is a duplicate-row generator unless shard assignment is a pure function of (chunk, block)
- WHERE: <HOME>\Downloads\codex\projects\csv_to_click\.venv\Lib\site-packages\clickhouse_connect\driver\httpclient.py:183, <HOME>\Downloads\codex\projects\csv_to_click\.venv\Lib\site-packages\clickhouse_connect\driver\httpclient.py:574-616, <HOME>\Downloads\codex\projects\csv_to_click\sr
- MECHANISM: Correctness. ReplicatedMergeTree insert deduplication (`insert_deduplicate=1`, `replicated_deduplication_window=100` blocks, `replicated_deduplication_window_seconds=604800`) hashes the inserted block and registers it in ZooKeeper under THAT shard's replica path. A re-sent byte-identical block to the SAME shard is therefore silently deduplicated -- which is exactly what protects the current Distributed path. Send the retry to a DIFFERENT shard (which a mutable round-robin counter, or a naive `next(cycle)` consumed at submit time, will happily do) and dedup does not apply: the rows land twice and `SELECT count()` through the Distributed table is wrong. Note also that with 672 blocks and a 100
- FIX: Compute the shard from immutable block identity, e.g. `shard = shard_list[(chunk_number - 1) * MAX_BLOCKS_PER_CHUNK + (block_number - 1) % len(shard_list)]`, evaluated inside `insert_payload` from the arguments it already receives, never from shared mutable state. Never retry a failed block on a different shard. Never fall back from direct-to-shard to the Distributed table mid-load -- that mixes placement strategies inside one load and voids the invariant; fail loudly instead, per AGENTS.codex.md 'Fail loudly'.
- GAIN: No throughput gain; prevents silent duplicates on a 45-minute load whose failure mode is currently invisible. | EFFORT: S
- RISK: Requires a unit test with a fake client that records (shard, chunk, block) and asserts the mapping is stable across a simulated retry. If the user's profile sets `insert_deduplicate=0` or a custom `replicated_deduplication_window`, even same-shard retries duplicate -- worth probing `client.server_settings['insert_deduplicate']` at load start and lo

### F27. `_table_exists_on_cluster` returns True when ANY host has the table, which direct-to-shard turns from harmless into a load-destroying race
- WHERE: <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\clickhouse.py:164-201, <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\app.py:996-1008
- MECHANISM: Correctness/operational. Today the first inserts always go to the coordinator's Distributed table, whose async spool tolerates a shard whose ON CLUSTER DDL has not landed yet -- the background sender simply retries. With direct-to-shard, the very first POST to a shard that has not yet created `<table>_local` fails with UNKNOWN_TABLE; `_format_load_error` (app.py:817-825) recognises exactly that string, and `_create_and_load` then calls `_cleanup_after_failed_load` and DROPs both tables ON CLUSTER -- destroying whatever the other shards already ingested and forcing a full 45-minute retry.
- FIX: Before the first direct insert, verify per-host coverage rather than existence: compare the set of `host_name` values returned by `SELECT DISTINCT host_name FROM system.clusters WHERE cluster='<cluster>'` against the `hostName()` values returned by the `clusterAllReplicas(..., system.tables)` query the repo already runs in `_existing_target_tables` (clickhouse.py:131-143 already SELECTs `hostName() AS host_name`, it is simply discarded at line 143). Alternatively run `SYSTEM SYNC DATABASE REPLICA` / poll `system.distributed_ddl_queue` for the CREATE entries to reach `Finished` on every host.
- GAIN: No throughput gain; removes a race that would cost a full re-run of a 45-minute load. Also fixes a latent weakness of the current path. | EFFORT: M
- RISK: The stricter check can hang on a cluster where one replica is legitimately down -- needs a bounded wait and a clear error naming the missing hosts. `tests/test_clickhouse.py:166 test_create_tables_drops_partial_state_and_verifies_local_before_distributed` drives `_verify_table_exists_on_cluster` through a fake client and will need its fake result r

### F28. The insert block size is set by `batch_size`, not by the payload cap, so the loader issues ~672 small inserts where ~163 would do
- WHERE: <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\pandas_loader.py:512-537, <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\pandas_loader.py:443-478
- MECHANISM: Directly relevant to the topology lens: with the Distributed engine, EACH insert produces one .bin spool file per shard on the initiator and, after forwarding, one MergeTree part per shard per partition. 672 inserts against S shards means 672*S spool files and up to 672 parts per shard partition -- close to `parts_to_delay_insert` (150) and `parts_to_throw_insert` (300) territory if background merges fall behind, which shows up as a steadily-growing per-insert delay indistinguishable from network slowness. Fewer, larger inserts reduce both the fixed per-insert cost and the parts pressure, on the current topology and on any future direct-to-shard topology.
- FIX: Decouple the pandas read chunk size from the insert block size: keep `batch_size` as the pandas `chunksize` (memory control) and add a separate accumulator that concatenates JSON lines across chunks until the effective payload cap is reached before submitting. This is also why raising `batch_size` crashes today -- it is overloaded to control both pandas chunk memory (object-dtype columns + the full list of per-row `bytes` objects held simultaneously in `iter_json_each_row_payloads`) and the insert size.
- GAIN: 4.1x fewer HTTP inserts (672 -> 163). If per-insert latency dominates (finding 1, hypothesis b), that alone is close to 4x on the network/server term. If bytes dominate, ~0%. Also removes the reason the user cannot raise the batch size. | EFFORT: M
- RISK: A 14.4 MB request is more likely to hit whatever proxy limit produced the 'read limit is reached' error that `_raw_insert_error` (pandas_loader.py:670-673) already knows about -- so this change must be paired with compression (which brings 14.4 MB back down to ~2.2 MB with zstd) and must keep the existing safety-ratio and error hint. Memory: an acc

### F29. Make direct-to-shard opt-in behind a discovery probe, never the default, and never a silent fallback
- WHERE: <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\app.py:261-401, <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\settings.py:19-36
- MECHANISM: Not a performance finding -- the answer to 'should this be opt-in'. The mode changes durability, retry semantics, shard placement, the Distributed DDL, and the network reachability requirements. It cannot be a default because (a) whether shard hosts are reachable at all from a Windows workstation through the mTLS ingress is unknown and environment-specific, and (b) it changes the meaning of the resulting table for every downstream reader.
- FIX: Add to the left column, directly under 'Load workers': a `st.checkbox("Write directly to shard local tables (experimental)", value=settings.direct_shard_insert)` defaulting to False, plus a `st.button("Discover shards")` that runs the read-only `system.clusters` query and the per-host `SELECT hostName()` probe and prints the shard/replica/host table into the existing log area. Keep the checkbox disabled (or refuse at Apply time with a clear error) until a successful probe is recorded in session state. When enabled, show a `st.caption` stating in one line: rows are distributed round-robin, the sharding key is NOT honoured, the Distributed table is created without a sharding key, and manual `I
- GAIN: No direct gain; makes an L-effort, high-blast-radius change safe to ship and easy to A/B against the Distributed path on the same file, which is the only way to measure the topology claim honestly. | EFFORT: M
- RISK: New tests required: shard discovery parses a fake `system.clusters` result and picks one replica per shard; deterministic (chunk, block) -> shard mapping survives a simulated retry; a failure on one shard cancels the other pending futures (the existing `cancel_pending`/`collect_completed` machinery at pandas_loader.py:587-611 already does this and 

### F30. Give the load a real cancel token and stop letting a stray rerun silently abort it mid-file
- WHERE: src/csv_click/app.py:138-152, 953-983, 995-1011; src/csv_click/pandas_loader.py:481-491, 614-645; .venv/Lib/site-packages/streamlit/runtime/scriptrunner_utils/exceptions.py:21-31; .venv/Lib/site-packages/streamlit/runtime/scriptrunner/script_runner.py:442-451
- MECHANISM: Any widget click, browser reconnect or toolbar Stop during the 45-minute load raises RerunException from inside on_progress. Because it derives from BaseException it passes straight through `except Exception:` in `_load_csv_via_raw_insert_parallel` (pandas_loader.py:642) and through both handlers in `_create_and_load` (app.py:995, 1003). So `cancel_pending()` never runs, `ThreadPoolExecutor.__exit__` blocks until the worker_count*2 in-flight POSTs drain, and the script reruns leaving: tables created, N million rows loaded, no log, no error, no cleanup, no record of where it stopped. There is no deliberate way to cancel a load at all.
- FIX: Add `cancel_callback: Callable[[], bool] | None` to `load_csv_via_raw_insert` and `_load_csv_via_raw_insert_parallel`, checked between blocks (mirroring the existing csv_read_cancel_requested naming), backed by a `threading.Event` in `st.session_state['load_cancel_event']`. Run the load on a `threading.Thread` with `add_script_run_ctx(thread, get_script_run_ctx())` (verified importable in 1.58) and drive the UI from `@st.fragment(run_every='1s')` (verified: `run_every` is in `st.fragment`'s signature) polling a shared progress dict, with a Stop load button next to it. Wrap the load body in `try/except BaseException/finally` so a RerunException still records state before propagating.
- GAIN: Converts a 45-minute uninterruptible operation that can be silently aborted by a misclick into one that is watchable, cancellable, and leaves a recorded outcome. | EFFORT: L
- RISK: Breaks tests/test_app_state.py::test_create_and_load_renders_progress_and_accumulated_log (asserts literal 'st.progress(0)' and 'log_container = st.empty()') and ::test_create_and_load_passes_load_workers_to_loader (asserts literal 'worker_count=load_workers' and 'client_factory=lambda: get_client(config)'). Background threads writing st.session_st

### F31. The existing csv_read_cancel_requested Stop mechanism cannot interrupt a running scan either - it is dead at runtime
- WHERE: src/csv_click/app.py:488-490, 565, 580-586, 614-648; src/csv_click/pandas_loader.py:216-236; .venv/Lib/site-packages/streamlit/runtime/scriptrunner/script_runner.py:710
- MECHANISM: The flag can only become True on a run that starts after the scan already finished. The four `_csv_read_cancel_requested()` guards at 626/633/639/647 and the whole `cancel_callback` parameter plumbed into pandas_loader are unreachable-as-True in the real app. A Full-scan of a 1 GB CSV therefore locks the browser tab with a spinner and no exit, and the feature the repo believes it shipped does not exist.
- FIX: Same pattern as the load: run the scan on a thread with an Event, or at minimum emit one `st.empty().text(f'scanned {rows} rows')` per chunk from inside the loop so a yield point exists, then catch RerunException/StopException and convert to the already-defined `CsvReadCancelled`. Keep the `cancel_callback` signature - only its wiring changes.
- GAIN: Makes an already-written, already-tested feature functional; removes an unbounded UI lock on the Full-scan path. | EFFORT: M
- RISK: tests/test_pandas_loader.py:169 test_analyze_csv_with_pandas_chunks_can_be_cancelled_between_chunks calls the loader directly with a plain lambda, so it stays green. Emitting per-chunk st calls reintroduces the throttling concern from the log finding - throttle it.

### F32. Replace the row-count-gated progress bar with byte-offset progress plus rows/s, MB/s, elapsed and ETA
- WHERE: src/csv_click/app.py:859, 883, 899-927, 962-964, 985; src/csv_click/pandas_loader.py:61-84
- MECHANISM: For exactly the case the user cares about (1 GB, ~45 min) the progress bar never moves and there is no ETA. The user cannot tell a slow load from a hung one, which is why a 45-minute load feels unbounded rather than merely slow.
- FIX: Byte progress needs no pre-count: open the file yourself (`fh = open(csv_path, 'rb')`), hand the handle to `pd.read_csv(fh, encoding=..., chunksize=...)` inside `iter_pandas_chunks`, and report `min(1.0, fh.tell() / os.path.getsize(csv_path))` - pandas read-ahead makes it optimistic by at most one buffer (~0.1% on 1 GB). Render one `st.columns(5)` row: inserted rows, rows/s, CSV MB/s, elapsed, ETA = `elapsed * (1/frac - 1)`. Keep total_rows as a secondary denominator when the strict path already counted it. Estimate: byte granularity within 1 MB, ETA stable to +/-10% after the first ~5% of the file.
- GAIN: A moving bar and an ETA on the 1 GB path where today there is neither; ~15 lines. | EFFORT: M
- RISK: `iter_pandas_chunks` currently takes a path and is called by 6+ tests in tests/test_pandas_loader.py with tmp_path values - keep the path overload. tests/test_app_state.py:102 asserts the literal 'st.progress(0)'.

### F33. Throttle the load log - it re-joins and re-sends the entire accumulated log on every block (O(N^2))
- WHERE: src/csv_click/app.py:809-814, 862-871, 953-970
- MECHANISM: At block i the join builds i * ~95 chars, so total characters built and pushed = 95*N(N+1)/2. For 1 GB at 16 MB payloads N is roughly 250-500 blocks -> 3-12 MB of redundant websocket traffic and 750-1500 synchronous ForwardMsg enqueues on the script thread. Verified `global.minCachedMessageSize` = 10000 bytes in this install, which the log text crosses after ~105 lines, so every later render also pays a full-payload hash. Estimated cost today: 1-15 s of 45 min (0.05-0.5%) - honestly small - but it is quadratic: drop batch_size to 2000 and N ~= 5000 gives ~1.2 GB of string building and a visibly stalling tab. Each of those 1500 st.* calls is also a RerunException injection point (see the canc
- FIX: Throttle to <=2 updates/sec (`if now - last_render < 0.5 and not final: return`), hold lines in `collections.deque(maxlen=200)`, and replace the `st.empty()` + `.code()` pair with a single `st.status('Loading CSV...', expanded=True)` container that gets one appended line per update; fold the metric and status text into that same container so it is one ForwardMsg per update instead of three.
- GAIN: 3 ForwardMsgs per block -> <=2/sec regardless of block count; removes the quadratic term and the unbounded browser DOM growth. ~10 LOC. | EFFORT: S
- RISK: tests/test_app_state.py:102-105 asserts the literals 'log_container = st.empty()', '_append_load_log', '_render_load_log' - all three must be rewritten.

### F34. strict_preflight serializes the whole file twice and duplicates protection the failure path already provides
- WHERE: src/csv_click/app.py:295-298, 899-927; src/csv_click/pandas_loader.py:389-406, 409-436; src/csv_click/settings.py:33
- MECHANISM: Exactly 2x the CPU-bound work - and the CPU-bound serialization IS essentially all of the work - for any file <=50 MB. On the 1 GB path it is a second cold open of the file plus 200k redundant rows (~4% on 5M rows). Worse, the risk it exists to prevent (create tables, then fail mid-file) is already handled: `_create_and_load` drops both tables on any CsvClickError at app.py:995-1001.
- FIX: (a) Make the AppSettings default size-aware: off above LARGE_CSV_PRECHECK_THRESHOLD_BYTES. (b) For <=50 MB the whole file fits in memory - buffer the payload list produced by the preflight and hand it to the insert loop instead of throwing it away, so validation becomes free. (c) For >50 MB drop the second file open and validate the head that schema inference already read. (d) Add `help=` naming the cost ('doubles the time for files under 50 MB').
- GAIN: 2x on the <=50 MB path (the preflight becomes free rather than duplicative); one fewer full file open and ~4% less work on the 1 GB path. | EFFORT: M
- RISK: tests/test_app_state.py:384 test_create_and_load_uses_sample_validation_for_large_files asserts both function names plus the exact string 'File is larger than 50 MB; using sample validation'; :120 asserts 'validate_csv_with_pandas_chunks(' is present. Both must be updated. tests/test_settings.py:7 does not assert the strict_preflight default, so fl

### F35. batch_size / max_insert_payload_mb / load_workers are unguarded coupled numbers - this is the reported crash
- WHERE: src/csv_click/app.py:274-294, 597-599; src/csv_click/settings.py:30-33, 52-54; src/csv_click/pandas_loader.py:364-386, 443-478, 555, 683-722
- MECHANISM: Peak RSS per chunk ~= raw pandas chunk + the converted DataFrame (every branch of `_convert_series` ends in `.astype('object')`, so every cell is a boxed Python object at ~50-100 B overhead) + `lines: list[bytes]` holding every serialized row of the chunk (pandas_loader.py:451) + `b'\n'.join(lines)` doubling it + worker_count*2 pending payloads still referenced by futures (line 555). That is roughly 4-6x the CSV chunk size, times (1 + workers*2) for the in-flight copies. At 200 B/row, batch_size=1_000_000 is ~200 MB of CSV -> ~1-2 GB peak. The UI actively invites this: step=10_000 with no ceiling.
- FIX: (1) Add `max_value` to both numbers (batch_size <= 500_000, payload <= 64) and `help=` on all three naming the memory relationship. (2) Add an 'Auto' checkbox, default on, that measures average CSV bytes/row from the preview already read and derives batch_size = clamp(50_000, 64MB/avg_row_bytes, 500_000), max_insert_payload_mb = 16, load_workers = min(4, os.cpu_count()), showing the derived values read-only. (3) Show a live 'estimated peak memory ~ X GB' caption computed from the same formula. (4) Add `settings_version: int = 2` to AppSettings so migrations stop sniffing magic values.
- GAIN: Eliminates the crash class the user hit; replaces three coupled numbers the user must guess with one derived default. | EFFORT: M
- RISK: tests/test_app_state.py:164-179 and :181-196 assert the literals 'min_value=1', 'max_value=6', 'Batch size', 'Max insert payload, MB', 'Load workers', 'settings.load_workers'. Adding widgets and max_value is safe; renaming or removing any of those is not. Adding an AppSettings field is safe - load_app_settings filters unknown keys at settings.py:46

### F36. Terminal error text is written to a transient st.empty() and is destroyed by the very rerun the user needs to act on it
- WHERE: src/csv_click/app.py:860, 986-1011; src/csv_click/pandas_loader.py:658-680
- MECHANISM: To follow that advice the user must change a number_input, which triggers a rerun, which re-executes main() from the top and never re-renders the placeholder. The message and the entire 45-minute log vanish at the exact moment they become useful. Retrying is therefore blind. Note also that CsvLoadError is never named in any except clause in app.py - it is swallowed by the CsvClickError base at line 995.
- FIX: Persist the outcome: `st.session_state['last_load_result'] = {'status':..., 'message':..., 'log': list(log_messages), 'rows': inserted_rows, 'elapsed':...}` and render it near the top of main() on every run with a Clear button. Use `st.status(..., state='error')` rather than `st.empty().error`. Catch CsvLoadError explicitly so its hint gets a dedicated, more prominent presentation.
- GAIN: The failure message and the log survive the rerun; retry stops being blind. ~12 LOC. | EFFORT: S
- RISK: Storing the log in session_state needs the deque cap from the throttling finding so a long load does not pin megabytes of strings per session.

### F37. Auto-dropping both target tables on any unexpected exception destroys 45 minutes of loaded data without confirmation
- WHERE: src/csv_click/app.py:828-842, 995-1011; src/csv_click/clickhouse.py:146-161, 329-332; src/csv_click/pandas_loader.py:524, 574
- MECHANISM: One transient HTTP 5xx or read timeout on insert 487 of 500 destroys 44 minutes of replicated data and forces a full restart from row 0. `create_tables` already cleans up its own partial DDL failures internally (clickhouse.py:329-332), so `_cleanup_after_failed_load` mostly fires for load-phase failures - precisely the case where dropping is most expensive.
- FIX: (1) Retry each raw_insert_batch 3x with exponential backoff on transport-level errors before failing the load. (2) Stop auto-dropping on load-phase failure: leave the tables, report how many rows landed and which chunk/block failed (`_raw_insert_error` already carries all of it), and offer an explicit 'Drop target tables' button plus 'Resume from chunk N' that passes skiprows to pd.read_csv. (3) Keep auto-drop only for DDL-phase failures.
- GAIN: Turns a total loss into a resumable one and removes an unconfirmed destructive ON CLUSTER DDL from the error path. | EFFORT: M
- RISK: User-visible behavior change; not covered by any test in tests/test_app_state.py, so the suite stays green. Resume needs the loader to report a stable chunk boundary.

### F38. build_table_names is called outside the try block, so an invalid table name produces a raw Streamlit traceback
- WHERE: src/csv_click/app.py:96 (vs try at 110-128); src/csv_click/clickhouse.py:56-64
- MECHANISM: Typing `my-table`, `orders 2024`, or a Cyrillic name - all plausible for this user base - replaces the entire wizard with a red Streamlit traceback instead of a field error. The handler that was clearly meant to catch this misses by 14 lines.
- FIX: Move line 96 inside the existing try, or better, add `distributed_table` to the `errors.append(...)` validation block at app.py:346-354 using the same regex, so the user gets an inline field error before reaching the DDL step.
- GAIN: A one-line move removes a full-page crash from the most-typed field in the form. | EFFORT: S
- RISK: None. No test covers it.

### F39. Fast-sample and Full-scan schema inference disagree about NA markers, silently writing the string 'nan' or failing 40 minutes in
- WHERE: src/csv_click/pandas_loader.py:87-112 (keep_default_na=False), 61-84 (no keep_default_na), 194-213, 216-245, 686; src/csv_click/app.py:53, 459-464, 635-646
- MECHANISM: Any CSV containing pandas' default na_values tokens (NA, N/A, NULL, None, -, nan, 1.#IND ...) is written into ClickHouse as the literal four-character string 'nan' with no warning. On a numeric column the same mismatch instead raises 'empty value is not allowed for non-nullable ...' partway through the load - after 40 minutes, on the 1 GB path that has no progress bar to say how far it got. Full scan and Fast sample produce different schemas for the same file.
- FIX: Use one set of read options everywhere: add `keep_default_na=False, na_values=['']` to `iter_pandas_chunks` (pandas_loader.py:70-76) so it matches `preview_csv_rows`. Add a test that runs `analyze_csv_with_pandas_sample` and `analyze_csv_with_pandas_chunks` over the same fixture containing 'NA' and asserts identical schemas.
- GAIN: Removes a silent data-corruption path and a class of 40-minutes-in failures; also makes the two inference modes agree, which they currently do not. | EFFORT: S
- RISK: Changes inferred types for any CSV with pandas NA tokens (those columns become non-nullable String rather than Nullable). Align iter_pandas_chunks to the preview, not the reverse - tests/test_pandas_loader.py:75 test_preview_csv_rows_keeps_empty_values_as_strings pins the preview behavior.

### F40. create_tables can sleep up to 60 s on the script thread with zero UI feedback and no interrupt point
- WHERE: src/csv_click/clickhouse.py:186-201, 303-328; src/csv_click/app.py:936-947
- MECHANISM: Worst case 60 s of blocking sleep between the 'Checking existing tables and creating DDL...' message and the next one. During that window there is no st.* call, so the app is both frozen and uninterruptible (same yield-point issue as the scan).
- FIX: Pass `log_callback` into `_verify_table_exists_on_cluster` and emit 'waiting for {db}.{table} on cluster, attempt i/30' each iteration; surface attempts/interval as parameters from the UI or reduce the default to attempts=15.
- GAIN: Removes a silent 60 s stall; ~4 LOC. | EFFORT: S
- RISK: None found: tests/test_clickhouse.py exercises create_tables at :166/:196/:214 and passes verify_attempts/verify_interval_seconds but asserts nothing about log_callback invocation counts (verified by grep).

### F41. Session state at risk on rerun - enumerate and protect the keys, and stop keeping the plaintext password there
- WHERE: src/csv_click/app.py:56-66, 271-273, 307-310, 330-341, 362, 380, 405-407, 481-482, 565-568, 657-661, 690, 698-701, 737, 745, 859-863
- MECHANISM: Three distinct losses. (1) The entire load outcome is unrecoverable after any rerun (see the transient-error finding). (2) A browser refresh creates a NEW session: csv_path, schema_rows, type_rows, csv_preview_rows and both confirmed flags are gone, so the user re-reads the CSV - which in Full-scan mode means re-scanning 1 GB. (3) `order_by` and `sharding_column` are `st.selectbox(..., options=target_names)` with NO key= (271, 273); target_names comes from the schema, so editing any target_name in the mapping editor changes the options list, changes the widget's auto-generated identity, and silently resets both selections to index 0 - the user can pick a different ORDER BY and sharding key t
- FIX: Give order_by and sharding_column explicit `key=` values and re-validate the stored choice against the new options instead of resetting. Persist load progress/outcome to session_state (see the transient-error finding). Keep the password out of the persisted params dict - store it in a separate, non-persisted key or read it only at get_client() time. Consider caching the inferred schema keyed by (path, mtime, read_options) so a refresh does not force a rescan.
- GAIN: Removes a silent wrong-ORDER-BY footgun, removes a full rescan after refresh, and takes a plaintext secret out of long-lived session state. | EFFORT: M
- RISK: tests/test_app_state.py:8 test_streamlit_widget_keys_are_not_reused_for_custom_session_state asserts widget keys and custom session_state keys are disjoint - new key= values must not collide with the CSV_READ_STATE_KEYS names.

### F42. 'Apply parameters' does not apply anything - params are written to session_state on every rerun regardless
- WHERE: src/csv_click/app.py:316-322, 368-401
- MECHANISM: The button labelled 'Apply parameters' has no effect on the current run's behavior - the values are already live. Users press it expecting it to be the commit step (the help text at 247-249 reinforces a 1-2-3 sequence) and get no feedback that anything happened. Meanwhile the real, invisible side effect is a write to the user's home directory.
- FIX: Rename to 'Save as defaults' and show `st.toast('Defaults saved to ~/.csv_click/settings.json')`, or make it an actual gate that sets a `params_confirmed` flag the load button requires.
- GAIN: Removes a lying label; ~3 LOC. | EFFORT: S
- RISK: tests/test_app_state.py:442 asserts the literal '"Apply parameters"' and key='apply_load_params_button' in the form source - renaming the label breaks it.

### F43. Duplicate Test connection button and a redundant second encoding probe at load time
- WHERE: src/csv_click/app.py:100-108 vs 316-328; app.py:628-632/657 vs 875-882
- MECHANISM: One redundant control the user has to reason about, and one redundant multi-encoding probe (cheap at nrows=20, but it is 4 extra file opens plus 4 `_mojibake_score` scans on the critical path of every load, and it is the second place that can raise the unrecoverable 'likely already corrupted' error).
- FIX: Keep the single Test connection button in the form. At load time read the effective options from session_state and re-probe only if the file mtime changed since analysis.
- GAIN: -1 duplicate control, -4 file opens per load, -1 place the mojibake hard-fail can fire. | EFFORT: S
- RISK: tests/test_app_state.py:118-119 asserts 'choose_read_options_for_preview' and 'effective_read_options' appear inside _create_and_load - removing the call breaks it.

### F44. loader.bat reinstalls dependencies on every launch and refuses to start when offline
- WHERE: loader.bat:25-34, 36-38, 99-104
- MECHANISM: 5-20 s of startup tax every single time, and a hard refusal to start on a laptop with no package-index access even when .venv is already complete and the app would run perfectly. The check that would prove the venv is fine (line 37) is gated behind the step that can block it.
- FIX: Run the import check first; only fall through to the pip steps if it fails. Add an explicit `loader.bat --reinstall` path for deliberate upgrades.
- GAIN: Near-instant startup on the common path; the app launches offline. | EFFORT: S
- RISK: tests/test_loader_bat.py:6 asserts the literal strings 'python -m pip install --upgrade pip' and 'python -m pip install -r requirements.txt' are present in the file - reordering keeps them present, so the test stays green.

### F45. README documents a UI that no longer exists
- WHERE: README.md:160-188 vs src/csv_click/app.py:72-152, 419-500
- MECHANISM: The README is the only onboarding artifact for a tool aimed at non-engineers; following it literally derails the user at step 2 and never mentions the two settings (Load workers, Schema inference mode) that most affect how long they wait.
- FIX: Rewrite lines 160-188 against the current main() order, add the three missing controls, delete the rand() claim, and state the batch_size/payload/workers guidance from the settings finding in one place.
- GAIN: ~30 lines rewritten; removes the largest source of user confusion outside the app itself. | EFFORT: S
- RISK: tests/test_loader_bat.py:29 asserts two Russian strings and their relative order, both in the launch section, which is untouched.

### F46. tests/test_app_state.py is 24 source-text assertions, not behavior tests - it will veto most ergonomics fixes while catching none of the real bugs
- WHERE: tests/test_app_state.py:1-458
- MECHANISM: The suite pins implementation strings, so it fails on behavior-preserving refactors and passes on behavior-breaking ones - it did not catch the dead progress bar, the non-functional cancel flag, the uncaught ValueError at app.py:96, or the NA-handling divergence, all of which it structurally 'covers'. Every one of the findings above requires editing these assertions, which means the suite gives zero safety signal for exactly the changes being proposed.
- FIX: Use `streamlit.testing.v1.AppTest` (present in the installed streamlit 1.58) to run app.py headless: set widget values, click buttons, assert on rendered elements and session_state. Keep the two tests that encode genuine invariants - widget keys disjoint from custom state keys (:8) and the wizard step order (:42) - and delete the string-matching rest.
- GAIN: -~350 lines of substring assertions, replaced by ~120 lines that actually execute the wizard; unblocks every ergonomics change and starts catching real regressions. | EFFORT: L
- RISK: AppTest cannot exercise the ClickHouse path without fakes - scope it to the CSV/mapping/types/params steps and keep the loader tests in tests/test_pandas_loader.py where they already run real code.

### F47. YAGNI: csv_reader.py is an entire second CSV reading path that nothing imports
- WHERE: src/csv_click/csv_reader.py:1-54; tests/test_csv_reader.py:1-24
- MECHANISM: A stdlib-csv chunked reader kept alive next to the pandas one it lost to; every future schema or type change has to be considered against a code path that can never run.
- FIX: src/csv_click/csv_reader.py:1: an entire duplicate CSV read path with zero production callers. Delete the module and tests/test_csv_reader.py.
- GAIN: -54 LOC of src, -24 LOC of tests, -1 module. | EFFORT: S
- RISK: None - no src importer. `convert_value` (its only schema.py dependency) is still used by pandas_loader.py:706/718/728.

### F48. YAGNI: detect_dialect reads the whole file into RAM to inspect 8 KB
- WHERE: src/csv_click/csv_reader.py:14; src/csv_click/schema.py:357
- MECHANISM: On the user's 1 GB CSV that is a ~1 GB decode + allocation to sniff a delimiter. It has never fired in production only because both call sites are dead.
- FIX: src/csv_click/csv_reader.py:14: reads the entire file to look at its first 8 KB. `path.open(encoding='utf-8-sig').read(8192)` is the one-liner - but both call sites are dead, so delete them with their modules instead.
- GAIN: -20 LOC across two files; removes a latent 1 GB allocation. | EFFORT: S
- RISK: None; both are on the dead paths.

### F49. YAGNI: clickhouse.insert_batch and the Protocol.insert member it needs have zero references anywhere
- WHERE: src/csv_click/clickhouse.py:29-30, 336-348
- MECHANISM: It is the Native-format path README.md:6-9 claims was rejected, kept as a monument rather than a decision record.
- FIX: src/csv_click/clickhouse.py:336: `insert_batch` has zero callers including tests. Delete it and the `insert` member of the ClickHouseClient Protocol; if the rationale matters, record it as a comment or an ADR rather than as unreachable code.
- GAIN: -15 LOC. | EFFORT: S
- RISK: None. Note: any future move to client.insert()/insert_df() would write a new implementation anyway, since this one takes list-of-lists and no column_type_names.

### F50. YAGNI: ensure_tables_do_not_exist is dead - create_tables does its own existence check
- WHERE: src/csv_click/clickhouse.py:109-118, 278-286; tests/test_clickhouse.py:17, 142
- MECHANISM: Two entry points for one invariant; the dead one could drift from the live one (the live one additionally tolerates and cleans up partial state at 283-286, the dead one does not).
- FIX: src/csv_click/clickhouse.py:109: `ensure_tables_do_not_exist` has no src callers; create_tables:278 already enforces the same invariant with better partial-state handling. Delete it and its test.
- GAIN: -10 LOC of src, -1 test. | EFFORT: S
- RISK: None.

### F51. YAGNI: chunk_to_json_each_row_payload is a one-line wrapper used only to measure a row's length in two tests
- WHERE: src/csv_click/pandas_loader.py:439-440; tests/test_pandas_loader.py:17, 216-223, 233
- MECHANISM: An unbounded-memory public API (it joins the entire chunk with no payload cap - exactly the thing iter_json_each_row_payloads exists to prevent) kept alive as a test helper.
- FIX: src/csv_click/pandas_loader.py:439: a public unbounded-payload builder whose only consumers are two tests. Rewrite those tests against `iter_json_each_row_payloads` (already imported in the same file) with `max_payload_bytes` large, and delete it.
- GAIN: -2 LOC of src, -1 public API that contradicts the module's own payload-bounding invariant. | EFFORT: S
- RISK: tests/test_pandas_loader.py:216 and :226 both change; :226 needs a different way to compute the single-row size (build one row via iter_json_each_row_payloads with a huge cap).

### F52. YAGNI: schema.py carries a complete stdlib-csv reimplementation of the pandas pipeline
- WHERE: src/csv_click/schema.py:167-203, 206-229, 354-363, 366-371, 374-381, 429-433; tests/test_schema.py:19, 26, 57, 70, 87, 108
- MECHANISM: Two implementations of type inference and validation that can and do diverge (the csv one uses `_detect_dialect` sniffing, the pandas one uses the user's chosen separator; the csv one strips values via `_ColumnStats.add_value`, the pandas one goes through `_value_to_string`). Readers cannot tell which one runs.
- FIX: src/csv_click/schema.py:167: a full duplicate CSV analysis+validation pipeline with no src callers. Delete these six functions - BUT first port the six tests that use them (tests/test_schema.py:19,26,57,70,87,108) onto `analyze_csv_with_pandas_sample`, because they are currently the only coverage of `_infer_type`'s decision table (UInt64 vs Int64 vs Decimal vs Bool vs String fallback), which IS live.
- GAIN: -~90 LOC of duplicate pipeline; one inference implementation instead of two. Coverage must move, not disappear. | EFFORT: M
- RISK: Deleting without porting the tests silently drops all coverage of _infer_type and convert_value edge cases.

### F53. YAGNI: schema_from_editor_rows / schema_to_editor_rows are drifting twins of the pandas_loader pair
- WHERE: src/csv_click/schema.py:285-307, 310-323 vs src/csv_click/pandas_loader.py:325-339, 342-361; tests/test_schema.py:94-105, 121-131
- MECHANISM: Two answers to 'what does an unchecked nullable box mean', both shipped, differing silently.
- FIX: src/csv_click/schema.py:285: an editor-row converter pair duplicated in pandas_loader.py:325 with divergent nullable defaults, used only by tests. Delete it; keep the pandas_loader pair as the single implementation and move the two useful test cases (manual Nullable override, DateTime64 custom type) onto `mappings_from_editor_rows`.
- GAIN: -38 LOC and one class of silent divergence. | EFFORT: S
- RISK: tests/test_schema.py:121 is the only coverage of `DateTime64(3, 'Europe/Moscow')` surviving validate_clickhouse_type_expression - port it, do not drop it.

### F54. YAGNI: the mojibake scoring heuristic guesses at what the codec already knows for certain
- WHERE: src/csv_click/pandas_loader.py:40, 183-191
- MECHANISM: It only recognises UTF-8-read-as-cp1251 for Russian text, silently ignores every other mojibake, and can false-positive on legitimate content. The information it approximates is available exactly: a strict decode either raises UnicodeDecodeError or it does not, and residual U+FFFD catches the rest.
- FIX: src/csv_click/pandas_loader.py:183: a hand-tuned Cyrillic bigram scorer approximating a decision the codec makes exactly. Replace with `try: raw.decode(enc) except UnicodeDecodeError` plus `'�' in text`, ranking candidates by whether strict decode succeeded.
- GAIN: -12 LOC and a heuristic replaced by a deterministic check. | EFFORT: S
- RISK: tests/test_pandas_loader.py:85 and :95 test detect_mojibake's messages - they change with the implementation.

### F55. YAGNI: detect_mojibake can only ever return None at its single call site
- WHERE: src/csv_click/pandas_loader.py:152-159, 171-180; tests/test_pandas_loader.py:85, 95
- MECHANISM: A guaranteed-None call plus a redundant full DataFrame scan, plus a 10-line function and two tests maintaining behavior that never occurs.
- FIX: src/csv_click/pandas_loader.py:159: `detect_mojibake(best_preview)` is provably always None here because line 152 already raised on any non-zero score. Delete the call, the function, and its two tests - or, better, make line 152 a warning instead of a raise so the function becomes reachable again (see next finding).
- GAIN: -12 LOC, -2 tests, -1 redundant preview scan per load and per analysis. | EFFORT: S
- RISK: None if deleted together; the two tests at :85/:95 go with it.

### F56. YAGNI/ergonomics: the mojibake check hard-fails the whole read with no user override
- WHERE: src/csv_click/pandas_loader.py:43-47, 152-157; src/csv_click/app.py:653-655
- MECHANISM: A single legitimate U+FFFD in the first 20 rows - common in exported data - makes the file permanently unloadable through this tool, with no checkbox to proceed anyway. The user's only recourse is to edit the CSV.
- FIX: src/csv_click/pandas_loader.py:152: an unrecoverable hard failure where a dismissible warning is already modelled. Downgrade to returning the existing MojibakeWarning and add a 'Load anyway' checkbox in the CSV step.
- GAIN: Removes a dead end; the warning path is already written (the dataclass and the app's warning rendering at app.py:532-534 both exist). | EFFORT: S
- RISK: Users could load genuinely corrupt files - which is their call to make, and is what the warning says.

### F57. YAGNI: choose_read_options_for_preview tries 4 encodings but two of them are the same codec
- WHERE: src/csv_click/pandas_loader.py:39, 120-127; src/csv_click/app.py:448, 453, 549
- MECHANISM: One guaranteed-wasted read pass per probe (x2 probes per load, see the redundant-probe finding), and a UI dropdown that presents the same codec twice - a user who tries 'cp1251' and then 'windows-1251' will conclude the tool is broken.
- FIX: src/csv_click/pandas_loader.py:39: 'cp1251' and 'windows-1251' are aliases of one codec, so the fourth candidate is unreachable. Drop the alias from ENCODING_SUGGESTIONS and from the app.py:448 dropdown; normalise user input with `codecs.lookup(enc).name`.
- GAIN: -1 wasted read pass per probe, -1 confusing UI option, -2 lines. | EFFORT: S
- RISK: app.py:549 `_encoding_index` lists the same four options and falls back to 'custom' - a saved settings.json with encoding='windows-1251' must still resolve, so normalise rather than reject.

### F58. YAGNI: the hand-rolled SQL-safety parser is two copies of one scanner where a regex or the server would do
- WHERE: src/csv_click/schema.py:36-48, 51-66, 69-84
- MECHANISM: 49 lines of bespoke parser defending an input space that is 18 enumerated values (CLICKHOUSE_TYPE_OPTIONS, schema.py:14-33) plus the occasional parameterized type. The two functions can drift, and neither is tested against an adversarial corpus.
- FIX: src/csv_click/schema.py:51: two near-identical hand-rolled scanners guarding an 18-value enum. Replace with a whitelist check against CLICKHOUSE_TYPE_OPTIONS plus one `re.fullmatch(r"(?:Nullable\\()?[A-Za-z][A-Za-z0-9_]*(?:\\([\\w ,'/:+.\\-]*\\))?\\)?", s)` for parameterized types; when a client is connected, `SELECT CAST(NULL AS <type>)` lets the server be the authority.
- GAIN: -34 LOC replaced by ~2, one validator instead of two near-duplicates. | EFFORT: S
- RISK: tests/test_schema.py:121 requires `DateTime64(3, 'Europe/Moscow')` to pass - the regex above accepts it (quote, digits, slash, comma inside parens). Verify against the existing type-expression tests in tests/test_pandas_loader.py before swapping.

### F59. YAGNI: validate_csv_with_pandas_chunks and validate_csv_sample_with_pandas_chunks are the same function twice
- WHERE: src/csv_click/pandas_loader.py:389-406, 409-436; src/csv_click/app.py:899-927
- MECHANISM: Two 15-line bodies to keep in sync, and 28 lines of near-identical branch code in app.py. A future change to the validation loop (e.g. the payload-reuse fix) has to be made twice.
- FIX: src/csv_click/pandas_loader.py:409: a row-capped copy of the function above it. Collapse to `validate_csv_with_pandas_chunks(..., max_rows: int | None = None)`; app.py's branch becomes `max_rows = None if small else sample_rows` plus one call and one log line.
- GAIN: -26 LOC in pandas_loader, -12 in app.py, one validation path instead of two. | EFFORT: S
- RISK: tests/test_app_state.py:397-400 asserts BOTH function names and both log strings appear in _create_and_load; tests/test_pandas_loader.py exercises both - update them.

### F60. YAGNI: column metadata exists in six shapes with three lossy hand-written converters between them
- WHERE: src/csv_click/pandas_loader.py:278-291, 294-322, 325-339, 342-361; src/csv_click/app.py:658-659, 690, 701, 737, 755-763, 766-790
- MECHANISM: Six representations, seven converters, and a join-by-source_name in the middle. AGENTS.codex.md:76 says 'Prefer dataclasses or Pydantic models over raw dicts for structured data' - this is the opposite. Each round trip is a chance for a field to be dropped or coerced (see the sample_values finding).
- FIX: src/csv_click/pandas_loader.py:278: the same column metadata is converted between six shapes by seven functions. Keep `list[SchemaMapping]` as the single source of truth in session_state; render each editor with `pd.DataFrame([asdict(m) for m in mappings])[visible_cols]` and merge edits back positionally by index; make `CsvSchema` a property/view that emits DDL columns rather than a third stored form.
- GAIN: -~90 LOC across app.py and pandas_loader.py and four fewer representations to keep in sync. | EFFORT: L
- RISK: Touches every step of the wizard and most of tests/test_pandas_loader.py's mapping tests plus tests/test_app_state.py:78 (asserts `st.session_state["type_rows"]` is used and `st.session_state["schema_rows"]` is not, inside _create_and_load).

### F61. YAGNI: _split_sample_values is duplicated and round-trips sample values through a lossy comma join
- WHERE: src/csv_click/pandas_loader.py:335, 357, 763-766; src/csv_click/schema.py:303, 429-433; src/csv_click/app.py:721, 786
- MECHANISM: A display-only field is parsed back out of its own display format, incorrectly, twice per wizard pass. app.py:721 already lists 'sample_values' in the data_editor's `disabled=[...]` list, so the user can never edit it - there is nothing to read back.
- FIX: src/csv_click/pandas_loader.py:763: `_split_sample_values` exists in two files and corrupts any sample value containing a comma, for a column the editor marks read-only. Carry sample_values on the stored SchemaMapping and look it up by source_name instead of re-parsing the display string; delete both copies.
- GAIN: -10 LOC across two files and one silent display-corruption bug. | EFFORT: S
- RISK: tests/test_pandas_loader.py exercises the editor-row round trip - assert the new behavior (comma-containing samples survive).

### F62. YAGNI: _duplicates implemented twice, 8 lines each, where Counter is one line
- WHERE: src/csv_click/pandas_loader.py:769-776; src/csv_click/schema.py:374-381
- MECHANISM: Not a hot path - it is a readability and duplication cost, 16 lines for something the stdlib does in one.
- FIX: src/csv_click/pandas_loader.py:769: `_duplicates` is duplicated verbatim in schema.py:374. Replace both with `{v for v, n in Counter(values).items() if n > 1}` in one shared place.
- GAIN: -14 LOC. | EFFORT: S
- RISK: None; identical semantics.

### F63. YAGNI: three ways to add or remove Nullable(...) coexist
- WHERE: src/csv_click/pandas_loader.py:755-760; src/csv_click/schema.py:279-282, 291-294, 350-351
- MECHANISM: Nullable handling is expressed four different ways, so 'is this column nullable' has four possible answers depending on which line you read.
- FIX: src/csv_click/pandas_loader.py:755: Nullable wrap/unwrap is implemented three times plus six inline startswith checks. Keep `unwrap_nullable` and one `with_nullable(type, nullable)` in schema.py, and derive `nullable` as a property rather than storing it alongside a type string that already encodes it.
- GAIN: -10 LOC and one representation of nullability instead of two (the bool field and the type prefix, which can disagree). | EFFORT: S
- RISK: SchemaMapping.nullable and final_type can currently disagree; unifying them may change behavior for hand-edited custom_type values - cover with a test.

### F64. YAGNI: three separate error classifiers substring-match exception prose instead of using the existing exception taxonomy
- WHERE: src/csv_click/app.py:817-825; src/csv_click/pandas_loader.py:668-673; src/csv_click/clickhouse.py:399-408; src/csv_click/errors.py:1-27
- MECHANISM: User-facing guidance depends on the English (or localized) wording of a third-party driver's error string. A ClickHouse or urllib3 version bump silently degrades all three messages to the generic fallback, and nothing tests that.
- FIX: src/csv_click/app.py:817: three independent classifiers re-derive the error kind from prose. Raise the specific errors.py subclass at the point where the condition is known (e.g. an InsertPayloadTooLargeError from raw_insert_batch's caller, a TableNotVisibleError from _verify_table_exists_on_cluster) and render with `except SpecificError` - which is what AGENTS.codex.md:26 'Fail loudly: raise specific exceptions with context' already asks for.
- GAIN: -25 LOC of string sniffing; error rendering becomes type-dispatched instead of substring roulette. | EFFORT: M
- RISK: tests/test_pandas_loader.py:486 asserts `pytest.raises(CsvLoadError, match='HTTP/proxy read limit')` and tests/test_clickhouse.py:300 tests _format_connection_error - both change shape.

### F65. YAGNI: the settings migration is keyed on a magic value with no version marker
- WHERE: src/csv_click/settings.py:19-35, 52-54; tests/test_settings.py:46-53
- MECHANISM: It silently overrides any user who deliberately sets batch_size to 1_000_000 on a settings file predating max_insert_payload_mb, and the next migration will need a second magic-value sniff. It is also the only written record that a large batch_size once broke something - a comment would carry more information than the code does.
- FIX: src/csv_click/settings.py:52: a one-shot migration keyed on a magic value and the absence of a field. Add `settings_version: int = 2` and branch on it; document the reason for the 1_000_000 clamp in a comment or in the new max_value guard rail.
- GAIN: -3 lines of magic replaced by 2 explicit ones; migrations become auditable and additive. | EFFORT: S
- RISK: tests/test_settings.py:46 writes `{"batch_size": 1000000}` with no version and expects 100_000 - a version-defaulting-to-1 branch keeps it green.

### F66. YAGNI: five identical help wrappers and ~100 lines of Russian prose embedded in app.py and pinned verbatim by tests
- WHERE: src/csv_click/app.py:155-258; tests/test_app_state.py:404-425
- MECHANISM: 104 of app.py's 1015 lines are prose; five functions of pure boilerplate; and fixing a typo in the help text fails the test suite. It also makes app.py look twice as complex as it is when judging where the real logic lives.
- FIX: src/csv_click/app.py:160: five wrapper functions whose only content is a constant title/body pair. Replace with `HELP: dict[str, str]` (or five .md files under src/csv_click/help/ loaded by key) and a single `_render_step_help(key)`; assert on the loaded content, not on app.py's source text.
- GAIN: -~40 LOC of wrapper boilerplate, ~100 lines of prose out of the logic file, and a test that stops failing on wording changes. | EFFORT: S
- RISK: tests/test_app_state.py:404 test_app_renders_inline_help_for_each_ui_step must be rewritten to read the new location - and should be, since it currently guarantees nothing about whether the help renders.

### F67. Compress the JSONEachRow request body — raw_insert() ships it 100% uncompressed today
- WHERE: src/csv_click/clickhouse.py:351-363 (raw_insert_batch), .venv/Lib/site-packages/clickhouse_connect/driver/httpclient.py:402-434 (raw_insert), httpclient.py:372-373 (data_insert, the contrasting path), httpclient.py:205-214 (write_compression init)
- MECHANISM: At the measured 0.93 MB/s effective wire rate, ~2.5 GB of body bytes = ~2700 s. Every byte of JSONEachRow is ASCII text that repeats every column name on every row (measured 2.34x expansion over the source CSV) and is then encrypted and pushed through TLS without any entropy coding. lz4 removes ~69% of those bytes for 1.6% extra CPU; zstd-1 removes ~81%. Order of magnitude: 2.51 GB -> 0.78 GB (lz4) or 0.47 GB (zstd1) for a 1 GiB CSV.
- FIX: In `csv_click.clickhouse.raw_insert_batch`, compress before the call and declare the encoding:    import lz4.frame, zstandard   def raw_insert_batch(client, database, table, column_names, payload, codec='zstd'):       if codec == 'zstd':           body = zstandard.ZstdCompressor(level=1).compress(payload)   # NEW ctx per call - see risk       elif codec == 'lz4':           body = lz4.frame.compress(payload)       else:           body, codec = payload, None       client.raw_insert(table=f'{quote_identifier(database)}.{quote_identifier(table)}',                         column_names=column_names, insert_block=body,                         fmt='JSONEachRow', compression=codec)  Do the compressio
- GAIN: If the load is wire-bound (measured evidence says ~90% of wall time is), 3.2x with lz4 and 5.4x with zstd-1 on the transfer portion for a typical CSV; 1.7x-13x depending on data entropy. 45 min -> ~14 min (lz4) / ~9 min (zstd1) for the measured profi | EFFORT: S
- RISK: HIGH-VALUE BUT TWO REAL HAZARDS. (1) THREAD SAFETY: sharing one `zstandard.ZstdCompressor` across threads SEGFAULTS CPython — reproduced twice on this machine (zstandard 0.25.0 / py3.12 / Windows, exit code 139, 4 threads x 16 calls on one shared instance). Construct a fresh ZstdCompressor per call, or use module-level `zstandard.compress()` (state

### F68. Switch the wire format from JSONEachRow to Native via client.insert() — gets compression for free and cuts client CPU 10x
- WHERE: src/csv_click/clickhouse.py:351-363, src/csv_click/pandas_loader.py:524 and :574 (the two raw_insert_batch call sites), .venv/.../driver/client.py:934-983 (insert), client.py:1150-1151 (the DESCRIBE round trip to avoid), .venv/.../driver/httpclient.py:355-400 (data_insert), transform.py:107-141
- MECHANISM: Three costs stack on JSONEachRow: (a) the client materializes two Python dicts and one json.dumps per row (measured 103k rows/s vs 1.09M rows/s for Native); (b) the payload is 3.24x larger before compression and 8.5x larger than Native+lz4; (c) the SERVER must tokenize JSON, match every key string to a column, and text-parse every scalar. Native is ClickHouse's internal block format — the server memcpy's columns straight into blocks, so insert-side parse CPU on the cluster drops to near zero. Published/first-principles estimate (NOT measured here, no server reachable): JSONEachRow parsing costs roughly 5-15x the CPU of Native and 2-4x that of CSV/TSV per row.
- FIX: Replace the JSONEachRow raw_insert path with:    ctx = client.create_insert_context(table=distributed_table, database=database,                                      column_names=cols,                                      column_type_names=[m.final_type for m in mappings if m.include],                                      column_oriented=True,                                      settings={...})   # one per worker thread, reused   ...   client.insert(data=[converted[c].tolist() for c in cols], context=ctx)  and pass `compress='zstd'` (or leave the default for lz4) in csv_click.clickhouse.get_client. Note: an InsertContext is stateful (current_row/current_block, insert_exception) — build one p
- GAIN: Wire bytes for a 1 GiB CSV: ~2.51 GB today -> ~0.30 GB (Native+lz4) or ~0.25 GB (Native+zstd), i.e. 8-10x fewer bytes. Client serialization CPU: ~10x lower (4.4 min -> ~25 s for 1 GiB). Server insert CPU: estimated 5-15x lower, unmeasurable from here | EFFORT: L
- RISK: Largest change of any in this report. (1) `np.nan` is NOT accepted where `None` is required — VERIFIED: the same context with np.nan in a Nullable(String) column raised `AttributeError: 'float' object has no attribute 'encode'` from driverc/dataconv.pyx:488, which the driver swallows into ctx.insert_exception and re-raises from data_insert. Any cod

### F69. Override date_time_input_format=best_effort, which clickhouse-connect force-sets on every single request
- WHERE: .venv/Lib/site-packages/clickhouse_connect/driver/client.py:234-235, applied via httpclient.py:244-247 (set_client_setting writes self.params) and httpclient.py:567 (self.params merged into every request's URL params)
- MECHANISM: Per-value cost on the SERVER for every DateTime/Date column in the file. For a 1 GiB CSV with 5M rows and 2 temporal columns that is 10M best_effort parses. Order of magnitude, not measured here (no server): best_effort is commonly 2-5x the cost of the basic parser per value; on a table where temporal columns are a meaningful share of the row this can be a single-digit percentage up to ~20% of server-side insert parse time.
- FIX: Immediately after `clickhouse_connect.get_client(**kwargs)` in csv_click.clickhouse.get_client:      client.set_client_setting('date_time_input_format', 'basic')  (or pass settings={'date_time_input_format':'basic'} per raw_insert). Guard it: only do this if the serializer is guaranteed to emit second-precision values.
- GAIN: Estimated 2-5x reduction in the DateTime-parsing component of the server-side insert; overall load improvement is data-dependent and probably in the 0-10% range for typical schemas. Low, but the change is one line and it also makes bad datetimes fail | EFFORT: S
- RISK: REAL CORRECTNESS INTERACTION, must be paired. `_clean_json_value` (pandas_loader.py:742-745) uses `value.isoformat()`, which emits fractional seconds (e.g. '2026-03-11T05:10:00.123456') whenever the pandas Timestamp carries microseconds. `best_effort` tolerates and truncates that; `basic` reading a `DateTime` (second precision, the only datetime ty

### F70. Apply the max-payload cap to the COMPRESSED body, not the raw JSON — 3-5x fewer HTTP round trips for free
- WHERE: src/csv_click/pandas_loader.py:443-478 (iter_json_each_row_payloads), src/csv_click/app.py:69 (INSERT_PAYLOAD_SAFETY_RATIO = 0.9), app.py:597-599, app.py:884-898, src/csv_click/settings.py:31 (max_insert_payload_mb = 16)
- MECHANISM: Each HTTP request costs a full round trip plus fixed server-side work: query parse, Distributed fan-out setup, a block-level insert_deduplicate check against Keeper per shard, and one part created per shard per block. Cutting 392 requests to ~75-120 removes ~270-320 of those fixed costs and ~270-320 x (Keeper round trip + part creation). Also fewer, larger parts means less background merge pressure on ReplicatedMergeTree — the vendored operations-and-performance.md warns 'Small frequent inserts create parts pressure'.
- FIX: Change iter_json_each_row_payloads (or its caller) to size on the compressed output: accumulate lines, and flush when the *compressed* size would exceed the budget. Cheapest correct implementation without double-compressing: keep the existing raw-byte accumulator but multiply the budget by a measured, per-file compression ratio, recomputed from the first few blocks (e.g. budget_raw = max_payload_bytes / observed_ratio, clamped). Or use a streaming compressor (`zstandard.ZstdCompressor().stream_writer` / `lz4.frame.LZ4FrameCompressor`) and flush when the compressor's emitted byte count crosses the budget — that is exact and costs nothing extra. Also decouple the two knobs in the UI: 'Batch si
- GAIN: 3-5x fewer HTTP requests for the same wire budget. If per-request fixed cost is significant (see finding on X-ClickHouse-Summary for how to measure it), this is worth a further 1.2-2x on top of the compression win; if the link is purely bandwidth-lim | EFFORT: M
- RISK: Larger blocks mean a larger blast radius per failed request (a retried 40 MB-of-rows block re-does more work) and more peak RAM in flight. Memory: _load_csv_via_raw_insert_parallel holds up to max_pending = worker_count*2 payloads (pandas_loader.py:555) — at 6 workers and a raw budget grown to ~48 MB that is 12 x 48 MB = 576 MB of payload bytes alo

### F71. The effective upload timeout is connect_timeout (60 s), not send_receive_timeout (1800 s)
- WHERE: src/csv_click/clickhouse.py:86-87, .venv/Lib/site-packages/clickhouse_connect/driver/httpclient.py:182, .venv/Lib/site-packages/urllib3/connectionpool.py:457-459 and 493 and 518-530, .venv/Lib/site-packages/urllib3/connection.py:436-439
- MECHANISM: Not a throughput cost — a failure mode and a ceiling on how large a payload may safely be. Each individual send()/SSL_write inside sendall() must complete within 60 s. On a link that stalls (VPN reconnect, proxy backpressure, a shard blocking the initiator), a multi-MB body upload raises socket.timeout, urllib3 wraps it, and httpclient.py:596-621 turns it into OperationalError. That is a plausible contributor to the crash the user sees when the payload grows, and it is one of the reasons the app's own 0.9 safety ratio exists.
- FIX: Raise connect_timeout in csv_click.clickhouse.get_client to something proportional to the largest expected body over the slowest expected link — e.g. connect_timeout=300 — and note in the code comment WHY (it is the send timeout, not just the TCP-connect timeout). Keep send_receive_timeout=1800. Compressing the body (finding #1) shrinks the exposure 3-5x by itself. Do not set connect_timeout=None: httpclient.py:178-182 accepts None but then a genuinely dead endpoint hangs forever.
- GAIN: No throughput gain; removes a class of spurious failures on large payloads and slow links. This is a prerequisite for safely raising the payload cap (previous finding). | EFFORT: S
- RISK: A larger connect timeout means a genuinely unreachable host takes 5 min instead of 1 min to fail, which is user-visible in the Streamlit wizard since the load runs synchronously in the script thread. No existing test asserts on the timeout values (grep of tests/test_clickhouse.py shows only client_cert kwargs are asserted, at :278-297), so nothing 

### F72. Every get_client() builds its own urllib3 PoolManager: N full mTLS handshakes + 4 bootstrap round trips per worker, and the clients are never closed
- WHERE: src/csv_click/clickhouse.py:74-99, src/csv_click/app.py:604, app.py:931, app.py:981 (client_factory=lambda: get_client(config)), .venv/.../driver/httpclient.py:143-153 and 787-793, .venv/.../driver/httputil.py:95-130 and 193-200, .venv/.../driver/client.py:197-235
- MECHANISM: A mutual-TLS handshake over a WAN is typically 3-4 RTTs plus asymmetric crypto on both ends — order 100-500 ms each. Six of them plus 18 bootstrap round trips is roughly 2-10 s of pure startup latency per load, which is small against 45 min but is pure waste and grows with worker count. The leak is the more insidious one: an operator who clicks 'Test connection' twenty times while tuning the form has twenty live PoolManagers with open sockets to a bank gateway.
- FIX: Three independent changes: (a) Create ONE PoolManager for the whole load and pass it to every client: build it once with get_pool_manager(verify=False, client_cert=..., client_cert_key=..., maxsize=max(8, worker_count)) and pass `pool_mgr=<that>` to clickhouse_connect.get_client. httpclient.py:129 assigns `self.http = pool_mgr` and then line 143's `if not self.http` short-circuits, so the shared manager is used and _owns_pool_manager stays False (so a stray close() cannot clear it). Sockets are then reused across workers, and the handshake count drops to (at most) maxsize regardless of worker count. (b) Close clients when the load finishes — wrap the worker clients in try/finally and call .c
- GAIN: 2-10 s saved per load (small in absolute terms), plus removal of a socket/PoolManager leak that grows unboundedly across a Streamlit session. The shared-pool change is also a prerequisite for finding #8 (socket buffer tuning) since it gives one place | EFFORT: M
- RISK: httputil.get_pool_manager_options (httputil.py:80) defaults maxsize=8 and block=False; with block=False and worker_count > maxsize, urllib3 silently creates and discards extra connections instead of blocking — set maxsize >= worker_count. A shared manager also means one worker's clear() (from check_conn_expiration, see next finding) drops everyone'

### F73. max_connection_age forces a pool-wide socket teardown every 10 minutes mid-load
- WHERE: .venv/Lib/site-packages/clickhouse_connect/driver/common.py:75, .venv/.../driver/httputil.py:133-141, called from .venv/.../driver/httpclient.py:582
- MECHANISM: Each forced clear() discards all warm keep-alive sockets and the next insert pays a full TCP + mutual-TLS handshake (order 100-500 ms on a WAN) plus TCP slow-start from a cold congestion window — which on a bandwidth-limited link costs more than the handshake itself for the first few MB of the next body.
- FIX: Before creating clients: `clickhouse_connect.common.set_setting('max_connection_age', 0)` (0 disables the check — httputil.py:135 `if reset_seconds:`), or set it well above the expected load duration, e.g. 4*3600. Do it once at app startup in csv_click.clickhouse, and log the choice. Note set_setting is process-global, which is fine for this single-purpose Streamlit app.
- GAIN: Removes ~4 x worker_count handshakes plus the associated TCP slow-start restarts from a 45-min load. Estimated 5-60 s recovered depending on RTT and how quickly the congestion window recovers; small, but it is a one-line change with no downside for a | EFFORT: S
- RISK: max_connection_age exists to defend against long-lived connections pinned to one node behind a load balancer (so rebalancing/DNS changes are picked up) and against silently-dead NAT'd sockets. Disabling it entirely is appropriate for a bounded bulk load but wrong for a long-running service. Prefer a large finite value over 0. No test covers this.

### F74. urllib3 socket options hardcode SO_SNDBUF=256 KiB, which disables TCP send autotuning and caps per-connection upload to 256 KiB / RTT
- WHERE: .venv/Lib/site-packages/clickhouse_connect/driver/httputil.py:37-42 (core_socket_options) and httputil.py:71-92 (get_pool_manager_options, which OVERWRITES any caller-supplied socket_options at line 90)
- MECHANISM: If the ceiling is BDP rather than link bandwidth, then with load_workers=1 the app is using ONE socket and is capped at 256 KiB/RTT no matter how fast the link actually is. Each additional worker adds an independent socket (each worker gets its own client and, today, its own PoolManager), so throughput should scale close to linearly with workers up to the real link capacity.
- FIX: FIRST, run the discriminating experiment, which costs nothing and needs no code change: run the same file at load_workers=1 and load_workers=4 and compare MB/s. If it scales ~4x, the bottleneck is per-connection BDP, not bandwidth, and raising workers (UI already allows up to 6, app.py:287-293) is the cheapest large win available. If it does not scale, the link is genuinely ~1 MB/s and compression is the only lever. If BDP-bound and you want more than 6 sockets' worth: before creating any client, extend the buffer —     import clickhouse_connect.driver.httputil as hu     hu.core_socket_options = [o for o in hu.core_socket_options if o[1] != socket.SO_SNDBUF] + \                              
- GAIN: Unknown until the 1-vs-4-worker experiment is run — that is the honest answer. IF BDP-bound: up to 4-6x from workers alone (multiplicative with the 3-5x from compression), i.e. 45 min could fall below 5 min. IF bandwidth-bound: zero. The experiment t | EFFORT: S
- RISK: Mutating a third-party module global is fragile across clickhouse-connect upgrades — isolate it in one function in csv_click.clickhouse with a comment naming the upstream line, and make it best-effort (wrap in try/except and log). Raising SO_SNDBUF raises per-socket kernel memory (4 MB x 6 workers = 24 MB, negligible). Raising load_workers raises c

### F75. ClickHouse server-side insert settings: set input_format_parallel_parsing chunking, and explicitly do NOT set the other seven
- WHERE: src/csv_click/clickhouse.py:74-99 (get_client passes no `settings` dict at all), src/csv_click/clickhouse.py:358-363 (raw_insert_batch passes no `settings`), src/csv_click/clickhouse.py:228-236 (the ReplicatedMergeTree DDL this reasoning depends on), .venv/.../driver/httpclient.py:430 (`params.updat
- MECHANISM: The one genuine miss is parallel parsing. Every 6.38 MB JSONEachRow payload is currently parsed by a SINGLE server thread because it falls under min_chunk_bytes_for_parallel_parsing (10 MiB). If server-side JSON parsing is a meaningful share of the ~6 s per request that the measured numbers imply, single-threaded parsing is leaving most of the shard's cores idle during the insert.
- FIX: Pass a settings dict on the insert (httpclient.py:430 merges it into the URL params, and per-request settings override the client-level self.params):    INSERT_SETTINGS = {       'input_format_parallel_parsing': 1,       'min_chunk_bytes_for_parallel_parsing': 1048576,   # 1 MiB instead of 10 MiB       'date_time_input_format': 'basic',                 # see the separate finding   }   client.raw_insert(..., settings=INSERT_SETTINGS)  Guard against a locked-down server: clickhouse_connect's default invalid_setting_action is 'error' (common.py:73), so an unknown or read-only setting raises ProgrammingError at insert time and would abort the whole load. Either set `clickhouse_connect.common.set
- GAIN: For the parallel-parsing change: estimated 1.5-4x on the SERVER-side parse phase of each insert, which is an unknown fraction of the ~6 s/request — so anywhere from negligible to ~2x on total load time. Genuinely uncertain without server telemetry; t | EFFORT: S
- RISK: invalid_setting_action='error' is the trap: sending any setting the user's role cannot write kills the load. Filter against client.server_settings first. Lowering min_chunk_bytes_for_parallel_parsing increases per-insert server memory (more concurrent parser buffers) and CPU contention on a shared cluster — coordinate with whoever operates tp17. Pe

### F76. X-ClickHouse-Summary is parsed by the driver and thrown away — free server-side telemetry that would settle where the 45 minutes actually go
- WHERE: src/csv_click/clickhouse.py:351-363 (raw_insert_batch returns None), src/csv_click/pandas_loader.py:524 and :574 (return values discarded), src/csv_click/app.py:953-970 (on_progress has no timing data), .venv/.../driver/httpclient.py:433-434 and 436-445 (_summary), .venv/.../driver/summary.py:5-21
- MECHANISM: Not a cost — a missing measurement that makes every other optimization unfalsifiable. The measured 6.1 s average per request is consistent with TWO completely different explanations: (a) pure bandwidth at 0.93 MB/s, in which case only compression helps; or (b) a fixed ~6 s server/Distributed cost per request, in which case fewer-larger requests and parallel parsing matter far more than compression. elapsed_ns from the summary distinguishes them immediately: wall_time_for_the_POST minus elapsed_ns is the wire+TLS time.
- FIX: Return the QuerySummary from raw_insert_batch, thread it through the progress callback, and display two derived numbers in the Streamlit metrics block (app.py:964): rows/sec and MB/sec, plus a 'server %' = sum(elapsed_ns)/wall_clock. Concretely:    summary = client.raw_insert(...)   return summary.summary   # dict with written_rows, written_bytes, elapsed_ns, query_id  and in on_progress compute a rolling rate. Also log summary['query_id'] for the first and last block so a failed load can be traced in system.query_log. This also fixes the separate UX problem that the progress bar never moves for files >50 MB (app.py:962 `if total_rows:`, and total_rows stays 0 on the sampled-preflight path) 
- GAIN: No direct speedup. It is the instrument that tells you whether the compression change bought 3x or 5x, whether workers scale, and whether the server or the wire dominates — which is worth more than any single guess in this report. Zero marginal cost: | EFFORT: S
- RISK: None functionally. Some proxies strip X-ClickHouse-Summary, in which case summary.summary is {} and written_rows returns 0 — treat missing keys as unknown rather than as zero, or the UI will display a misleading '0 rows written'. Tests: tests/test_clickhouse.py:29-36 FakeClient.raw_insert returns None; it would need to return an object with a .summ

### F77. Every byte is written to the Distributed table, which stages it to initiator disk and re-sends it — 2x network, 1x extra disk
- WHERE: src/csv_click/app.py:977 (`table=distributed_table`), src/csv_click/clickhouse.py:254-262 (Distributed DDL, sharded by sipHash64), src/csv_click/clickhouse.py:56-58 (the `_local` name is already computed and available)
- MECHANISM: Single-node parse bottleneck plus disk round-trip. The whole cluster's parsing capacity is unused; one node's cores and one node's disk are the ceiling. This is invisible to the client because the initiator acks before the data reaches the shards — which also means the '45 minutes' may not include the tail of the background distribution at all.
- FIX: Ordered by cost: (a) CHEAP DIAGNOSTIC, no code change: after a load, check `system.distribution_queue` for the Distributed table to see whether the background sender is backlogged, and check whether all inserts share one initiator hostname. This tells you whether (b)/(c) are worth doing. (b) LOW RISK: set `distributed_background_insert_batch = 1` (older alias `distributed_directory_monitor_batch_inserts`) on the table or the insert so the background sender coalesces many staged files into one request per shard instead of one per block. Reduces the shard-side request count by the same factor as the block count. (c) THE REAL FIX, HIGH EFFORT: shard client-side and write directly to `<table>_lo
- GAIN: (b) estimated 1.1-1.5x on the distribution tail, mostly invisible to the client. (c) estimated 2-4x on the server-side portion for an N-shard cluster (parsing parallelizes across shards, disk staging disappears), but the number depends entirely on sh | EFFORT: L
- RISK: (c) is a substantial change to a load path that currently has a clean single-target contract, and it breaks the app's guarantee that ClickHouse owns the sharding decision. Getting sipHash64 semantics, shard weights, and the modulo exactly right in Python is error-prone; a mismatch silently misplaces rows so that queries through the Distributed tabl

### F78. 'read limit is reached' does not come from clickhouse-connect — and whether compression raises that ceiling is testable in one run
- WHERE: src/csv_click/pandas_loader.py:658-680 (_raw_insert_error), src/csv_click/app.py:894-898 and app.py:69 (INSERT_PAYLOAD_SAFETY_RATIO), tests/test_pandas_loader.py:426 and :484 (the tests that pin the string)
- MECHANISM: Not a slowdown — it is the constraint that has forced the user to run batch/bucket=20000 and to leave 10% of the payload budget unused, and it is the reason 'raising it causes a crash'. Every request is therefore 6.38 MB instead of something much larger, producing ~392 requests for a 1 GiB file (measured extrapolation) instead of ~75-120.
- FIX: Run the one-request discriminating experiment: send the same N rows twice, once as today and once lz4-compressed (which is ~3.2x smaller on the wire), at a raw size that reliably triggers the error today. If the compressed one succeeds, the limit is on the request body and finding #4 (size the cap on the compressed body) unlocks 3-5x fewer requests. If it still fails, the limit is post-decompression and the raw cap must stay. Regardless of the outcome, improve the diagnostic: _raw_insert_error currently reports only the uncompressed payload MB (pandas_loader.py:675-679). Report BOTH the uncompressed and the on-the-wire compressed size, plus the row count and the codec, so the next person can
- GAIN: No direct speedup; it converts a guess into a fact and, if the limit is body-side, it is what makes the 3-5x request-count reduction in finding #4 safe to ship. One test load answers it. | EFFORT: S
- RISK: None to implement. Note that enabling compression ALSO moves the INSERT statement into the URL query param (verified in finding #1's harness), so if the intermediary enforces a URI length limit you could trade one gateway error for another — mitigate by passing column_names=None for JSONEachRow. Tests: tests/test_pandas_loader.py:415-445 and :474-4

### F79. available_compression is ['lz4','zstd','gzip','deflate'] on this machine — brotli is not installed, and gzip is too slow to consider
- WHERE: .venv/Lib/site-packages/clickhouse_connect/driver/compression.py:14-18, .venv/.../driver/httpclient.py:205-214
- MECHANISM: Choosing the wrong codec or level is a self-inflicted CPU cost. gzip at 58 MB/s would consume ~43 s of CPU per GiB of JSON for a ratio zstd-1 beats at 553 MB/s. zstd's default level 3 is both slower AND slightly worse than level 1 on this kind of repetitive JSON, so accepting `zstandard.compress()`'s default (which is what the driver's ZstdCompressor uses, compression.py:56-58) leaves performance on the table.
- FIX: Pick zstd LEVEL 1 explicitly (`zstandard.ZstdCompressor(level=1)`) for the raw_insert path — best ratio-per-CPU on this data by a clear margin. Keep lz4 as the fallback for a gateway that does not understand `Content-Encoding: zstd`. Never offer gzip or deflate. Do not rely on brotli being present. If you go the client.insert()/Native route instead, note that the driver's ZstdCompressor (compression.py:56-58) hardcodes `zstandard.compress(block)` at level 3 with no way to pass a level — that is a reason to prefer the explicit raw_insert path if you want level 1, or to accept level 3 (measured 296 MB/s, still ~15x faster than the JSON serializer it replaces).
- GAIN: Choosing zstd-1 over zstd-3: same ratio, 1.9x less CPU. Choosing zstd-1 over gzip-6: same ratio, 9.5x less CPU. Choosing zstd-1 over lz4: 1.6x better ratio (5.38x vs 3.32x) for 1.3x more CPU — worth it whenever the link, not the CPU, is the constrain | EFFORT: S
- RISK: An intermediary that does not forward or understand `Content-Encoding: zstd` will reject or corrupt the request; lz4 has the same exposure. Make the codec configurable with a 'none' option and surface the choice in the UI or settings so a failing environment can be worked around without a code change. `Content-Encoding: zstd` is a registered HTTP c

### F80. Replace per-row JSONEachRow with column-oriented Native inserts (client.insert with column_type_names)
- WHERE: src/csv_click/pandas_loader.py:439-478 (iter_json_each_row_payloads / chunk_to_json_each_row_payload), 648-655 (_iter_rows/_json_each_row_line), 512-538 and 616-636 (both load loops), src/csv_click/clickhouse.py:351-363 (raw_insert_batch)
- MECHANISM: JSONEachRow costs one Python-level tuple, two dicts, N _clean_json_value calls, one json.dumps and one .encode per ROW, i.e. O(rows x cols) interpreter work with ~45 us/row for 20 columns. Native writes each COLUMN with one C-level loop (struct.pack/array-based writers in driverc) and emits fixed-width binary, so per-cell cost drops from ~2100 ns to ~215 ns. It also does not repeat the column names on every row (see the 469 -> 174 B/row raw drop) and it can compress in the same call. Order of magnitude: ~40 us/row -> ~12 us/row end-to-end, and 2.54 GB -> 0.57 GB on the wire for a 1 GB CSV.
- FIX: In load_csv_via_raw_insert, build a column-oriented block instead of JSON bytes and call client.insert(table=..., data=columns, column_names=..., column_oriented=True, column_type_names=[m.final_type for m in mappings], database=...). Pass column_type_names (or a single reusable InsertContext created once via client.create_insert_context(...) and reused per block) so the driver skips the DESCRIBE TABLE round trip at client.py:1150-1151. Set compress='lz4' (or write_compression) on get_client so data_insert picks it up at httpclient.py:372-374. Keep iter_json_each_row_payloads only if a JSON fallback toggle is wanted; otherwise delete it and chunk_to_json_each_row_payload with it. Replace the
- GAIN: 3.3-3.9x on client CPU (measured, 3 runs), 4.4x fewer bytes on the wire with lz4 / 6.4x with zstd (measured). For a 1 GB / 5.42M-row CSV: client CPU floor 246 s -> 63 s; wire 2.54 GB -> 0.57 GB. Server-side JSONEachRow parsing is also typically 3-10x | EFFORT: L
- RISK: Native is type-strict: the block must contain exactly the Python types each ClickHouseType expects (int for Int/UInt, decimal.Decimal for Decimal, datetime.date for Date, int epoch-seconds + column_formats[col]='int' for DateTime, bool for Bool, str for String, None only for Nullable). Any mismatch surfaces as a DataError instead of ClickHouse's le

### F81. Schema is inferred from a dtype=str preview but the load path calls read_csv with no dtype= -- silently corrupts Decimals, turns literal NULL/N/A into the string "nan", strips leading zeros, and drift
- WHERE: src/csv_click/pandas_loader.py:70-76 (iter_pandas_chunks: read_csv with NO dtype=, default keep_default_na) vs 93-100 (preview_csv_rows: dtype=str, keep_default_na=False), consumed by analyze_csv_with_pandas_sample:194-213 and analyze_csv_with_pandas_chunks:216-245; conversion at 683-722 (_convert_s
- MECHANISM: Not primarily a speed defect, but it is inseparable from the speed fix: read_csv is doing per-chunk type inference and float parsing whose result is immediately thrown away by str() in _convert_series. Measured: read_csv no-dtype 0.740 s vs dtype=str 1.005 s vs an explicit per-column dtype map 0.932 s for 200k x 20 -- so the inference is ~35% of read time and buys nothing for Decimal/Date/DateTime/Bool/String targets. Any vectorized rewrite of _convert_series requires a known input dtype anyway.
- FIX: Give iter_pandas_chunks the mapping list and build an explicit dtype map: dtype=str + keep_default_na=False for every target whose final type is String / Decimal(...) / Date / DateTime / Bool / any custom type, and the matching numpy dtype ('int64'/'float64') only for non-nullable Int64/UInt64/Float64 targets. Keep na_values=[] so only truly empty fields are NA, matching what preview_csv_rows already assumes. Then _convert_series receives exactly the strings the schema was inferred from, and convert_value/Decimal(str) become exact. Add a regression test asserting a Decimal(38, 10) column round-trips its digits and that a literal 'NULL' in a String column stays 'NULL'.
- GAIN: Correctness: eliminates silent precision loss on every Decimal column and the NULL/N/A -> "nan" substitution. Performance: read_csv gets ~0.19 s/200k rows slower with the hybrid dtype map, but it is a prerequisite for the vectorized converter, and th | EFFORT: M
- RISK: Columns whose CSV text is genuinely numeric but whose target is String will now keep their exact text (e.g. '100' stays '100' instead of becoming '100' via str(int) -- same result) -- but a column that previously produced '100.0' because a NaN forced float64 will now produce '100'. That is the intended fix, yet it changes bytes already loaded by ea

### F82. _clean_json_value calls pd.isna() on every scalar and re-dispatches an isinstance chain -- 467 ns per cell, the single largest per-cell cost in the JSON stage
- WHERE: src/csv_click/pandas_loader.py:734-748 (_clean_json_value), called from 654 (_json_each_row_line dict comprehension)
- MECHANISM: pd.isna is a Python function that does a type check, then np.isnan or libmissing.checknull, for every one of rows x cols scalars. At 5.42M rows x 20 cols = 108M cells for a 1 GB CSV, pd.isna alone is 108M x ~85 ns = 9.2 s, and the whole _clean_json_value is 108M x 467 ns = 50 s. The isinstance ladder re-derives, per cell, information that is already known statically from mapping.final_type -- the column type never changes within a load.
- FIX: If the Native rewrite (finding 1) lands, _clean_json_value disappears entirely. If JSON is kept as a fallback, precompute one cleaner function per column from mapping.final_type (a list of callables built once per load) instead of branching per value, and replace pd.isna(value) with `value is None` -- after finding 2's dtype fix and a vectorized converter, the only missing marker in the frame is None, never NaN/NaT. Better still: do the cleaning column-wise and vectorized in convert_chunk_to_schema (Timestamp -> .dt.strftime once per column, Decimal -> .astype(str) once per column) so _json_each_row_line becomes a plain dict build.
- GAIN: ~467 ns/cell -> ~30-60 ns/cell for the JSON path (ESTIMATE, 8-15x on this stage), i.e. ~9.4 us of the measured 45.4 us/row for 20 columns. Moot if finding 1 is implemented. | EFFORT: S
- RISK: Switching pd.isna to `value is None` is only safe once the frame is guaranteed NaN-free; today _convert_series line 686 can leave np.nan in a non-nullable String column (see finding 2b), and pd.errors would then leak NaN into json.dumps, which raises with allow_nan=False -- that would turn today's silent "nan" string into a hard failure. Sequence t

### F83. _iter_rows materialises a tuple AND a dict per row, then _json_each_row_line builds a SECOND dict per row
- WHERE: src/csv_click/pandas_loader.py:648-650 (_iter_rows) and 653-655 (_json_each_row_line)
- MECHANISM: Each row allocates a 20-element tuple (itertuples), a 20-entry dict, and a second 20-entry dict, plus 20 string-hash lookups for the second dict's row[column]. That is ~3 heap objects and 40+ dict operations per row purely as plumbing. At 5.42M rows this is 5.42M x 2,045 ns = 11 s of pure allocation churn per GB, plus the GC pressure of 16M short-lived dicts.
- FIX: Delete _iter_rows and the two-dict handoff. Minimum viable: inline the cleaners into a single positional loop -- `for values in chunk[columns].itertuples(index=False, name=None): line = encoder(dict(zip(columns, (c(v) for c, v in zip(cleaners, values)))))` -- which removes one dict entirely. Preferred: drop row iteration altogether (finding 1, Native column blocks), or use conv.to_json(orient='records', lines=True) which is C-accelerated and measured at 8.0 us/row vs 25.6 us/row for the hand-rolled loop (3.2x) on the same chunk.
- GAIN: Removing the second dict: ~1,188 ns/row (~2.6% of 45.4 us/row). Removing both dicts and the row loop via Native: the whole 25.6 us/row serializer becomes 4.3-4.9 us/row. df.to_json as a drop-in JSON accelerator: 25.6 -> 8.0 us/row measured (3.2x) wit | EFFORT: S
- RISK: df.to_json does not honour _clean_json_value's semantics (Decimal rendering, allow_nan=False, DateTime isoformat with 'T'), so switching to it changes on-the-wire text for Decimal/Date/DateTime columns and would need per-column pre-stringification first. Any change here breaks tests/test_pandas_loader.py::test_chunk_to_json_each_row_payload_has_no_

### F84. json.dumps + .encode('utf-8') are called once per row instead of once per block
- WHERE: src/csv_click/pandas_loader.py:655 (json.dumps(...).encode("utf-8")) and 467/478 (b"\n".join(lines))
- MECHANISM: json.dumps(obj, **kwargs) with non-default kwargs constructs a fresh JSONEncoder object on every call (see cpython json/__init__.py: the cached encoder is used only when all kwargs are default), so 5.42M rows = 5.42M encoder constructions. .encode('utf-8') per row allocates 5.42M small bytes objects that are then thrown at b'\n'.join. Together: 5.42M x 4,519 ns = 24 s per GB.
- FIX: Hoist a single module-level encoder: _ENCODE = json.JSONEncoder(ensure_ascii=False, allow_nan=False, separators=(",", ":")).encode, build the block as a str with "\n".join(...) and call .encode('utf-8') ONCE per payload. separators=(",",":") also drops 2 bytes per column per row (~40 B/row here, ~8.5% of the 469 B/row payload). Note the byte-size cap in iter_json_each_row_payloads currently measures len(line) in bytes per row, so it must switch to a running estimate or encode-then-measure per block.
- GAIN: 4,519 -> ~2,700 ns/row on the dumps+encode stage (1.7x measured), i.e. ~1.8 us of 45.4 us/row (~4%). separators=(",",":") additionally cuts ~8% of wire bytes for free. Moot if finding 1 lands. | EFFORT: S
- RISK: Switching to a running byte estimate loses the exact-payload-size guarantee that iter_json_each_row_payloads currently provides, which is the thing standing between the user and the 'read limit is reached' error handled at pandas_loader.py:670-673. tests/test_pandas_loader.py::test_json_each_row_payloads_are_split_by_byte_limit (226) and ::test_jso

### F85. .astype("object") on every converted column inflates the in-memory chunk 3.67x and is why raising batch_size crashes
- WHERE: src/csv_click/pandas_loader.py:691-695, 700-704, 706, 711, 716, 718, 722 (every _convert_series branch ends in dtype="object" or .astype("object")), plus 451/473 (lines list accumulated before b"\n".join)
- MECHANISM: object dtype replaces an 8-byte numpy slot with an 8-byte pointer plus a full boxed Python object (28 B for int, 32 B for float, 72+ B for Decimal/Timestamp, 80+ B for datetime.date). Every scalar becomes an individually refcounted heap allocation, which both costs memory and destroys cache locality for the subsequent itertuples pass -- that is a large part of why itertuples measures 333 ns/cell here. Scaling batch_size linearly scales the 3.67x-inflated frame plus max_pending payloads, so the memory ceiling is hit at roughly batch_size x cols x ~48 B + 2 x workers x payload_bytes.
- FIX: Keep native dtypes out of _convert_series: return int64/float64/bool numpy arrays for non-nullable numerics, pandas nullable Int64/Float64 (or object-with-None only for the nullable case), datetime64[s] for DateTime, and leave String columns in the arrow/str dtype read_csv already produced. Feed those column arrays straight into the Native InsertContext (column_oriented=True), which consumes numpy arrays without boxing. Then stream blocks to the executor rather than accumulating a full lines list. This also removes the reason the user cannot raise batch_size.
- GAIN: 3.67x less chunk memory measured (19.1 -> ~5.2 MB at batch 20000; 95.4 -> ~26 MB at batch 100000), which should let batch_size go up ~3-4x without the crash. Secondary CPU gain from cache locality is real but I did not isolate it; the end-to-end vect | EFFORT: M
- RISK: Nullable Int64/Float64 pandas extension dtypes behave differently under .tolist(), pd.isna and itertuples than object dtype, so any code still on the JSON path must be updated in lockstep. tests/test_pandas_loader.py::test_convert_chunk_supports_include_rename_and_nullable_int (line 193) asserts converted['C_SPOSOB_KVIT_0'].map(type).tolist() == ['

### F86. _convert_series uses per-value Python lambdas where a vectorized pandas/numpy operation exists -- DateTime costs 2,677 ns/cell, 17x a vectorized equivalent
- WHERE: src/csv_click/pandas_loader.py:683-722, specifically 706 (Decimal: series.map(convert_value)), 711 (Date: .map + astype), 716 (DateTime: .map(to_pydatetime)), 718 (Bool: .map(convert_value)), 722 (custom types: .map)
- MECHANISM: series.map(lambda ...) is one Python frame per cell. convert_value re-derives nullable/inner_type from the type STRING on every single cell, and _value_to_string runs pd.isna + str + strip on every cell. For 108M cells (1 GB, 20 cols) the DateTime column alone is 5.42M x 2,677 ns = 14.5 s, and the four Decimal columns are 4 x 5.42M x 1,257 ns = 27 s. Total convert_chunk_to_schema measured at 13.31 us/row = 31.4% of the whole pipeline.
- FIX: Hoist unwrap_nullable out of the loop (it is already called once at line 684 -- convert_value at line 706/718 calls it again per value; pass the pre-split inner_type instead). Then vectorize each branch against dtype=str input: Decimal -> [Decimal(x) for x in col.to_numpy(object)] (still a loop but skips convert_value/_value_to_string entirely: 319 vs 1,257 ns/cell); Date -> pd.to_datetime(col, format='ISO8601').dt.date; DateTime -> pd.to_datetime(col, format='ISO8601').to_numpy('datetime64[s]') and hand the int64 view straight to Native with column_formats[col]='int' (no to_pydatetime, no isoformat); Bool -> col.str.lower().isin(('true','1','yes','y')); String -> col.to_numpy(dtype=object) 
- GAIN: convert_chunk_to_schema 13.31 us/row -> ~3.5-5 us/row on this 20-column shape (measured 2.3x-13.9x per column depending on type; the mix here gives ~3x). Roughly 8-10 us of the 45.4 us/row total. | EFFORT: M
- RISK: Vectorized parsers are stricter about mixed formats than the current per-value path: pd.to_datetime(col, format='ISO8601') raises on a column mixing '2024-01-01 10:00:00.123456' and '2024-01-02 11:00:00', and I confirmed the CURRENT code already fails on exactly that input with a misleading message (see the _first_bad_value finding). Behaviour on m

### F87. Non-nullable String columns pay a pd.isna() call per cell that is guaranteed to be discarded
- WHERE: src/csv_click/pandas_loader.py:686
- MECHANISM: pd.isna dispatch is 78-95 ns per scalar (measured). Swapping the operands to `nullable and pd.isna(value)` makes Python skip the call entirely when nullable is False. For a 1 GB CSV where half the 108M cells are non-nullable String, that is 54M x ~85 ns = 4.6 s of pure waste.
- FIX: Two-line change: hoist the branch out of the lambda -- `if not nullable: return series.astype(str)` / `return series.map(lambda v: None if pd.isna(v) else str(v))`. Combined with finding 2's dtype=str read, the non-nullable case collapses to `series.to_numpy(dtype=object)` with no per-value call at all.
- GAIN: 326 -> 200 ns/cell for the trivial reorder (1.6x on String columns), or 326 -> 21 ns/cell (15.5x) once the input is already str dtype. On a String-heavy file this is 2-5% of total wall time on its own. | EFFORT: S
- RISK: series.astype(str) renders NaN as the string 'nan', which is exactly the bug in finding 2b -- do not apply this shortcut before the dtype=str/keep_default_na=False fix lands, or it will make the corruption faster rather than fixing it. No existing test covers the non-nullable-String-with-NaN case.

### F88. JSONEachRow repeats every column name on every row: 221 of 469 bytes per row (47%) is key text, and it is sent uncompressed
- WHERE: src/csv_click/pandas_loader.py:654 (dict keyed by column name) -> 655 json.dumps; src/csv_click/clickhouse.py:358-363 (raw_insert fmt="JSONEachRow", no compression argument); .venv/Lib/site-packages/clickhouse_connect/driver/httpclient.py:402-410 (raw_insert's compression parameter defaults to None 
- MECHANISM: Every byte of key text is generated by the client (CPU), pushed through TLS (CPU + bandwidth), and re-parsed by ClickHouse (server CPU) 5.42M times. clickhouse_connect declares available_compression = ['lz4','zstd','br','gzip','deflate'] and write_compression = 'lz4' by default, but raw_insert (unlike _prep_insert at httpclient.py:372-374, which does `if context.compression is None: context.compression = self.write_compression`) never consults it, so the fallback silently does not apply to this code path.
- FIX: Smallest possible diff, independent of everything else: pass compression="lz4" (or "zstd") through raw_insert_batch in src/csv_click/clickhouse.py:358-363 -- client.raw_insert(..., compression="lz4"). Note that when compression is set, raw_insert stops prefixing the INSERT statement to the body and moves it to the `query` URL parameter instead (httpclient.py:418-424), so the payload must be the compressed bytes only; compress with lz4.frame.compress in the WORKER thread, not the producer, so it parallelises. Medium diff: switch fmt to JSONCompactEachRow (arrays instead of objects) which removes the 221 B/row of keys with zero code churn elsewhere. Best: Native (finding 1).
- GAIN: compression="lz4" alone: 2.63x fewer wire bytes for +747 ns/row (+1.6% CPU). compression="zstd": 4.25x for +1,803 ns/row. JSONCompactEachRow alone: ~47% fewer bytes and slightly less json.dumps work, with no compression dependency. If the 45 min is b | EFFORT: S
- RISK: The server must accept Content-Encoding: lz4 on INSERT; clickhouse-connect lists it as supported but an intermediate proxy (the same one producing the 'read limit is reached' error handled at pandas_loader.py:670-673) may not. zstd is more widely proxied. Compressing in the producer thread would ADD to the already-serial main-thread CPU -- it must 

### F89. client.insert() without column_type_names issues a DESCRIBE TABLE round trip per insert
- WHERE: .venv/Lib/site-packages/clickhouse_connect/driver/client.py:1150-1151 inside create_insert_context, reached from client.insert() at client.py:967-976 when context is None; relevant to any migration off raw_insert (src/csv_click/clickhouse.py:336-348 insert_batch already has this defect and is dead c
- MECHANISM: DESCRIBE TABLE on a Distributed table is a full request/response over the same TLS connection, serialised ahead of the insert body. At a plausible 50-300 ms RTT for this deployment that is 13-80 s per GB of pure dead time -- 0.5-3% of the current 45 min, but 20-100% of the CPU budget once the serializer is fixed. mappings already carry final_type, so the information is available locally and the round trip is 100% avoidable.
- FIX: Create ONE InsertContext per load with client.create_insert_context(table=f"{db}.{table}", column_names=[m.target_name for m in mappings if m.include], column_type_names=[m.final_type for m in mappings if m.include], column_oriented=True, settings={...}) and reuse it for every block by assigning context.data = columns before each client.insert(context=ctx) call (client.py:967-981 explicitly supports 'Optional reusable insert context to allow repeated inserts into the same table with different data batches'). Note InsertContext is NOT thread-safe (it carries current_row/current_block/insert_exception mutable state), so with worker_count>1 build one context PER WORKER in the existing threading
- GAIN: Eliminates ~271 round trips per GB. 13-80 s saved per load (ESTIMATE, depends entirely on RTT which I cannot measure from here). Also lets query-level settings (async_insert, max_insert_block_size, insert_distributed_sync) be attached once instead of | EFFORT: S
- RISK: Sharing one InsertContext across the ThreadPoolExecutor workers would corrupt current_row/current_block and produce interleaved or truncated blocks -- this is a real, silent-data-loss-shaped hazard, so the per-thread construction is mandatory, not optional. If column_type_names disagrees with the actual table (e.g. the user edited the DDL between C

### F90. strict_preflight re-runs the entire serializer over 200,000 rows and throws the bytes away before the real load starts
- WHERE: src/csv_click/app.py:899-926 (_create_and_load), src/csv_click/pandas_loader.py:409-436 (validate_csv_sample_with_pandas_chunks: `for _payload, _payload_rows in iter_json_each_row_payloads(...): pass`) and 389-406 (validate_csv_with_pandas_chunks, same discard loop)
- MECHANISM: iter_json_each_row_payloads is called only for its side effect of raising CsvLoadError when a single row exceeds max_payload_bytes (pandas_loader.py:458-463). Producing the bytes is the whole cost; the check needs only the LENGTH.
- FIX: For the >50 MB sample path, the payload-size check can be done without json.dumps: compute a per-row upper bound from column max string lengths (a vectorized col.str.len().max() per column) and compare against max_payload_bytes, or serialize only the widest N rows. For the <=50 MB strict path, do the validation and the load in ONE pass -- run convert_chunk_to_schema + serialization once and hold/stream the result rather than discarding it. Both keep the existing wizard UX and the same error messages.
- GAIN: ~9-10 s per 1 GB load (0.4% of 45 min) -- small on its own. For a 50 MB file it halves the total time, and it removes a fixed ~10 s of dead air before the progress log starts moving, which matters because app.py:859-970's progress bar never advances  | EFFORT: S
- RISK: Weakening the preflight means an over-wide row is discovered mid-load instead of before the CREATE TABLE, leaving a partially populated target -- the current code deliberately front-loads the failure. tests/test_pandas_loader.py::test_validate_csv_sample_with_pandas_chunks_validates_types_and_payload (line 270) asserts CsvLoadError with match='sing

### F91. Aggressive option: pyarrow.csv.read_csv + client.insert_arrow bypasses pandas entirely and is ~100x cheaper on client CPU
- WHERE: Replacement for src/csv_click/pandas_loader.py:61-84 (iter_pandas_chunks) + 364-386 (convert_chunk_to_schema) + 439-478 (JSON payloads); target APIs are client.insert_arrow (client.py:1033) / insert_df_arrow (client.py:1056), which route through clickhouse_connect.driver.query.arrow_buffer and apply
- MECHANISM: The current path pays Python-per-cell costs three times (pandas type inference, _convert_series lambdas, JSON row loop). Arrow parses CSV in multi-threaded C++ directly into columnar buffers and ships those buffers with an IPC header -- there is no Python object created per cell at any point. The 45.44 us/row -> 0.42 us/row difference is the entire per-cell interpreter tax disappearing.
- FIX: Add an opt-in fast path (a checkbox next to 'Strict preflight', or automatic when pyarrow is importable and no custom types are in the mappings). Read with pyarrow.csv.read_csv using column_types built from the mappings (pa.decimal128(p, s) for Decimal, pa.date32() for Date, pa.timestamp('s') for DateTime, pa.string() for String, pa.int64/uint64/float64/bool_) and a ReadOptions block_size that produces batches, then client.insert_arrow(full_table, batch, database=...) per batch. Set write_compression='zstd' (or lz4) on get_client so client.py:1052 picks it up. Keep the existing pandas path as the fallback for custom/unsupported types so the wizard UX is unchanged.
- GAIN: 85-118x on client CPU (measured, no network) and 4.5x fewer wire bytes. Client CPU per 1 GB drops from ~246 s to ~2-4 s -- effectively removing the client from the critical path entirely. Real end-to-end gain will be bounded by the network and by Cli | EFFORT: L
- RISK: Biggest semantic change of everything proposed. (1) Arrow's CSV parser has different NULL/empty-string and quoting semantics from pandas, so the schema inferred by analyze_csv_with_pandas_sample (which uses a dtype=str pandas preview) may not match what Arrow produces -- a divergence of the same family as finding 2. (2) Arrow has no equivalent for 

### F92. _first_bad_value re-scans the column and reports the wrong value in conversion errors
- WHERE: src/csv_click/pandas_loader.py:725-731 (_first_bad_value), called from 381 in convert_chunk_to_schema's except handler
- MECHANISM: Diagnostic cost, not throughput: _first_bad_value re-scans up to batch_size values with a Python loop (measured ~510 ns/cell for the Decimal path) on the error path only. The real cost is operator time: on a 5.4M-row load the user is pointed at the wrong row.
- FIX: Have _convert_series raise with the actual offending value in hand. For the vectorized branches use errors='coerce' first, locate the NaT/NaN positions with a boolean mask, and report series.iloc[mask.idxmax()] plus its absolute row number (chunk_number-1)*batch_size + position. Drop _first_bad_value's independent re-scan so the error and the failure come from the same code path.
- GAIN: No throughput gain. Prevents a class of debugging dead-ends on multi-hour loads, and is a prerequisite for adopting the strict vectorized parsers in finding 7 (which raise on more inputs than today's per-value path). | EFFORT: S
- RISK: Very low. tests/test_pandas_loader.py::test_invalid_manual_type_reports_chunk_column_and_value (line 288) asserts 'chunk 3', 'ID' and 'abc' appear in the message; a row-number addition keeps it passing, but if the message format is restructured that assertion needs updating.

### F93. SHIP THIS: replace the pandas→dict→json row pipeline with pyarrow.csv → Arrow IPC(zstd) → raw_insert(fmt="Arrow")
- WHERE: src/csv_click/pandas_loader.py:481-645 (load_csv_via_raw_insert / _load_csv_via_raw_insert_parallel), src/csv_click/pandas_loader.py:648-655 (_iter_rows/_json_each_row_line), src/csv_click/clickhouse.py:351-363 (raw_insert_batch)
- MECHANISM: Three compounding costs, each measured. (1) Per-row Python object churn: _iter_rows does itertuples + dict(zip()) per row (258,944 rows/s), _json_each_row_line builds a SECOND dict per row via _clean_json_value (167,284 rows/s), then json.dumps+encode (242,880 rows/s). Combined ~1.4-2.6s per 100k rows, all under the GIL. (2) _convert_series (pandas_loader.py:683-722) forces every column to dtype="object" via per-value lambdas — 6.07s for 600k rows — which is also why peak memory is 700-1037 bytes/row. (3) The JSONEachRow text encoding inflates 113 bytes of CSV per row into 237 bytes of JSON, and clickhouse_connect never compresses it (see the raw_insert finding). Arrow collapses all three at
- FIX: 1. Add `ch_type_to_arrow(ch: str) -> tuple[pa.DataType, pa.DataType|None]` in a new src/csv_click/arrow_loader.py returning (parse_type, post_cast_type) — see the mapping-table finding for the exact table. 2. Build once per load:    ReadOptions(block_size=<batch UI value>*<avg row bytes>, encoding=read_options.encoding)   # pyarrow 24 ReadOptions HAS `encoding`; verified working for cp1251, 176-191 MB/s (~20% slower than utf-8)    ParseOptions(delimiter=read_options.separator)    ConvertOptions(column_types={src: parse_type}, include_columns=[included src names],                   strings_can_be_null=True, quoted_strings_can_be_null=False,                   true_values=["true","True","TRUE",
- GAIN: Client CPU for 1 GB: 367 s -> 5.8 s (63x, measured, not estimated). Wire bytes for 1 GB: 2250 MB -> 253 MB (8.9x, measured). Peak memory: 706-1037 bytes/row -> 108 bytes/row (measured), which removes the crash-at-higher-batch failure. End-to-end wall | EFFORT: L
- RISK: (a) NO LIVE SERVER was reachable, so three things are unverified: that this cluster accepts `FORMAT Arrow` into a Distributed table, that its bundled Arrow reader handles IPC-internal zstd bodies, and that Arrow->ClickHouse type conversion behaves for the exotic types users can type into `custom_type`. clickhouse_connect's own client.insert_arrow (

### F94. The ClickHouse->Arrow mapping table (the crux): 13 of 18 CLICKHOUSE_TYPE_OPTIONS map 1:1, 4 need a post-cast, and free-text custom_type is the only real hole
- WHERE: src/csv_click/schema.py:14-33 (CLICKHOUSE_TYPE_OPTIONS), src/csv_click/app.py:722-733 (custom_type free-text column), src/csv_click/pandas_loader.py:345-347 (custom_type overrides final_type)
- MECHANISM: Not a performance finding — this is the feasibility gate for the Arrow proposal. If the mapping had holes in the 18 dropdown options, the approach would be dead. It does not: 13 map 1:1, 4 need a two-line post-cast, and 1 (Bool) needs a ConvertOptions argument.
- FIX: Implement ch_type_to_arrow() returning (parse_type, post_cast_type|None) exactly as tabled above, plus a `guards` entry per column for the range/precision checks. For a type NOT in the table (custom_type escape hatch), pick ONE of:   (a) Parse as pa.string() and let ClickHouse's Arrow reader cast on ingest. Cheap, but UNVERIFIED against this server and it moves error reporting to the server. This is what I would ship, with (b) as the guard rail.   (b) Detect unmapped types up front in mappings_to_schema()/the load entry point and hard-fail with a specific message naming the column and type, per AGENTS.codex.md:27 'fail loudly'. Never silently downgrade.   (c) Staging-table route: Arrow-inser
- GAIN: Feasibility, not throughput. Concretely it buys: strict Decimal/Date/DateTime validation on the client (today's code silently passes overflowing decimals to the server), '' vs NULL fidelity for strings, and optional custom timestamp_parsers for dd.mm | EFFORT: M
- RISK: Two behavioural regressions to accept or paper over: '+42' in an integer column, and 'yes/no' booleans if you forget the true_values/false_values lists. Both are covered above. The custom_type hole is the one that needs a product decision, not a technical one — check with the user how often that box is actually used before choosing (a) vs (b).

### F95. Alternative 2 (server-side CSV parsing) is nearly as fast as Arrow but you must give up the mapping UI or route through input() — recommend REJECT as primary, keep as a future 'raw passthrough' mode
- WHERE: src/csv_click/clickhouse.py:351-363, .venv/Lib/site-packages/clickhouse_connect/driver/httpclient.py:402-431 (raw_insert), .venv/Lib/site-packages/clickhouse_connect/driver/httpclient.py:447-500 (command)
- MECHANISM: Not slow — this is the second-fastest option measured. It is rejected on integration risk, not speed.
- FIX: Do NOT make this the primary path. If you want it later, ship it as an explicit 'Raw CSV passthrough (fastest, no type checking)' checkbox in the load form that bypasses the mapping/type steps entirely and is only offered when every column is included, unrenamed and typed String. Implementation: quote-parity-aware chunker + zstandard.ZstdCompressor(level=1) + raw_insert(table=..., column_names=None, insert_block=blob, fmt="CSVWithNames", compression="zstd", settings={"input_format_parallel_parsing":1, "input_format_skip_unknown_fields":1, "date_time_input_format":"best_effort"}).
- GAIN: ESTIMATE ~8.6x (45 min -> ~5.2 min) on the derived 0.96 MB/s link, i.e. within noise of Arrow+zstd's 10.1x. The client CPU is 3 s vs Arrow's 5.8 s — a difference of no consequence. | EFFORT: M
- RISK: Same 10 tests break as the Arrow path, plus you would be relying on server-side settings you cannot assert in a unit test. The quoted-newline splitter needs its own dedicated test with an embedded CRLF-in-quotes fixture. Highest-risk failure mode: a mis-split chunk silently inserts a mangled row rather than erroring.

### F96. Alternative 3 (shell out to clickhouse-client / clickhouse-local) — REJECT outright for this deployment
- WHERE: loader.bat:16-38 (bootstrap), src/csv_click/clickhouse.py:74-99 (get_client), src/csv_click/clickhouse.py:366-396 (_validate_certificate_files)
- MECHANISM: N/A — this would be fast (clickhouse-client can push CSV at hundreds of MB/s). It loses on deployability, not throughput.
- FIX: Do not pursue. If the team ever wants a bulk path outside the wizard, document `clickhouse-client --secure --format_csv_delimiter --query 'INSERT ... FORMAT CSV' < file.csv` in README.md as a manual escape hatch for power users on Linux, and leave the Streamlit app alone.
- GAIN: None realisable in this deployment. | EFFORT: L
- RISK: Shipping it would make loader.bat's dependency bootstrap non-hermetic and add a second, differently-authenticated network path to maintain.

### F97. Alternative 4 (Native via clickhouse_connect driverc, or hand-rolled RowBinary) — REJECT: measured 6x slower than Arrow because the pandas conversion cost stays
- WHERE: .venv/Lib/site-packages/clickhouse_connect/driver/transform.py:105-140 (NativeTransform.build_insert), .venv/Lib/site-packages/clickhouse_connect/driver/client.py:932-982 (insert/insert_df), src/csv_click/pandas_loader.py:683-722 (_convert_series)
- MECHANISM: Both Native and RowBinary consume ROW-oriented Python data. The whole cost of the current pipeline is materialising row-oriented Python objects (pandas_loader.py:683-722 forces every column to dtype='object'; peak memory 706-1037 bytes/row measured). Changing the wire format downstream of that does not remove it. Arrow wins because it never creates the Python objects at all — the CSV parser writes directly into columnar C++ buffers.
- FIX: Do not build this. The ONE thing worth stealing from the Native path: client.insert() accepts `column_type_names` and a reusable `InsertContext` (client.py:938-939, 962-971), which avoids a DESCRIBE TABLE round trip per insert. That saving is real but tiny — one round trip per insert against a 0.96 MB/s link — and raw_insert (which the Arrow path uses) never issues a DESCRIBE at all, so the Arrow path already has it.
- GAIN: ESTIMATE ~5.8x (45 min -> ~7.7 min) for Native+lz4 vs Arrow+zstd's ~10.1x. Not worth the rewrite. | EFFORT: L
- RISK: RowBinary specifically: silent data corruption. There is no server-side schema in the RowBinary stream, so a column-order or width mistake writes garbage that inserts successfully.

### F98. Alternative 5 (polars and/or orjson) — REJECT both, measured: orjson buys at most 1.2x and polars is redundant with pyarrow
- WHERE: src/csv_click/pandas_loader.py:653-655 (_json_each_row_line), .venv/Lib/site-packages/streamlit-1.58.0.dist-info/METADATA:42 and :70, .venv/Lib/site-packages/clickhouse_connect/driver/client.py:1056-1113 (insert_df_arrow accepts polars natively)
- MECHANISM: orjson attacks the smallest of the three per-row costs. polars attacks a cost pyarrow has already eliminated.
- FIX: Add neither. Keep requirements.txt at three lines. If you want the mechanical certainty, pin `pyarrow>=14` explicitly in pyproject.toml dependencies and requirements.txt rather than relying on streamlit's transitive pull — that is the only dependency change this whole effort needs, and it is defensive, not additive.
- GAIN: orjson: ~1.2x on client CPU only, ~1.05x end to end. polars: ~0x. Both measured/derived, not guessed. | EFFORT: S
- RISK: Adding polars to requirements.txt risks loader.bat:31-34 failing the `pip install -r requirements.txt` step on a machine behind a proxy that has not whitelisted it — turning a performance improvement into an app that will not start.

### F99. RUN THIS DIAGNOSTIC FIRST: 39 of the 45 minutes are unattributed, and every proposal above is sized against a back-solved link speed
- WHERE: src/csv_click/pandas_loader.py:524 and :574 (raw_insert_batch call sites), .venv/Lib/site-packages/clickhouse_connect/driver/httpclient.py:433-445 (_summary), .venv/Lib/site-packages/clickhouse_connect/driver/summary.py:5-21 (QuerySummary)
- MECHANISM: Unknown — that is the point. Shipping a large rewrite against an unmeasured bottleneck is exactly the failure mode AGENTS.codex.md:29 ('Verify before acting') warns about.
- FIX: Add ~6 lines to the existing insert seam (clickhouse.py:351-363) for one instrumented run:     t0 = time.perf_counter()     summary = client.raw_insert(...)     wall = time.perf_counter() - t0     server_s = int(summary.summary.get('elapsed_ns', 0)) / 1e9     log(f"block: {len(payload)} B, wall {wall:.2f}s, server {server_s:.2f}s, upload {len(payload)/wall/1e6:.2f} MB/s") Read one 1 GB load's log and decide:   server_s / wall  <  0.2  -> network-bound. Ship Arrow+zstd, and ALSO raise load_workers (see the GIL finding) because concurrent streams will help.   server_s / wall  >  0.6  -> server-bound. Ship Arrow+zstd anyway (it removes JSONEachRow parsing, which is the most expensive input form
- GAIN: No direct speedup. It converts the 3-9 minute uncertainty band on the Arrow projection into a number, and it prevents shipping an L-effort rewrite against the wrong bottleneck. | EFFORT: S
- RISK: None. Additive logging behind the existing seam; no test touches the return value of raw_insert_batch (tests/test_clickhouse.py::test_raw_insert_batch_uses_json_each_row only asserts the kwargs).

### F100. raw_insert never falls back to write_compression, so every byte goes out uncompressed — the 30-line fix if the Arrow rewrite is deferred
- WHERE: src/csv_click/clickhouse.py:351-363 (raw_insert_batch), .venv/Lib/site-packages/clickhouse_connect/driver/httpclient.py:402-431 vs :355-388
- MECHANISM: For a 1 GB CSV this ships 2250 MB over a link measured at ~0.96 MB/s. lz4 would make it 662 MB; zstd-1 would make it 347 MB. The compression itself costs ~2 s of CPU for the whole file — 0.5% of the 367 s the pipeline already spends.
- FIX: One argument in src/csv_click/clickhouse.py:358-363:     client.raw_insert(table=..., column_names=..., insert_block=payload, fmt="JSONEachRow", compression="zstd") Note that when compression is truthy, httpclient.py:426 moves the INSERT statement from the body prefix into params["query"] instead — a supported path, but it changes the request shape, so verify against the proxy that produces the 'read limit is reached' error handled at pandas_loader.py:670. Also note the proxy now sees the COMPRESSED size, so the effective payload cap gets ~6.5x more generous — which independently addresses the 'raising batch_size causes a crash' symptom.
- GAIN: ESTIMATE 2.6x with lz4 (45 min -> ~17.6 min) or 3.7x with zstd-1 (45 min -> ~12.1 min), on the derived 0.96 MB/s link. Compare with the full Arrow rewrite's ~10.1x. This is the highest gain-per-line change available in the codebase and should ship IM | EFFORT: S
- RISK: Only tests/test_clickhouse.py::test_raw_insert_batch_uses_json_each_row changes (add the compression kwarg to the assertion). The real risk is the TLS-terminating proxy: if it rejects Content-Encoding: zstd on request bodies, fall back to lz4, then to gzip (2.06x/3.47x/3.47x measured on the CSV; 3.40x for lz4 on JSON). gzip-6 costs 25 MB/s of CPU (

### F101. Arrow is what finally makes the existing worker pool do anything: today 100% of the CPU-bound work is in the main thread holding the GIL
- WHERE: src/csv_click/pandas_loader.py:614-641 (_load_csv_via_raw_insert_parallel producer loop), src/csv_click/pandas_loader.py:556-563 (threading.local client), src/csv_click/app.py:980-981
- MECHANISM: Amdahl. With the serialization in the producer, worker_count>1 can at best hide network latency behind CPU that is itself the bottleneck. The user's default of load_workers=1 (settings.py:32) is therefore not obviously wrong today — raising it would help little.
- FIX: Restructure the producer/consumer boundary in the Arrow rewrite so the worker does BOTH arrow_buffer() and the POST — i.e. submit the pyarrow RecordBatch (or the post-cast Table), not the finished bytes. Compression then happens on N threads in C++ with the GIL released. The pyarrow.csv.open_csv iteration itself must stay in the producer (the reader is not thread-safe), but at 176 MB/s it is no longer the bottleneck. Keep max_pending = worker_count * 2 (pandas_loader.py:555) as backpressure; with Arrow at 108 bytes/row this is now a bounded, small memory footprint instead of a hazard. Also: raise the default load_workers above 1 only AFTER the diagnostic finding tells you the link is per-con
- GAIN: ESTIMATE 2-4x on top of the Arrow gain IF the link turns out to be per-connection limited (case (b) in the diagnostic finding) — i.e. ~4.5 min could become ~1.5-2 min. ZERO additional gain if it is a hard bandwidth cap. Do not promise this number unt | EFFORT: M
- RISK: tests/test_pandas_loader.py::test_load_csv_via_raw_insert_parallel_uses_client_factory asserts `sorted(event[3] for event in progress_events) == [1,2,3,4]` with batch_size=1 — moving serialization into the worker does not change progress semantics, so this one should survive if the progress_callback signature (chunk_number, block_number, block_rows

### F102. The Arrow rewrite fixes two silent data-corruption bugs in the current load path for free
- WHERE: src/csv_click/pandas_loader.py:70-76 (iter_pandas_chunks read_csv), src/csv_click/pandas_loader.py:686 (_convert_series String branch), contrast with src/csv_click/pandas_loader.py:93-100 (preview_csv_rows)
- MECHANISM: Correctness, not speed — but it strengthens the case for the Arrow rewrite over the one-line compression fix, because the compression fix leaves both bugs in place.
- FIX: If the Arrow rewrite ships, this is fixed by construction; add a regression test with a CSV containing NULL / N/A / nan / None literals in a non-nullable String column and assert they round-trip verbatim. If the Arrow rewrite is deferred, patch pandas_loader.py:70-76 to `pd.read_csv(..., dtype=str, keep_default_na=False, na_filter=False)` so the load path matches preview_csv_rows, and change _convert_series' String branch at :686 to return '' rather than str(nan) for missing non-nullable values. Note this will slow the current path slightly (all-string dtype), which is another argument for going straight to Arrow.
- GAIN: Zero throughput. Eliminates a class of silent corruption that no existing test covers — tests/test_pandas_loader.py has no fixture containing the strings NULL, N/A or nan. | EFFORT: S
- RISK: Fixing bug 1 in the pandas path changes what analyze/validate accept for existing users who have (perhaps unknowingly) been relying on 'NULL' -> SQL NULL. Call it out in the changelog. Under the Arrow rewrite the same caveat applies, and ConvertOptions(null_values=[...]) is available as an opt-in if anyone actually wants the old behaviour.

### F103. Memory: the current path peaks at 706-1037 bytes per row, which is the mechanical cause of 'raising batch_size crashes'
- WHERE: src/csv_click/pandas_loader.py:683-722 (_convert_series, every branch ends in .astype("object") or a Python list comprehension), src/csv_click/pandas_loader.py:451-478 (iter_json_each_row_payloads accumulates a list of per-row bytes objects)
- MECHANISM: _convert_series (pandas_loader.py:683-722) explicitly materialises every value as a Python object: `.astype("object")` on five branches, and list comprehensions `[None if pd.isna(v) else int(v) for v in converted.tolist()]` on the integer and float branches. Then _iter_rows builds a dict per row, _json_each_row_line builds a second dict per row, and iter_json_each_row_payloads holds the entire list of encoded lines until the payload cap is hit. Four full materialisations of the same data coexist.
- FIX: Covered by the Arrow rewrite — Arrow never creates the Python objects. Additionally, replace the `batch_size` UI control's meaning: today it is a row count (settings.py:30, default 100,000), which gives users no way to reason about memory. Under Arrow, drive pyarrow.csv.ReadOptions(block_size=...) in BYTES, which makes peak memory a directly controllable, predictable number and makes 'increase this until it crashes' impossible. Keep the label and the widget so the wizard UX is unchanged (app.py's form is untouched); just reinterpret the value, or better, expose it as MB.
- GAIN: 6.5-9.6x reduction in peak load-time memory (measured). Removes the crash ceiling entirely, which in turn means larger blocks, which means fewer HTTP requests and fewer ClickHouse parts per GB — see .codex/.agents/skills/clickhouse-architect/referenc | EFFORT: S
- RISK: Reinterpreting batch_size touches settings.py's persisted AppSettings (settings.py:30) and the migration shim at settings.py:52-53. tests/test_settings.py and tests/test_app_state.py::test_connection_form_exposes_bounded_insert_payload_setting would need review. Safer alternative: leave batch_size as rows and derive block_size = batch_size * measur

### F104. Secondary: full-scan schema inference runs at 5.32 MB/s and would take ~3.2 minutes on the user's 1 GB file
- WHERE: src/csv_click/pandas_loader.py:216-245 (analyze_csv_with_pandas_chunks), src/csv_click/schema.py:126-154 (_ColumnStats.add_value), src/csv_click/app.py:54-55 (SCHEMA_INFERENCE_FULL_SCAN option)
- MECHANISM: Per-scalar Python interpretation of the whole file. It is only 3.2 min against a 45 min baseline, but once the load drops to ~4.5 min it becomes 40% of the total for anyone who picks Full scan.
- FIX: Out of scope for the primary change — flag it as follow-up work. The natural fix once pyarrow is in: run pyarrow.csv with all columns as pa.string() (measured 306 MB/s on this file, 3.3 s per GB) and compute the stats with pyarrow.compute kernels (pc.match_substring_regex for the int/uint tests, pc.strptime for date/datetime, null_count for has_empty) — or simply let pyarrow's own type inference run with no column_types and map the inferred Arrow types back to the CLICKHOUSE_TYPE_OPTIONS vocabulary. Either lands in the 3-10 s range for 1 GB, a 20-60x improvement. Separately: schema.py:354-363 _detect_dialect does `path.read_text(encoding='utf-8-sig')[:8192]` — it decodes the ENTIRE file into
- GAIN: ESTIMATE 20-60x on the Full-scan inference path (3.2 min -> 3-10 s per GB). No effect on the default Fast-sample path beyond the 2.3 s -> ~0.3 s it would also gain. | EFFORT: M
- RISK: Changing inference changes the types the wizard proposes, which is the most user-visible surface in the app. tests/test_schema.py and the six analyze_* tests in tests/test_pandas_loader.py pin the current behaviour precisely (e.g. Bool requires has_explicit_bool_literal, schema.py:329; Decimal scale <=2 -> Decimal(18,2), :336-339). Any reimplementa

### F105. Bound per-chunk memory with a measured formula; today raising batch_size scales resident memory ~5x the chunk's CSV bytes and the payload cap does not bound it
- WHERE: src/csv_click/pandas_loader.py:512-538, 616-638 (producer loop), 364-386 (convert_chunk_to_schema), 443-478 (iter_json_each_row_payloads), src/csv_click/app.py:274-279 (Batch size widget, min_value=1, NO max_value)
- MECHANISM: Memory, not time: the pipeline materializes each chunk three times over (typed DataFrame, object/str DataFrame, per-row bytes) before a single byte is sent. Because `chunk` and `converted` are both reachable, the peak is additive rather than a hand-off. Raising the 'bucket' from 20000 to a few hundred thousand rows on a wide UTF-8 CSV crosses 2-4 GB of commit on a Windows client that also runs Streamlit, Chrome and AV; Python raises MemoryError (or the OS refuses the commit and the process dies). At 20000 rows x ~411 B/row the JSON block is only 8.2 MB, i.e. BELOW the 14.4 MB effective cap — proving the cap is inert at the user's settings and B alone drives both memory and block size.
- FIX: Make the invariant unforgeable rather than trusting the UI number. (a) Derive a memory budget without adding a dependency (psutil is NOT installed): `ctypes.windll.kernel32.GlobalMemoryStatusEx` on Windows / `os.sysconf('SC_AVPHYS_PAGES')*os.sysconf('SC_PAGE_SIZE')` on POSIX, default `MEM_BUDGET = min(2048 MB, available/4)`, overridable via `CSV_CLICK_MEM_BUDGET_MB`. (b) Measure `c` for free from the existing 20-row preview or by counting newlines in the first 64 KiB. (c) Clamp: `B_max = floor((MEM_BUDGET - 3.3*P - 3*W*P) / (6*c))` (K=6 for headroom over the measured 5.0) and `W_max = max(1, min(6, floor(0.25*MEM_BUDGET / (3*P))))`; log the clamp exactly the way app.py:894-898 already logs t
- GAIN: Removes the OOM class entirely (the crash becomes impossible by construction). Memory reduction at current settings is modest (~15%); at large batch settings it is the difference between 4.9 GB and a bounded ~0.4 GB. No throughput change on its own. | EFFORT: M
- RISK: Clamping a user-entered value is a visible behavior change and must be logged; tests/test_app_state.py asserts on log strings inside `_create_and_load`. Fixing the reader chunk to a constant changes the `chunk_number` values in progress logs and in `_raw_insert_error` messages; tests/test_pandas_loader.py:301-345 asserts payload/row splitting. Meas

### F106. batch_size silently changes the DATA written to ClickHouse — reproduced; this is a correctness bug, not a performance one
- WHERE: src/csv_click/pandas_loader.py:70-76 (pd.read_csv with chunksize but no dtype=, no keep_default_na=False), 683-722 (_convert_series), 734-748 (_clean_json_value)
- MECHANISM: Not a speed issue — silent corruption triggered by exactly the knob the user is being told to raise. It also means part of the reported 'crash when raising the bucket' may be a CsvSchemaError (non-nullable integer meeting a blank in a newly-merged chunk) rather than an OOM. Because the exception path drops the tables, the user cannot inspect the partial result to tell which happened.
- FIX: Read everything as text and let `_convert_series` be the single authority on typing: `pd.read_csv(..., dtype=str, keep_default_na=False, na_filter=False, engine="c")`. `preview_csv_rows` (lines 93-100) ALREADY does exactly this (`dtype=str, keep_default_na=False`), so the schema is inferred from text while the load re-infers numerics — that inconsistency is the bug. This also removes per-chunk inference cost. Infer the datetime `format=` once from the sample and pass it explicitly, instead of letting each chunk guess.
- GAIN: Eliminates batch-size-dependent output. Secondary speed win: read_csv stops running type inference per chunk — measured read_csv is 0.17-0.73 s of the 1.5-5.3 s producer budget, so roughly 5-15% producer speedup. The point is correctness. | EFFORT: M
- RISK: HIGH blast radius on the schema path: `analyze_csv_with_pandas_chunks` (line 216) also uses `iter_pandas_chunks` and feeds `_value_to_string`, whose `pd.isna(value)` check will never fire once everything is `str` — empty cells become `""` not NaN, changing `_ColumnStats.has_empty` and therefore whether a column becomes Nullable. tests/test_pandas_l

### F107. The preflight makes the crash happen BEFORE any insert, because sample_rows is tied to batch_size
- WHERE: src/csv_click/app.py:899-917, specifically line 902 `sample_rows = max(SAMPLE_PRECHECK_ROWS, effective_read_options.batch_size)`; src/csv_click/pandas_loader.py:409-436
- MECHANISM: Doubles the cost of the first `sample_rows` rows AND is the first place the enlarged batch allocates. A user who raises the bucket and crashes 'immediately' is almost certainly crashing here, not in the load. Useful discriminator against the user's log: if the last line is 'Validating first CSV rows against selected types...' the crash is preflight; if it is 'Loaded chunk N, block M' it is the load.
- FIX: Decouple: `sample_rows = SAMPLE_PRECHECK_ROWS` (fixed 200_000), read with a small fixed chunk independent of the UI batch_size — the preflight validates types, which has nothing to do with insert block sizing. Once the reader chunk is fixed at a constant this disappears for free. Also stop serializing during preflight: `convert_chunk_to_schema` alone proves convertibility; the only extra thing serialization gives is the 'single row larger than max payload' check (pandas_loader.py:458), replaceable by a max-row-width estimate from the converted frame.
- GAIN: Removes the second full serialization pass over the first max(200k, batch) rows. At the measured producer rate (5.2-6.4 MB-CSV/s) that is 6-40 s saved for a 200k-row sample, and removes the largest allocation the user hits when raising the bucket. | EFFORT: S
- RISK: tests/test_app_state.py::test_create_and_load_uses_sample_validation_for_large_files asserts the literal strings `SAMPLE_PRECHECK_ROWS = 200_000`, `validate_csv_sample_with_pandas_chunks(`, and `"Sample validation finished: first"`. tests/test_pandas_loader.py:255-299 asserts sample-row counting and payload validation; dropping the serialization st

### F108. MemoryError is caught by a bare `except Exception`, reported as an empty message, and then DROPS both tables
- WHERE: src/csv_click/app.py:1003-1011 (`except Exception as exc` -> `_cleanup_after_failed_load` -> `_format_load_error`), 817-825, 828-842; src/csv_click/clickhouse.py:146-161
- MECHANISM: This is why the OOM is experienced as 'a crash' with no diagnosis: 45 minutes of ingested data is destroyed and the message is blank. Worse, the cleanup is itself a heavy ON CLUSTER DDL executed while the process is already out of memory, so it can fail too and mask the original error (the nested handler only logs `Cleanup error: ...`).
- FIX: (a) Catch MemoryError explicitly before the generic handler and emit an actionable message naming the current batch_size, the measured bytes/row and the derived safe batch_size. (b) Change `_cleanup_after_failed_load` to take `inserted_rows` and only drop when `inserted_rows == 0`; otherwise keep the tables, log `Load failed after N rows; tables kept for inspection`, and surface an explicit 'Drop tables' button. (c) Make `_format_load_error` fall back to `type(exc).__name__` when `str(exc)` is empty — AGENTS.codex.md mandates 'Fail loudly: raise specific exceptions with context'.
- GAIN: No throughput change; turns an unexplained data-destroying crash into a diagnosed, recoverable one. Prerequisite for the retry work. | EFFORT: S
- RISK: Leaving tables behind changes the next run: `create_tables` (clickhouse.py:278-286) raises ExistingTableError when both exist, so without the new drop button the wizard dead-ends. tests/test_clickhouse.py covers drop_target_tables/create_tables; tests/test_app_state.py may assert cleanup source text.

### F109. worker_count > 1 is capped by Amdahl on the single-threaded producer: ceiling = 1 + I/P, saturating at W* = I/P workers
- WHERE: src/csv_click/pandas_loader.py:614-641 — read_csv, convert_chunk_to_schema and every json.dumps run in the calling thread; `executor.submit(insert_payload, ...)` (line 628) offloads only `raw_insert_batch`
- MECHANISM: T(W) = N * max(P_produce, I_insert / W). Speedup(W) = (P+I)/max(P, I/W); ceiling = 1 + I/P; knee at W* = I/P. Adding workers past W* buys nothing and costs memory (3*W*P). The `load_workers` widget allows up to 6 (app.py:290) with no indication of where the knee is.
- FIX: Two things, in order. (1) INSTRUMENT: the progress callback already receives `block_rows` and `payload_bytes`; also pass produce-time and insert-time per block and display 'producer X MB/s | network Y MB/s | W* = N'. This one measurement decides everything and costs ~10 lines. (2) SHRINK THE SERIAL SECTION instead of adding workers — see the Native-serialization finding: 20x cheaper, moving W* from ~1 to ~20 so the existing thread pool finally pays off. Then clamp `load_workers` to `min(6, ceil(W*))` and log it.
- GAIN: Instrumentation: 0 speedup but converts a guess into a number. Combined with the 20x producer speedup, W=6 should reach ~5x (82% efficiency measured in the simulation) instead of the current ~1.2x — worker_count becomes a real knob for the first time | EFFORT: S
- RISK: Low for instrumentation. Clamping load_workers is user-visible; tests/test_app_state.py:380 asserts the literal `"worker_count=load_workers"` in `_create_and_load`'s source, so any change to that call site breaks it — rewrite it to assert behavior via a fake loader instead of source text.

### F110. GIL convoy: a compute-bound producer inflates each worker's socket-write GIL reacquisition from 0.5 ms to 14.4 ms, and urllib3 issues ~900 of them per 14.4 MB body
- WHERE: src/csv_click/pandas_loader.py:648-655 (_iter_rows / _json_each_row_line — the GIL-held hot loop); .venv/Lib/site-packages/urllib3/connection.py HTTPSConnection blocksize default = 16384 (verified via inspect.signature)
- MECHANISM: While the producer holds the GIL, ALL W workers stall simultaneously, so lost worker-seconds = W x producer_CPU_time. On top of the Amdahl term there is a convoy tax: each of the ~880 writes per body that lands inside a producer quantum waits up to one 5 ms switch interval (14.4 ms measured in practice on Windows). At a 30% producer duty cycle that is 880 * 0.30 * ~2.5 ms ~= 0.7 s of added latency per payload per worker (estimate, ~7% of a 9 s insert); the simulation showed combined Amdahl+convoy loss of 18% at W=6 with P/I=0.05.
- FIX: Do NOT reach for `sys.setswitchinterval()` — it trades worker latency for producer throughput and both are on the critical path. Shrink the GIL-held section instead (Native serialization). Where GIL-held work is unavoidable, prefer primitives that release it: run compression on the inserter threads (measured to release the GIL), and consider handing `memoryview` slices to the transport so the join copy also disappears.
- GAIN: 5-18% recovered at W>=4, on top of the Amdahl improvement. Not a headline number — included because it is the mechanism behind 'workers don't help' and because it rules out the switchinterval tuning people usually try first. | EFFORT: S
- RISK: None for the measurement; the mitigation is entirely subsumed by the serialization change.

### F111. In-flight payload memory is 3*W*P, not W*P, because raw_insert() concatenates a second full copy of every payload — passing compression= removes that copy for free
- WHERE: .venv/Lib/site-packages/clickhouse_connect/driver/httpclient.py:402-433, specifically line ~422 `insert_block = (query + "\n").encode() + insert_block`; called from src/csv_click/clickhouse.py:351-363; in-flight count set by src/csv_click/pandas_loader.py:555 `max_pending = worker_count * 2`
- MECHANISM: An extra 15 MB memcpy per block is ~5 ms (negligible time) but it doubles transport-side memory exactly when memory is the failure mode, and it happens on the worker thread while holding the GIL for the duration of the copy.
- FIX: Compress in the caller and pass `compression=`: `raw_insert(table=..., column_names=..., insert_block=compressed, fmt="JSONEachRow", compression="zstd")`. This takes the `else` branch, so the INSERT statement moves into `params["query"]` and the concatenation never happens. Then replace the future-count cap with a BYTE semaphore: a `threading.Semaphore` counting `INFLIGHT_MAX // GRANULE` permits, acquire `ceil(len(payload)/GRANULE)` before `executor.submit` and release in the worker's `finally`. Set `INFLIGHT_MAX = min(0.25 * MEM_BUDGET, 3 * W * P)` and keep queue depth at `max(1, W)`. That is the backpressure primitive the design is missing: it bounds bytes, which is what OOMs, instead of b
- GAIN: Memory: removes W*P (91 MB at W=6/P=15.1 MB) and makes the in-flight term provably bounded. Throughput: this same change is what enables compression at all. | EFFORT: M
- RISK: `compression` must be one of `available_compression = ["lz4","zstd","br","gzip","deflate"]` and the server must allow `enable_http_compression` for this user; if the mTLS reverse proxy in front of tp17.wb-bank.ru strips or rejects `Content-Encoding`, the insert fails — needs one live smoke test. tests/test_clickhouse.py asserts the `raw_insert_batc

### F112. Every insert is sent uncompressed; zstd level 1 shrinks the JSONEachRow body 5.84x at 647 MB/s and releases the GIL
- WHERE: src/csv_click/clickhouse.py:351-363 (raw_insert_batch, no compression kwarg), src/csv_click/clickhouse.py:74-99 (get_client, no `compress=` arg); .venv httpclient.py:402 `compression: str | None = None` with no fallback to `self.write_compression` (unlike `_prep_insert` at ~372)
- MECHANISM: The measured 380 KB/s of CSV = ~0.87 MB/s of JSON on the wire. If the link or the proxy is the constraint (the 9.3 s per 8.2 MB block strongly suggests it is), bytes-on-the-wire is the single dominant term — and it is currently 2.2-2.7x LARGER than the source CSV because JSONEachRow repeats every column name on every row (measured inflation 2.20x for 20 ASCII columns, 2.68x for 60 Cyrillic columns).
- FIX: In `raw_insert_batch`, add `compression: str | None = "zstd"`, compress with `zstandard.ZstdCompressor(level=1).compress(payload)` on the worker thread (not the producer — it releases the GIL, so it belongs where it parallelizes), and pass `compression=compression` through to `client.raw_insert`. Guard with a settings toggle and fall back to uncompressed on the first 415/501 response. Prefer zstd-1 over lz4 here: 2.3x better ratio at the same speed.
- GAIN: If the load is wire-bound (the evidence says it is): 5.8x fewer bytes -> a 1 GB CSV goes from ~45 min toward ~8 min on the transport term alone. If the server-side insert is the real constraint, the gain collapses to ~0 — this is the highest-value/lo | EFFORT: S
- RISK: Depends on the proxy accepting `Content-Encoding: zstd`. Older ClickHouse builds accept zstd for HTTP input; if the deployment is old, fall back to lz4 (2.50x, still worthwhile). Must be feature-flagged so a rejected encoding degrades instead of failing the load.

### F113. Do NOT move serialization to a ProcessPoolExecutor — Native serialization is 20x cheaper and 2.5x smaller, which fixes the GIL problem without processes
- WHERE: src/csv_click/pandas_loader.py:439-478, 648-655 (the JSONEachRow serializer to be replaced); .venv/Lib/site-packages/clickhouse_connect/driver/client.py insert()/insert_df()/insert_arrow(); clickhouse_connect.driverc is present in this .venv
- MECHANISM: ProcessPool buys true parallelism but pays 0.82 s x W of spawn, 0.7-1.1 GB of duplicated interpreter RSS, and 0.27 s of pickling per block to offload ~1.3 s of serialization — a ~20% efficiency ceiling for the wide-column case. Native serialization removes 95% of the work outright with zero IPC, zero spawn, zero pickling, keeping the single-process Streamlit model intact. The only ProcessPool design that would actually pay is byte-range sharding of the CSV (each process reads its own [offset, length) and inserts directly), which needs quote-aware newline splitting — high risk and squarely outside 'an optimization, not a rewrite'.
- FIX: SIMPLER AND SUFFICIENT — recommend this: keep the thread pool, replace the payload producer. Use `client.insert(table, data, column_names=..., column_type_names=[m.final_type for m in mappings], database=...)` with a reusable `InsertContext` (avoids a DESCRIBE TABLE round trip per block — client.py accepts `context=`). `insert()` goes through `_prep_insert`, which DOES fall back to `self.write_compression` (lz4 here), so compression comes free with no extra code — that is exactly the httpclient.py:372-vs-402 asymmetry the lead already found. `convert_chunk_to_schema` then no longer needs object dtype; it can hand column-wise lists straight to the driver, which also removes the 3.02x-of-CSV o
- GAIN: Producer: 20x measured on CPU (156k -> 3.1M rows/s), taking the 1 GB producer floor from 2.7-3.3 min to roughly 10-20 s. Wire: 2.54x smaller before compression, ~5x with lz4 on top. Combined with the wire-bound hypothesis, a 1 GB load plausibly lands | EFFORT: L
- RISK: Native typing is strict where JSONEachRow was forgiving: Decimal(p,s) scale, Date range (Native Date is 2 bytes, 1970-2149 — a 1969 or 2200 date JSONEachRow accepted now fails), and Nullable handling all become the driver's business. schema.py:14 CLICKHOUSE_TYPE_OPTIONS must be validated against `get_from_name` for every option. tests/test_pandas_l

### F114. No transient-error retry anywhere: a single HTTP 503 after 44 minutes fails the load and drops both tables
- WHERE: .venv httpclient.py:402-433 raw_insert -> `self._raw_request(insert_block, params, headers, server_wait=False)` with `retries` defaulting to 0; httpclient.py:627-629 `if response.status in (429,503,504): if attempts > retries: self._error_handler(response, True)`; src/csv_click/pandas_loader.py:598-
- MECHANISM: Not slow — brittle. The expected number of transient errors over 290 sequential 9-second HTTPS requests through a corporate mTLS proxy is not small, and every one costs the entire 45-minute load plus the data.
- FIX: Retry per block inside `raw_insert_batch` (clickhouse.py:351) so the policy lives in code this repo owns and existing tests already target that function.   RETRY on: urllib3/OperationalError transport failures, HTTP 429/503/504, and ClickHouse codes 202 TOO_MANY_SIMULTANEOUS_QUERIES, 209 SOCKET_TIMEOUT, 210 NETWORK_ERROR, 252 TOO_MANY_PARTS (longer backoff), 241 MEMORY_LIMIT_EXCEEDED (retry once at half the block size).   DO NOT RETRY: 27 CANNOT_PARSE_INPUT_ASSERTION_FAILED, 53 TYPE_MISMATCH, 60 UNKNOWN_TABLE, 117 INCORRECT_DATA, 497 ACCESS_DENIED — deterministic; retrying only wastes time.   Policy: 5 attempts, `sleep = min(30, 0.5 * 2**n) * random.uniform(0.5, 1.5)`. IDEMPOTENCY: pass `set
- GAIN: Turns a ~45-minute all-or-nothing operation into one that survives transient faults. No steady-state throughput change (retries are off the happy path). `distributed_foreground_insert=1` will make each POST measurably slower — measure it; if the cost | EFFORT: M
- RISK: `insert_deduplication_token` silently DISCARDS a block if the same token is reused with different content, so the token must be derived from (load_id, chunk, block) and the chunking must be deterministic — another argument for a fixed reader chunk. Dedup only holds within `replicated_deduplication_window` (default 100 blocks) / `replicated_deduplic

### F115. The failure path can hang the Streamlit UI for up to 30 minutes
- WHERE: src/csv_click/pandas_loader.py:587-596 cancel_pending, 642-644; src/csv_click/clickhouse.py:87 `send_receive_timeout: 1800`
- MECHANISM: Up to 1800 s of unexplained UI freeze on the exact path the user is most likely to hit.
- FIX: Replace the blocking drain with `done, still = wait(pending, timeout=CANCEL_DRAIN_SECONDS)` (30 s), log how many inserts were abandoned, and let the executor's threads finish in the background. Reduce `send_receive_timeout` for the insert clients specifically — a 15 MB block should never legitimately take 5 minutes, so 300 s is generous and turns a hang into an error. Have `cancel_pending` clear the set so the outer handler is a no-op.
- GAIN: Failure is reported in <=30 s instead of up to 30 min. No happy-path change. | EFFORT: S
- RISK: Abandoning in-flight inserts means the number of rows actually landed is unknown when the error is reported — fine once cleanup no longer drops unconditionally and dedup-token resume exists, but the changes should ship together. Lowering send_receive_timeout could break a legitimately slow `create_tables` ON CLUSTER DDL if the same client is reused

### F116. Every load permanently leaks worker_count+1 urllib3 PoolManagers inside the long-lived Streamlit process
- WHERE: src/csv_click/clickhouse.py:74-99 get_client (no close, no context manager anywhere in src/ or tests/ — verified by grep for `.close()`); .venv httputil.py:129 `all_managers[manager] = int(time.time())`; .venv httpclient.py:791-793 `def close(self): if self._owns_pool_manager: ... all_managers.pop(s
- MECHANISM: Streamlit is a long-lived process and the user is expected to run several loads per session (every 'Test connection' press adds one more via `_test_connection`). Nothing is ever reclaimed: after 5 loads at W=6 that is 35 PoolManagers, up to 280 sockets and their kernel buffers, held for the process lifetime. A slow leak rather than the acute crash, but it compounds with the acute memory pressure from large batch sizes.
- FIX: Own the lifetime explicitly. Keep a list of the clients actually created and close them in a `finally` in `_load_csv_via_raw_insert_parallel`; close the main client in `_create_and_load`'s `finally`. Better, given the retry work: create the W insert clients once per load in a small `InsertClientPool` dataclass with `__enter__/__exit__`, which also gives a natural home for the reusable `InsertContext`. Per AGENTS.codex.md ('Explicit over implicit'), a bare `get_client` with no owner is the wrong shape here.
- GAIN: Bounded socket and memory footprint across repeated loads in one Streamlit session. Roughly 5-30 MB plus 8 TLS sessions reclaimed per leaked client — small per load, unbounded over a session. | EFFORT: S
- RISK: Closing the main client must happen AFTER `_cleanup_after_failed_load` (which uses it). thread-local clients are created lazily inside worker threads, so the close loop needs its own registry rather than reaching into the thread-local.

### F117. The progress callback runs on the producer's critical path and rebuilds the entire log on every block (O(blocks^2))
- WHERE: src/csv_click/app.py:953-970 on_progress -> log() -> _render_load_log; app.py:809-814, 859-871; called from pandas_loader.py:609-610 inside collect_completed, i.e. on the producer thread
- MECHANISM: For the current ~290 blocks the O(n^2) term is small (~26 KB log re-sent 290 times, ~7.5 MB of websocket traffic) and costs maybe 1-3% of producer time. It becomes a real failure mode in the opposite direction from the reported crash: if the user LOWERS the bucket (a natural reaction to the crash) to e.g. 1000 rows, blocks go to ~5800, the cumulative join is ~1.5 GB of string churn and the ForwardMsg traffic is ~1.5 GB, which can back up Streamlit's browser queue and kill the tab. (Estimated from message sizes, not measured end-to-end.)
- FIX: Decouple UI from the pipeline. Workers push `(rows, bytes, t)` onto a small `queue.Queue`; the Streamlit script thread drains it and repaints at most every 0.5 s or every 32 MB, whichever comes first. Cap `log_messages` with `collections.deque(maxlen=200)` and render only the tail. Replace the dead progress bar with a byte-based one: `Path(csv_path).stat().st_size` is already read at app.py:900 and the reader knows its file offset, so use `bytes_read / file_size` — this works for the 1 GB case and yields rows/s, MB/s and ETA from the same counters, which is also the P/I measurement the Amdahl finding needs.
- GAIN: 1-3% of producer time recovered today; removes a quadratic term that becomes dominant at small batch sizes; and gives the user (and this investigation) the throughput readout that is currently missing entirely. | EFFORT: M
- RISK: tests/test_app_state.py::test_create_and_load_uses_sample_validation_for_large_files asserts literal log strings inside `_create_and_load` (`"Strict validation finished:"`, `"Sample validation finished: first"`, `"File is larger than 50 MB; using sample validation"`), and other tests assert inline help text — any restructuring must preserve those e

### F118. A conversion error at a large batch_size becomes an O(batch) stall before the (data-destroying) error is reported
- WHERE: src/csv_click/pandas_loader.py:378-385 (the except handler calls `_first_bad_value(chunk[mapping.source_name], mapping.final_type)`), 725-731 (_first_bad_value)
- MECHANISM: At batch_size = 1_000_000 this is ~1M `pd.isna` dispatches plus 1M `convert_value` calls before any message reaches the user: an estimated 2-10 s stall plus ~40-80 MB of transient boxed objects, allocated exactly when the process is already under memory pressure. Combined with the missing-dtype finding (raising batch_size CAUSES new conversion errors by merging chunks), this is a plausible component of the reported crash: a long unexplained freeze, then a blank-ish error, then both tables dropped.
- FIX: Bound the scan: `for value in series.head(10_000).tolist():` and say so in the message ('first bad value within the first 10000 rows of chunk N'). Better, make the diagnosis vectorized and exact — the branches that raise already know why (`converted.isna()` gives the mask), so pass the offending index through instead of re-deriving it: `idx = converted.isna().idxmax(); bad = series.iloc[idx]`. Include the absolute row number (`chunk_start + idx`) so the user can find the line in the CSV, which the current message cannot tell them.
- GAIN: Turns a 2-10 s stall into <10 ms and makes the error message actionable (exact row number instead of a possibly-wrong first value). No happy-path change. | EFFORT: S
- RISK: tests/test_pandas_loader.py asserts on the wording `Cannot convert chunk N, column 'X', value 'V' to T`; changing the message or the value selection breaks those assertions and they should be updated to assert the new row-number field.


## VERDICTS THAT SURVIVED (24 of 134)

### V1 [conf=high]
- CORRECTED GAIN: 0% throughput. Zero effect on the 45 min / 1 GB (~380 KB/s) measured problem. Real but narrow correctness value: fixes a latent dead-end affecting only users who manually type LowCardinality/Array/Map into the undocumented custom_type free-text field
- CAVEAT: The proposed fix is incomplete and must not be merged as scoped. Three gaps: (1) A LowCardinality-only regex leaves the identical bug in every other type that cannot sit inside Nullable — verified in .venv: _normalize_nullable_type('Array(String)', True) -> 'Nullable(Array(String))' and ('Map(String,String)', True) -> 'Nullable(Map(String,String))', both illegal in ClickHouse. Fix the wrapper gene
- WHY: FACTS CONFIRMED, FRAMING INFLATED. All three cited measurements reproduce exactly in the project .venv, so I cannot refute the mechanism.  (a) Code says what is claimed, verbatim. pandas_loader.py:755-760 `if nullable and not clickhouse_type.startswith("Nullable("): return f"Nullable({clickhouse_type})"`. schema.py:279-282 unwrap_nullable only peels a LEADING `Nullable(`. Reproduced: _normalize_nullable_type('LowCardinality(String)', True) -> 'Nullable(LowCardinality(String))'; unwrap_nullable('LowCardinality(Nullable(String))') -> (False, 'LowCardinality(Nullable(String))'); _convert_series(pd.Series(['a',None]),'LowCardinality(Nullable(String))') -> CsvSchemaError at pandas_loader.py:720-721. End-to-end via the REAL app path (mappings_from_editor_rows -> mappings_to_schema) with custom_type='LowCardinality(String)', nullable=True yields final_type='Nullable(LowCardinality(String))', an

### V2 [conf=high]
- CORRECTED GAIN: Zero throughput gain — 0% of the measured 45 min / 380 KB/s. Correctness only: removes a conditional load abort that fires only when a column contains an integer above 2**64-1. Realistic in THIS dataset (20-digit Russian account numbers), but in the 
- CAVEAT: The plan MUST NOT claim this fix "turns a mid-load abort into a preflight error". For files >50 MB — the entire premise of the finding — strict preflight is validate_csv_sample_with_pandas_chunks (first 200k rows, app.py:901-916), and _convert_series runs inside convert_chunk_to_schema on BOTH the preflight and the real load path, so a range check there fires at the identical row at the identical 
- WHY: The underlying defect is real and I reproduced it end-to-end, but the proposed fix and the claimed gain both need correction.  (a) Cited code is quoted accurately. schema.py:392-393 `_is_uint` is `re.fullmatch(r"\+?\d+", value)` — unbounded digits. schema.py:331-334 `if stats.all_uint: return "UInt64", ""` before the Int64 branch. pandas_loader.py:687-695 is `pd.to_numeric(series, errors="raise")` then `[None if pd.isna(v) else int(v) ...]` with no bound check.  (b) Reproduced in the repo .venv (pandas 3.0.3): a CSV whose 100k-row sample sees only `1,2` infers `('big','UInt64')`; read_csv returns that column as object dtype holding the EXACT Python int (pd.to_numeric does NOT float-truncate in pandas 3.0.3, so the analyst's evidence is accurate); the emitted JSONEachRow line is `{"id": 3, "big": 123456789012345678901234567890}`. Not already fixed anywhere: convert_value (schema.py:248-25

### V3 [conf=high]
- CORRECTED GAIN: Network term only: 3.3x (lz4) / 6.0x (zstd-1) — measured, slightly below the claimed 3.66x/6.51x. END-TO-END is capped by Amdahl at the ~3.4 min producer floor I measured (12.03 MB-JSON/s), so the claim's "1.0x–6.5x" is impossible as an end-to-end fi
- CAVEAT: The 0.97 MB/s "effective uplink" was never measured — it is 2.30 GB divided by the 45 minutes being explained, which is circular. The plan MUST state that the entire gain is conditional on the residual ~40 min being uplink bandwidth rather than server-side ingest throughput, and MUST require an independent one-off measurement (time a single fixed-size raw_insert of an already-built block, uncompre
- WHY: I tried to break this claim and could not. It survives on the facts, but its headline number is inflated and it omits a test-breakage and a real regression risk.  (a) CODE QUOTED ACCURATELY — CONFIRMED. <HOME>\Downloads\codex\projects\csv_to_click\.venv\Lib\site-packages\clickhouse_connect\driver\httpclient.py:402-433 reads exactly as claimed: `def raw_insert(... compression: str | None = None ...)` then `if compression: headers["Content-Encoding"] = compression`. The contrast is real: `_prep_insert` at line 372-375 has `if context.compression is None: context.compression = self.write_compression` / `if context.compression: headers["Content-Encoding"] = context.compression`; raw_insert has no such fallback. <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\clickhouse.py:351-362 `raw_insert_batch` passes only table/column_names/insert_block/fmt — no compression, no 

### V4 [conf=high]
- CORRECTED GAIN: No throughput gain whatsoever on the 45-min / 380 KB/s load - this is on the opt-in Full-scan schema-inference path (default is Fast sample). Real gain: lets the user abort a schema scan that currently must run to completion, estimated at 5-15 min fo
- CAVEAT: The plan MUST reject the proposal's "at minimum, catch RerunException/StopException and convert to CsvReadCancelled" fallback. Per .venv/Lib/site-packages/streamlit/runtime/scriptrunner_utils/script_requests.py:250-257, on_scriptrunner_yield consumes the RERUN request and resets state to CONTINUE before the exception is raised, so swallowing it discards rerun_data.widget_states and kills the queue
- WHY: The core diagnosis survives verification and is in fact understated. Verified quotes: app.py:585-586 `_csv_read_cancel_requested()` reads `st.session_state.get("csv_read_cancel_requested", False)`; app.py:488-490 renders the Stop button AFTER `_apply_csv_path` (483) and only when `"csv_path" in st.session_state` (485-486); app.py:565 `_apply_csv_path` sets the flag to False immediately before `_analyze_csv`, wiping any stale True; app.py:625-648 places four guards plus `cancel_callback=_csv_read_cancel_requested` inside `with st.spinner(...)` with zero st.* calls between them; pandas_loader.py:216-236 only calls `cancel_callback()`.  Streamlit internals confirm and strengthen it. script_runner.py:458-465 docstring: "This function is called every time the app script enqueues a ForwardMsg, which means that most `st.foo` commands - which generally involve sending a ForwardMsg to the fronten

### V5 [conf=high]
- CORRECTED GAIN: Zero throughput gain — 0% off the 45 minutes, 0 KB/s. What it actually buys: eliminates a silent mid-file abort whose dominant trigger is a WebSocket disconnect (disconnect_session -> request_script_stop -> StopException through every `except Excepti
- CAVEAT: The plan MUST state three things. (1) This yields zero throughput improvement — it does not touch the 45-minute/380 KB/s problem and must be sequenced after the per-row JSON serialization and the missing lz4/zstd `compression` on `raw_insert`, which shrink this bug's exposure window by 10-20x. (2) Storing the cancel Event in `st.session_state` recreates the bug: `MemorySessionStorage` defaults to 
- WHY: I tried to refute this and could not. Every cited fact checks out, and on one point the analyst UNDERSTATED the problem.  (a) DOES THE CODE SAY THAT — yes, all verified verbatim: - <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\pandas_loader.py:481-492 — `def load_csv_via_raw_insert(client, csv_path, read_options, database, table, mappings, max_insert_payload_bytes=..., worker_count=1, client_factory=None, progress_callback=None)`. No cancel parameter. Contrast pandas_loader.py:216-220 `def analyze_csv_with_pandas_chunks(csv_path, read_options, cancel_callback=None)` with the checks at :226 and :235 raising `CsvReadCancelled`. - pandas_loader.py:642-644 — `except Exception: cancel_pending(pending); raise` inside the `with ThreadPoolExecutor(...)` at :614. Also :575 and :605 are `except Exception`. - app.py:138-152 — `_create_and_load(...)` called directly from a `st.bu

### V6 [conf=high]
- CORRECTED GAIN: Cosmetic UX only, zero throughput effect. It converts a red inline Streamlit exception box (which leaves the whole wizard and session_state intact and self-heals on the next rerun) into a field-level message. Frequency is at most once per load attemp
- CAVEAT: Do NOT implement it the way the claim prefers. Adding the identifier rule to the errors.append block at app.py:346-354 makes the failure SILENT: those errors are only shown when `submitted` is true (app.py:356-360), otherwise the function returns None and the wizard simply disappears after the type editor with no message. Either move line 96 inside the existing try at 110-128 (safe, one line, pres
- WHY: The underlying defect is REAL and I confirmed it in source, but the claim's mechanism, evidence detail, and gain are all overstated, and its preferred fix variant is worse than the status quo.  CONFIRMED FACTS: - app.py:96 `table_names = build_table_names(distributed_table)` is outside the try at 110-128 whose `except ValueError as exc: st.error(f"DDL parameter error: {exc}")` was plainly meant to catch exactly this class of error. - clickhouse.py:56-58 build_table_names -> quote_identifier; clickhouse.py:61-64 raises ValueError("Unsafe ClickHouse identifier: ...") for anything not matching [A-Za-z_][A-Za-z0-9_]*. - Real asymmetry: database and cluster also hit quote_identifier but via build_create_local_table_sql (clickhouse.py:220-222), i.e. INSIDE the try, so they render a clean "DDL parameter error". Only distributed_table escapes the handler. - No submit gate: _render_connection_and

### V7 [conf=high]
- CORRECTED GAIN: Correctness only — zero throughput gain; contributes nothing to the 45 min / 1 GB problem. Real benefit: stops silent 'nan' string corruption in text columns for CSVs containing NA/N/A/NULL/None/nan/<NA> tokens, and makes Fast sample and Full scan ag
- CAVEAT: The proposed fix silently changes Full-scan inference semantics and the plan must say so. Today a numeric column containing NA infers as Nullable(Decimal(18,2))/Nullable(Int64) and loads NA as NULL; with keep_default_na=False it infers as String, and under the default Fast sample mode it becomes a hard mid-load failure instead. This breaks the common R/pandas-export case where NA means missing. Th
- WHY: I tried to refute this and could not refute the core defect — it reproduces end-to-end in the project's own .venv. But two thirds of the surrounding claim are wrong or undisclosed.  (a) CODE IS QUOTED ACCURATELY. pandas_loader.py:93-100 `preview_csv_rows` passes `dtype=str, keep_default_na=False`. pandas_loader.py:70-76 `iter_pandas_chunks` passes only `sep/encoding/chunksize/usecols` — no `keep_default_na`, no `na_values`, no `dtype`. Line 686 is verbatim: `return series.map(lambda value: None if pd.isna(value) and nullable else str(value))`. app.py:53 `SCHEMA_INFERENCE_SAMPLE`, and the radio at ~459 has `index=0`, so Fast sample is the default; ~635-646 dispatches Full scan -> `analyze_csv_with_pandas_chunks`, else -> `analyze_csv_with_pandas_sample`. `iter_pandas_chunks` is used by the real load path (lines 512, 616), not just validation.  (b) FIX IS NOT IN PLACE. `grep -rn "keep_defa

### V8 [conf=high]
- CORRECTED GAIN: UX only — zero throughput gain; the 1 GB load stays ~45 min. Delivers a moving bar plus rows/s, MB/s, elapsed and ETA where today there is nothing. Real cost ~50-80 lines across src/csv_click/app.py and src/csv_click/pandas_loader.py plus one test up
- CAVEAT: The plan MUST state two things. (1) ETA cannot reuse `start = time.time()` at app.py:864 — that timestamp precedes the preflight block at app.py:899-927, so it charges sample validation (max(200000, batch_size) rows serialized and discarded) to load time and biases every ETA high; take a second timestamp immediately before the load_csv_via_raw_insert call. (2) The byte offset must NOT be added as 
- WHY: DIAGNOSIS: fully substantiated, quoted verbatim. app.py:883 `total_rows = 0`; the sole assignment is app.py:921 inside the `else` of `if file_size_bytes > LARGE_CSV_PRECHECK_THRESHOLD_BYTES` (=50MB, app.py:67/901), itself inside `if strict_preflight:` (899). app.py:962 `if total_rows:` therefore never fires for 1 GB; the bar goes 0 -> `progress.progress(1.0)` at 985, and app.py:964 `metrics.metric("Inserted rows", inserted_rows)` is an undenominated counter. Worse than claimed: `file_size_bytes` is computed only inside `if strict_preflight:`, so with strict preflight disabled even a small file gets no bar.  MECHANISM: verified empirically against the installed pandas 3.0.3 on a 135 MB file. `pd.read_csv(open(p,'rb'), encoding='utf_8', chunksize=20000)` + `fh.tell()` yields monotone near-linear offsets (1572864, 2883584, 4456448, 5767168, ...), quantized to a ~1.3-1.5 MB read-ahead => ~0.

### V9 [conf=high]
- CORRECTED GAIN: Real gain: a ~30-line documentation-hygiene fix, zero throughput impact. It corrects one renamed button (Analyze CSV -> Read CSV), one split screen (Schema -> Column mapping + Type review), one inverted step order, one actively false statement (shard
- CAVEAT: Two constraints the proposal omits. (1) README.md is Russian-language UTF-8 and .codex/checkpoints/2026-06-20-csv-to-click-dev-checkpoint.md:256,264 explicitly records it rendering as mojibake under Windows PowerShell and warns to inspect the bytes directly before editing -- any rewrite must preserve UTF-8 encoding or it corrupts the whole doc for a Windows-only audience. (2) Do NOT fold batch_siz
- WHY: Every factual specific in the claim is verified against the source, and I could not break any of them. (a) `Analyze CSV` occurs in exactly one place in the entire repo -- README.md:179 -- and nowhere in src/, tests/, or loader.bat; the actual control is app.py:431 `st.form_submit_button("Read CSV", ...)`. The `Schema` table does not exist: app.py:675 is `st.subheader("Column mapping")` and app.py:715 is `st.subheader("Type review")`, two editors behind two gates (`Apply column mapping` :692, `Apply types` :744), and README's four columns are split across them while `custom_type` and `sample_values` go undocumented. main() at app.py:76-90 hard-gates csv -> mapping -> types -> connection with early returns, so README steps 2-10 all live in the LAST step; Separator/Encoding specifically moved out of the connection form (tests/test_app_state.py test_connection_form_no_longer_collects_csv_pat

### V10 [conf=high]
- CORRECTED GAIN: ~18 physical lines removed (13 for insert_batch + blank separators + 2 Protocol lines), not 15 — the estimate is honest, slightly understated. That is 0.65% of the 2,783 LOC in src/. Performance gain is exactly zero: no bytes/sec, no seconds off the 
- CAVEAT: Must be sequenced AFTER the transport decision, and the plan must say so. `ClickHouseClient.insert` (clickhouse.py:29) and `insert_batch` (336-348) are exactly the seam a Native-format `client.insert()` path — the leading candidate for the actual 380 KB/s bottleneck — would have to reinstate, along with an `insert` method on every test fake (tests/test_clickhouse.py:26, tests/test_pandas_loader.py
- WHY: I tried to refute this and could not break the factual core. It survives, but two-thirds of the write-up around it is wrong and the item is misfiled in a performance plan.  (a) DOES THE CITED CODE SAY THAT — YES, verbatim. <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\clickhouse.py:25-33:     class ClickHouseClient(Protocol):         def query(self, sql: str): ...         def insert(self, table: str, data, column_names: list[str], database: str): ...         def raw_insert(self, **kwargs): ... clickhouse.py:336-348:     def insert_batch(client, config, distributed_table, schema, rows) -> None:         client.insert(table=distributed_table, data=rows, column_names=schema.column_names, database=config.database)  Reference audit (rg over the WHOLE repo, including the two .ipynb files, which are JSON text and are searched):   `insert_batch` -> src\csv_click\clickhouse.py:

### V11 [conf=high]
- CORRECTED GAIN: Zero performance gain — 0% of the 45-min/1 GB load; the function runs zero times per load. Real gain is -12 lines of src (10 code + 2 separators) and -11 lines of test, plus removal of a trap: a stricter dead policy that, if a future dev wired it int
- CAVEAT: The plan must not describe these as "two entry points for one invariant." They are two different invariants: the dead one raises if EITHER table exists; the live one raises only if BOTH exist and otherwise drops both and continues. It also must not claim drift risk in the future tense — git (c6788c7, 4e18be6^) shows the function never had a src caller in any commit, so it was born dead and the div
- WHY: (a) The cited code says exactly what is claimed. src/csv_click/clickhouse.py:109-118 is verbatim:  def ensure_tables_do_not_exist(client, config, distributed_table, local_table) -> None:     existing = _existing_target_tables(client, config, distributed_table, local_table)     if existing:         found = ", ".join(sorted(existing))         raise ExistingTableError(f"Target table already exists: {found}")  and create_tables at :278-286 is verbatim:     existing = _existing_target_tables(client, config, names.distributed, names.local)     expected = {names.distributed, names.local}     if existing == expected:         found = ", ".join(sorted(existing)); raise ExistingTableError(...)     if existing:         log_callback("Partial target table state found. Dropping target tables before retry.")         drop_target_tables(...)  Repo-wide grep for `ensure_tables_do_not_exist` returns exactly

### V12 [conf=high]
- CORRECTED GAIN: -14 LOC is accurate as stated (verified line-by-line: -5 in schema.py, -9 in pandas_loader.py). But the performance gain is exactly zero: 0 of the 45 minutes, 0 of the 380 KB/s deficit. Net maintainability value is also lower than advertised, because
- CAVEAT: This must not be listed as a performance optimization - it yields 0 seconds on the 1 GB / 45-minute load and belongs in a separate cleanup commit, never bundled with or credited toward the throughput work. Two evidence claims must be corrected before the plan cites them: the two functions are NOT byte-identical (schema.py:375-376 annotates its locals, pandas_loader.py:770-771 does not), and schema
- WHY: The core factual claim survives verification, so I cannot refute it, but two pieces of its evidence are wrong and its placement in a throughput engagement is misleading.  VERIFIED TRUE: Two near-identical private `_duplicates` helpers exist at src/csv_click/pandas_loader.py:769-776 and src/csv_click/schema.py:374-381. Neither has been replaced by Counter (no `from collections import Counter` anywhere in src/; only `collections.abc.Callable` at pandas_loader.py:3). Callers are exactly as claimed: pandas_loader.py:250 (_init_column_stats), pandas_loader.py:297 (mappings_to_schema), schema.py:176 (analyze_csv_schema).  EVIDENCE ERROR 1 - "byte-identical" is false. schema.py:375-376 annotates the locals (`seen: set[str] = set()`, `duplicates: set[str] = set()`); pandas_loader.py:770-771 does not (`seen = set()`, `duplicates = set()`). Functionally identical, textually different. Minor, but t

### V13 [conf=high]
- CORRECTED GAIN: Wire bytes: real and well-substantiated — 2.17x-2.34x expansion over CSV shipped 100% uncompressed today; lz4 removes ~60-80%, zstd-1 ~75-90% for ~1% extra CPU. That part is confirmed.  End-to-end wall clock, corrected for Amdahl against my measured 
- CAVEAT: The "~90% of wall time is wire" premise is derived by SUBTRACTION (45 min total minus client CPU), not measured. Nothing in the evidence separates bandwidth-proportional time from per-request time — Distributed-engine fan-out, ReplicatedMergeTree part commit, and full-body buffering by the intermediary in front of tp17.wb-bank.ru:443 are all invisible to that subtraction, and none of them shrink w
- WHY: I could not refute this. Every load-bearing fact reproduced independently.  (a) CODE SAYS IT, VERBATIM. httpclient.py:409 `compression: str | None = None`; :417-418 `if compression: headers["Content-Encoding"] = compression` — and that is the ONLY use of the parameter for encoding purposes. No `if compression is None: compression = self.write_compression`, in direct contrast to data_insert at :372-373 which does exactly that. httpclient.py:205-207 confirms write_compression='lz4' is set because get_client leaves compress at default. src/csv_click/clickhouse.py:351-363 raw_insert_batch passes table/column_names/insert_block/fmt and nothing else. I re-ran their harness with my own independent HttpClient stub:   no compression -> headers {'Content-Type':'application/octet-stream'}, body = b'INSERT INTO `sandbox`.`t` (`a`, `b`) FORMAT JSONEachRow\n{"a":1,"b":"x"}\n{"a":2,"b":"y"}'   compress

### V14 [conf=high]
- CORRECTED GAIN: Zero throughput gain, correctly stated. Real but currently-latent risk reduction, and the analyst's own quantification is missing. The precise ceiling is payload_bytes / connect_timeout = 14.4 MB / 60 s = ~245 KB/s minimum sustained TLS write rate pe
- CAVEAT: Do NOT raise the shared connect_timeout in get_client to 300. It is the same knob urllib3 uses for TCP connect and the mTLS handshake (connectionpool.py:459 -> conn.connect() -> create_connection(..., self.timeout)), so 300 s makes the synchronous Streamlit "Test connection" button hang five minutes on a blackholed host -- the exact VPN case this finding cites -- and turns _verify_table_exists_on_
- WHY: TITLE CLAIM: SUBSTANTIATED, and in fact understated. Every citation is verbatim correct. Traced end-to-end: raw_insert (httpclient.py:402-432) builds insert_block as bytes -> _raw_request passes kwargs["body"]=data with timeout=self.timeout, retries=self.http_retries (httpclient.py:574) -> urllib3 urlopen sets conn.timeout = timeout_obj.connect_timeout (connectionpool.py:459), writes the ENTIRE body at conn.request() (line 493), and only then sets conn.timeout = read_timeout (line 530). connection.py:438-439 re-stamps sock.settimeout(self.timeout) before the write; and on a FRESH connection the socket is created with the same 60s via _validate_conn -> conn.connect() -> create_connection(..., self.timeout). Either path: 60s governs the body write.  THE ANALYST UNDERSTATED THE SEVERITY. body_to_chunks (urllib3/util/request.py:223-225) for a bytes body returns chunks = (to_bytes(body),) -- 

### V15 [conf=high]
- CORRECTED GAIN: Zero throughput gain (conceded, and confirmed: error path only, ~10 ms). Diagnostic gain is real but narrow: it fixes the wrong-value report only for Date/DateTime columns with heterogeneous timestamp formats. Numeric branches already report the righ
- CAVEAT: The proposed fix must not be implemented as specified — it is broken. (1) `series.iloc[mask.idxmax()]` mixes label and positional indexing and raises IndexError on every chunk after the first, because read_csv(chunksize=) chunks carry a continuing global index (verified: chunk indexed [20000,20001] -> mask.idxmax()==20001 -> "IndexError: single positional indexer is out-of-bounds"); IndexError is 
- WHY: The defect is real and I reproduced it exactly against the installed .venv (pandas 3.0.3). pandas_loader.py:725-731 is quoted correctly, and DataFrame({"ts": ["2024-01-01 10:00:00.123456","2024-01-02 11:00:00"]}) with final_type DateTime yields: "Cannot convert chunk 1, column 'ts', value '2024-01-01 10:00:00.123456' to DateTime: time data \"2024-01-02 11:00:00\" doesn't match format ...". convert_value uses datetime.fromisoformat which accepts both, so the loop finds nothing and returns series.iloc[0]. Not already fixed; no test or doc depends on it.  BUT the finding is materially overstated and the proposed fix is defective.  Scope: the two paths can only disagree for Date/DateTime. Measured, Int64/UInt64/Float64 already report correctly AND pandas adds the in-chunk position ("Unable to parse string \"abc\" at position 2"). Decimal/Bool/String/fallback go through series.map(convert_val

### V16 [conf=medium]
- CORRECTED GAIN: Byte reduction is real but smaller than claimed: lz4 1.99x (not 2.63x), zstd-3 3.33x (not 4.25x), JSONCompactEachRow 46% (claim of 47% confirmed exactly), JSONCompactEachRow+lz4 2.54x. Wall-clock: client CPU is only ~8% of the 45 min (measured produc
- CAVEAT: Setting compression="lz4" re-introduces the exact HTTP header of the code path this repo already abandoned. README.md:6-9 states raw_insert/JSONEachRow was chosen because client.insert()/insert_df() "могут падать" in this HTTP/proxy environment — and client.insert() is precisely the path that sends Content-Encoding: lz4 (httpclient.py:372-375, default write_compression="lz4"). The original Native 
- WHY: I tried to break this one and could not break the mechanism. I did break the gain number and found a blocking risk the analyst missed.  (a) CITATIONS — ALL EXACT, verified by reading the files. - <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\pandas_loader.py:653-655 is verbatim: `cleaned = {column: _clean_json_value(row[column]) for column in columns}` then `json.dumps(cleaned, ensure_ascii=False, allow_nan=False).encode("utf-8")`. Per-row dict keyed by column name. Confirmed. - <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\clickhouse.py:358-363: `client.raw_insert(table=..., column_names=..., insert_block=payload, fmt="JSONEachRow")`. No `compression` argument. Confirmed. - .venv\Lib\site-packages\clickhouse_connect\driver\httpclient.py:402-418: `def raw_insert(..., compression: str | None = None, ...)` then `if compression: headers["Content-Encod

### V17 [conf=high]
- CORRECTED GAIN: Zero throughput gain (the claimant says so himself — accurate). Diagnostic-only, and narrower than pitched: the parser disagreement exists ONLY in the four vectorized branches (Int64/UInt64 via pd.to_numeric, Float64, Date, DateTime via pd.to_datetim
- CAVEAT: The finding is sound but the proposed fix must NOT be implemented as written — it contains two outright bugs and one silent-data-loss risk, all verified against the installed pandas 3.0.3:  1. `series.iloc[mask.idxmax()]` raises IndexError on every chunk after the first. read_csv(chunksize=) yields chunks with a CONTINUING absolute index (verified: chunk 2 of a batch_size=4 read has index [4,5,6,7
- WHY: (a) The cited code says exactly what is claimed. <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\pandas_loader.py:725-731:      def _first_bad_value(series: pd.Series, clickhouse_type: str) -> object:         for value in series.tolist():             try:                 convert_value(_value_to_string(value), clickhouse_type)             except CsvSchemaError:                 return value         return series.iloc[0] if len(series) else ""  called from the except handler at :378-385 which formats "... value '{bad_value}' to {final_type}: {exc}".  I reproduced the claimed output verbatim in the repo's .venv (pandas 3.0.3), not from reasoning:   ERR: Cannot convert chunk 1, column 'ts', value '2024-01-01 10:00:00.123456' to DateTime: time data "2024-01-02 11:00:00" doesn't match format "%Y-%m-%d %H:%M:%S.%f" Mechanism confirmed: datetime.fromisoformat (schema.py:266-270,

### V18 [conf=high]
- CORRECTED GAIN: Real and worth doing, but roughly 2.5-3.5x on wall time, not 3.2-5.4x. Under the claim's own most-favorable assumption (all ~41 non-CPU minutes are byte transfer): lz4 45 min -> ~17 min, zstd-1 45 min -> ~12 min. If any meaningful share of those 41 m
- CAVEAT: The plan MUST state (1) that four existing tests break and how each is updated: tests/test_clickhouse.py:323 (exact-kwargs assertion), tests/test_pandas_loader.py:326 (progress size vs insert_block size diverge), tests/test_pandas_loader.py:352 (measured: lz4 frame expands a 27-byte payload to 41, so the <=30 assertion fails - small payloads must skip compression below a size threshold), and tests
- WHY: MECHANISM: CONFIRMED, not refuted. (a) Every code quote checks out. src/csv_click/clickhouse.py:351-363 raw_insert_batch passes only table/column_names/insert_block/fmt - no compression. .venv/.../httpclient.py:417-418 is verbatim `if compression: headers["Content-Encoding"] = compression` with NO `if compression is None: compression = self.write_compression` fallback, in direct contrast to line 372-373 in the data_insert path. httpclient.py:125-173 shows self.headers never carries a Content-Encoding, so (b) the fix is definitively NOT already in place - JSONEachRow bodies go out 100% uncompressed today.  I reproduced the analyst's stub harness independently against the installed driver and got their exact results, plus one thing they did not report but which matters: when `compression` is truthy, raw_insert moves the statement OUT of the body and into params["query"] (`{'query': 'INSERT

### V19 [conf=high]
- CORRECTED GAIN: PERFORMANCE: approximately zero, and slightly negative. Measured read_csv 0.441 s (no dtype) -> 0.548 s (hybrid map) -> 0.754 s (dtype=str) per 200k x 20 rows; read_csv is 6.2% of the CPU pipeline (0.91 s of 14.68 s) and the pipeline is itself ~10% o
- CAVEAT: The plan MUST state that keep_default_na=False + na_values=[] is not safe on its own: it turns every empty cell into "" instead of NaN, and because _convert_series (pandas_loader.py:685-686) tests pd.isna(value), every Nullable(String) NULL silently becomes an empty string. Verified end to end: current output {"s": null, "n": null} becomes {"s": "", "n": null}. This contradicts README line 226 and
- WHY: The headline defect is REAL and I reproduced it byte-for-byte against the installed venv, so I cannot refute it. The cited code says exactly what is claimed: pandas_loader.py L66-74 iter_pandas_chunks calls pd.read_csv with chunksize/usecols and NO dtype= and NO keep_default_na=, while L93-100 preview_csv_rows (the sole input to analyze_csv_with_pandas_sample, the path used for the 1 GB file) uses dtype=str, keep_default_na=False. Symptoms (a), (b) and (d) reproduce with the exact strings claimed: {"amt": "100000000000000.02"} for input 99999999999999.9999999999; {"code": "nan", "name": "nan"} for literal NULL/N-A; and per-chunk drift where the SAME source column reaches a Nullable(String) target as ['100','200','300'] in chunk 1 and ['400.0', nan, '600.0'] in chunk 2. Not already fixed anywhere.  But four things in the claim are wrong or inflated and must be corrected before this goes i

### V20 [conf=high]
- CORRECTED GAIN: Zero throughput gain -- correctly claimed as such. Robustness gain is real but narrower and differently shaped than described: it is not stall insurance, it is removal of a minimum-sustained-upload-rate floor of payload_bytes/60s (245 KB/s at the 14.
- CAVEAT: The plan must NOT state that this contributes to the user's reported crash when raising batch/bucket above 20000, and must NOT state that it motivated INSERT_PAYLOAD_SAFETY_RATIO. Both are false: iter_json_each_row_payloads (pandas_loader.py:443-478) caps every payload at max_payload_bytes from the separate "Max insert payload, MB" knob, so batch_size does not change HTTP body size; and the repo's
- WHY: CORE FINDING SUBSTANTIATED (and understated), SUPPORTING NARRATIVE REFUTED.  (a) Code quoted accurately. clickhouse.py:86-87 sets connect_timeout=60/send_receive_timeout=1800; httpclient.py:182 builds Timeout(connect=60, read=1800); _raw_request passes it as kwargs["timeout"]. urllib3 is 2.7.0 as cited; connectionpool.py:459 assigns conn.timeout = connect_timeout, :493 writes the entire body inside conn.request(), :518/530 only then swaps in read_timeout; connection.py:436-439 does sock.settimeout(self.timeout) before writing. So the live socket timeout during the body write is 60 s. Confirmed.  MECHANISM AS STATED IS WRONG, AND REALITY IS MORE SEVERE. The claim says "each individual send()/SSL_write inside sendall() must complete within 60 s" and frames it as a stall detector. Measured against a local TLS server (self-signed, tiny SO_SNDBUF/SO_RCVBUF set before listen to force backpress

### V21 [conf=medium]
- CORRECTED GAIN: Zero throughput gain, as claimed — honest, not inflated. Real value is narrow: converts an unverified success message into a verified one, worth roughly a few seconds to a couple of minutes of added wall-clock at the end of a ~45-minute load. It is a
- CAVEAT: The plan MUST state three corrections or the fix will do harm. (1) `SYSTEM FLUSH DISTRIBUTED db.table` is NODE-LOCAL and is wrong here: pandas_loader.py:558-563 creates one client per worker thread via app.py:981 `client_factory=lambda: get_client(config)`, all through the LB at tp17.wb-bank.ru:443, so spool files are spread across many initiator nodes. Use `ON CLUSTER <cluster>` and/or poll `clus
- WHY: (a) CODE CHECK — the core citations are accurate, verbatim.  app.py:985-988:     progress.progress(1.0)     elapsed = time.time() - start     log(f"Load finished: {inserted_rows} rows in {elapsed:.2f} sec.")     status.success(f"Load finished: {inserted_rows} rows in {elapsed:.2f} sec") This runs immediately after `inserted_rows = load_csv_via_raw_insert(...)` (app.py:972-983), with no server query in between.  pandas_loader.py:608 is `rows_count += block_rows`, fed from the future's return value (line 604), which comes from `insert_payload` returning `block_rows` computed client-side by `iter_json_each_row_payloads`. Purely client-side. Confirmed.  `grep -rn "SELECT count\|count()\|FLUSH DISTRIBUTED\|distribution_queue\|insert_distributed_sync\|distributed_foreground_insert\|async_insert"` over the repo (excluding .venv) returns ZERO hits in src/ or tests/ — only two hits in example_pan

### V22 [conf=high]
- CORRECTED GAIN: Bytes on the wire: verified real, 4.3x (lz4) / 7.7x (gzip-6) / 8.3x (zstd-1) on a real 5.67 MB block from this pipeline. End-to-end wall clock: UNKNOWN, honest range 1.0x-3.5x, not the claimed 3.7-6.5x. Hard floor established by measurement: the Pyth
- CAVEAT: The plan MUST state three things. (1) The bandwidth-bound premise is unmeasured and the claim's supporting arithmetic is circular (the 0.97 MB/s "measured uplink" was back-derived from the 45 min it then explains). Before or alongside the change, run the free discriminating experiment: load_workers is a UI number_input (app.py:287) defaulting to 1 (settings.py:32) — a 1-vs-8 A/B on the same file s
- WHY: I tried hard to kill this one and could not kill the core of it. But the headline gain and the prioritization argument in the title are both unsound.  (a) THE CITED CODE IS ACCURATE — verified verbatim. `.venv/Lib/site-packages/clickhouse_connect/driver/httpclient.py`, `raw_insert` begins at line 402; line 409 is `compression: str | None = None`, and lines 417-418 are `if compression:` / `headers["Content-Encoding"] = compression` (the claim said 414 — a harmless 3-line miscitation). The contrast with `_prep_insert` at lines 372-375 is real:     if context.compression is None:         context.compression = self.write_compression `raw_insert` has no such fallback. I also read `_raw_request` (line 539 onward): it does `dict_copy(self.headers, headers)` and `kwargs["body"] = data` with no body compression anywhere. So the JSONEachRow payload genuinely goes out uncompressed. `available_compr

### V23 [conf=high]
- CORRECTED GAIN: Zero throughput gain — and net-negative on wall time if `distributed_foreground_insert=1` is adopted as specified. Real gain is reliability only, and unquantified: no 503/429/504 has been observed on this cluster, so the honest statement is "closes a
- CAVEAT: The plan must NOT ship retry-on-timeout without resolving idempotency, and must not ship `distributed_foreground_insert=1` blind. Three things have to be stated: (1) `clickhouse_connect/common.py:74` defaults `invalid_setting_action` to `"error"` and `client.py:279` raises `ProgrammingError` for an unrecognized setting, so `distributed_foreground_insert` (or the `insert_distributed_sync` alias) wi
- WHY: (a) CODE CHECK — every citation is accurate, verbatim.  - `.venv/.../clickhouse_connect/driver/httpclient.py:432` (inside `raw_insert`, def at :402): `response = self._raw_request(insert_block, params, headers, server_wait=False)` — no `retries=` kwarg. - `httpclient.py:545`: `retries: int = 0` in the `_raw_request` signature. Contrast `httpclient.py:287/293/296/344/661/684`, which all pass `retries=self.query_retries` (default 2) — reads get retries, inserts do not. - `httpclient.py:627-628`: `if response.status in (429, 503, 504): / if attempts > retries:` → `_error_handler(response, True)`. `attempts` is incremented to 1 at the top of the loop, `retries=0`, so `1 > 0` on the very first response and it raises `OperationalError` immediately. Confirmed. - `httpclient.py:602`: `max_attempts = max(2, retries + 1)` — the only insert-path retry, gated on `remote_close = isinstance(ex.__conte

### V24 [conf=high]
- CORRECTED GAIN: Correctness: real and reproduced — worth fixing, but scope it honestly. Blast radius is NOT every load: only (i) numeric-looking columns mapped to String/Decimal/Bool that contain at least one blank (the str()/_value_to_string branches), and (ii) amb
- CAVEAT: The plan MUST state that the fix as written (`dtype=str, keep_default_na=False, na_filter=False` alone) introduces a NEW silent corruption and cannot be merged in that form: with na_filter=False an empty cell becomes "", and _convert_series's String branch at src/csv_click/pandas_loader.py:686 tests `pd.isna(value)`, which is False for "" — so every Nullable(String) NULL is written to ClickHouse a
- WHY: The CORRECTNESS core is substantiated and reproduces byte-for-byte in this .venv (pandas 3.0.3). The PERFORMANCE sub-claim is refuted and is backwards. The PROPOSED FIX is defective as written and breaks a test the analyst did not mention.  (a) CODE SAYS WHAT IS CLAIMED — verified verbatim. <HOME>\Downloads\codex\projects\csv_to_click\src\csv_click\pandas_loader.py:70-76     reader = pd.read_csv(         csv_path,         sep=read_options.separator,         encoding=read_options.encoding,         chunksize=read_options.batch_size,         usecols=usecols,     ) No dtype=, no keep_default_na=, no na_filter=. Line 686:     return series.map(lambda value: None if pd.isna(value) and nullable else str(value)) Lines 708/713: pd.to_datetime(series, errors="raise"). preview_csv_rows at :93-100 does use dtype=str, keep_default_na=False.  (b) REPRODUCED END-TO-END, not just at the series le

